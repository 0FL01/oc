# M5 — Повседневный workflow

Порядок определяется planning/tasks.json. Читать только текущую задачу и её профильный контракт; задачи с external blocker не блокируют независимые offline ветки. Все тесты ниже — требуемые реализации/запуски, не уже полученные результаты.

## T22 — Ежедневный TUI

Зависимости: T06, T15.

Dynamic models/variants, paged sessions/resume, prompt UX, tool cards/diff and bounded view state; redacted workspace catalogs, primary-agent selection, command dispatch, skill card и Location/session switch action без второго registry.

Проверки: UI02, UI03, UI06; методики в docs/TEST_PLAN.md и planning/acceptance.json.

Выход: evidence/T22/report.md, реализованные tests, code+checkpoint commit. Нельзя закрыть по одному описанию без фактической проверки.

## T23 — DCP TUI и команды

Зависимости: T22, T19.

Context/stats/manual controls и /dcp-compress [focus], outcome notification, counters from runtime, not duplicate state.

Проверки: UI04; методики в docs/TEST_PLAN.md и planning/acceptance.json.

Выход: evidence/T23/report.md, реализованные tests, code+checkpoint commit. Нельзя закрыть по одному описанию без фактической проверки.

## T24 — Интеграция lifecycle

Зависимости: T19, T20, T21, T22.

Full runtime turn loop; один prompt assembler; command expansion через durable SubmitInput; atomic immutable config-generation publication; Location-scoped sessions; MCP/DCP/reasoning cleanup и reload только между turns. Не парсить definitions и не владеть E2E05.

Проверки: STORE05, DCP08; методики в docs/TEST_PLAN.md и planning/acceptance.json.

Выход: evidence/T24/report.md, реализованные tests, code+checkpoint commit. Нельзя закрыть по одному описанию без фактической проверки.

## T25 — Offline coding/compaction E2E

Зависимости: T24, T11.

Ordered checkpoints: configured-workspace A→B offline E2E с native aliases; затем seeded Rust coding E2E; затем compression/restart E2E. Scripted provider, no network/JS runtime/external payment.

Проверки: E2E01, E2E03, E2E05; методики в docs/TEST_PLAN.md и planning/acceptance.json.

Выход: evidence/T25/report.md, реализованные tests, code+checkpoint commit. Нельзя закрыть по одному описанию без фактической проверки.

## T26 — PTY и UX qualification

Зависимости: T23, T25.

PTY paste/resize/Unicode/cancel/panic restoration, slow consumer и long-history paging; inspect actual behavior not only render snapshot.

Проверки: UI01, UI03, UI04, UI05; методики в docs/TEST_PLAN.md и planning/acceptance.json.

Выход: evidence/T26/report.md, реализованные tests, code+checkpoint commit. Нельзя закрыть по одному описанию без фактической проверки.

## T27 — Live пользовательский workflow

Зависимости: T16, T25, T26.

Bounded real coding task, compress/resume, webfetch and codex_web search; optional browser smoke only explicit opt-in. Report remaining models unqualified.

Проверки: E2E02, E2E03, E2E04, MCP06; методики в docs/TEST_PLAN.md и planning/acceptance.json.

Выход: evidence/T27/report.md, реализованные tests, code+checkpoint commit. Нельзя закрыть по одному описанию без фактической проверки.
