# M1 — Первый вертикальный slice

Canonical definitions/dependencies/acceptance ownership находятся в `planning/tasks.json`. Phase содержит T03, T04, T05 и T06.

- **T03:** typed core, MockProvider, cancellation и bounded channels; component tests без преждевременного STORE01 PASS.
- **T04:** SQLite, durable events/blobs, crash semantics и safe owned data root.
- **T05:** headless CLI, persistence/resume и authoritative STORE01/UI05.
- **T06:** минимальный Ratatui client над тем же application handle; final PTY qualification остаётся T26.

Сначала получить один работающий offline input→stream→persist→resume путь, не строить daemon или transport abstraction.
