# M1 — Первый вертикальный slice

Порядок определяется planning/tasks.json. Читать только текущую задачу и её профильный контракт; задачи с external blocker не блокируют независимые offline ветки. Все тесты ниже — требуемые реализации/запуски, не уже полученные результаты.

## T03 — Core и MockProvider

Зависимости: T01.

Typed commands/queries, session worker, scripted provider, cancellation path и bounded channels. Вертикальный input→response без real network.

Проверки: STORE01; методики в docs/TEST_PLAN.md и planning/acceptance.json.

Выход: evidence/T03/report.md, реализованные tests, code+checkpoint commit. Нельзя закрыть по одному описанию без фактической проверки.

## T04 — SQLite и outputs

Зависимости: T03.

DB worker, own data-root lock, durable input/turn/tools/events, bounded blobs и crash cases. Никакой upstream DB migration.

Проверки: STORE02, STORE03, STORE04; методики в docs/TEST_PLAN.md и planning/acceptance.json.

Выход: evidence/T04/report.md, реализованные tests, code+checkpoint commit. Нельзя закрыть по одному описанию без фактической проверки.

## T05 — Headless CLI

Зависимости: T04.

run/sessions/--help, NDJSON, exit codes, saved resume; diagnostics stderr. Не блокировать Cancel на долгом stream.

Проверки: UI05; методики в docs/TEST_PLAN.md и planning/acceptance.json.

Выход: evidence/T05/report.md, реализованные tests, code+checkpoint commit. Нельзя закрыть по одному описанию без фактической проверки.

## T06 — Минимальный Rust TUI

Зависимости: T05.

Prompt/stream/history/cancel/exit в Ratatui, limited viewport, PTY test; тот же application handle.

Проверки: UI01, STORE05; методики в docs/TEST_PLAN.md и planning/acceptance.json.

Выход: evidence/T06/report.md, реализованные tests, code+checkpoint commit. Нельзя закрыть по одному описанию без фактической проверки.
