# M6 — Квалификация и handoff

Порядок определяется planning/tasks.json. Читать только текущую задачу и её профильный контракт; задачи с external blocker не блокируют независимые offline ветки. Все тесты ниже — требуемые реализации/запуски, не уже полученные результаты.

## T28 — Soak и ресурсы

Зависимости: T25, T26.

Fixed synthetic long-session workload, outputs/history/queue/caches/MCP lifecycle, crash injection; measure actual host baseline and regression bounds.

Проверки: LOAD01, LOAD02, LOAD03, LOAD04, STORE03, STORE04; методики в docs/TEST_PLAN.md и planning/acceptance.json.

Выход: evidence/T28/report.md, реализованные tests, code+checkpoint commit. Нельзя закрыть по одному описанию без фактической проверки.

## T29 — Clean build и audit

Зависимости: T28, T02.

Clean new HOME/XDG/no Node/Bun runtime test включая configured workspace, fmt/clippy/tests/build, source/license/secret/compat audit. No importer/release.

Проверки: BUILD02, BUILD03, OPS01, OPS02, OPS04; методики в docs/TEST_PLAN.md и planning/acceptance.json.

Выход: evidence/T29/report.md, реализованные tests, code+checkpoint commit. Нельзя закрыть по одному описанию без фактической проверки.

## T30 — Final handoff и delivery

Зависимости: T29.

Заполнить FINAL по A01–A13 и связать authoritative executable evidence без полного перезапуска suites. Live blockers не скрывать: task отчёта можно закрыть с BUILD_READY_LIVE_BLOCKED, но goal не READY. Reviewed own-branch push и отдельный delivery status.

Проверки: OPS03; методики в docs/TEST_PLAN.md и planning/acceptance.json.

Выход: evidence/T30/report.md, реализованные tests, code+checkpoint commit. Нельзя закрыть по одному описанию без фактической проверки.
