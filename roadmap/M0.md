# M0 — Контракт и foundation

Порядок определяется planning/tasks.json. Читать только текущую задачу и её профильный контракт; задачи с external blocker не блокируют независимые offline ветки. Все тесты ниже — требуемые реализации/запуски, не уже полученные результаты.

## T00 — Preflight и продолжимый журнал

Зависимости: нет.

Проверить host/uid/origin, сохранить исходный diff и source lock; проверить journal utility, выделить own worktree paths. Не запускать paid requests.

Проверки: ENV01, ENV02; методики в docs/TEST_PLAN.md и planning/acceptance.json.

Выход: evidence/T00/report.md, реализованные tests, code+checkpoint commit. Нельзя закрыть по одному описанию без фактической проверки.

## T01 — Workspace и зависимости

Зависимости: T00.

Создать четыре packages, pin actual toolchain, lockfile, minimal Tokio/Ratatui/rusqlite/reqwest/rmcp compile smoke. Не добавлять ORM/DI/HTTP daemon.

Проверки: BUILD01; методики в docs/TEST_PLAN.md и planning/acceptance.json.

Выход: evidence/T01/report.md, реализованные tests, code+checkpoint commit. Нельзя закрыть по одному описанию без фактической проверки.

## T02 — Минимальный source contract

Зависимости: T01.

Проверить pinned sources/licenses; зафиксировать Responses wire, MCP strict version, DCP range schema, user discovery oracle и configured-workspace source paths/tests. Получить representative source-derived fixtures root/instruction/definition order, frontmatter и exact plugin identities с provenance; executable baseline failures фиксировать, не выдавать fixtures за executed parity.

Проверки: OPS01; методики в docs/TEST_PLAN.md и planning/acceptance.json.

Выход: evidence/T02/report.md, реализованные tests, code+checkpoint commit. Нельзя закрыть по одному описанию без фактической проверки.
