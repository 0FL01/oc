# M3 — OpenProxy без хардкода моделей

Порядок определяется planning/tasks.json. Читать только текущую задачу и её профильный контракт; задачи с external blocker не блокируют независимые offline ветки. Все тесты ниже — требуемые реализации/запуски, не уже полученные результаты.

## T12 — Native Responses stream

Зависимости: T02, T07.

OpenProxy transport и bounded incremental SSE; exact options/base URL; ошибки/EOF/retries/cancel без OAuth/fallback.

Проверки: PROV01, PROV02, PROV06, PROV07; методики в docs/TEST_PLAN.md и planning/acceptance.json.

Выход: evidence/T12/report.md, реализованные tests, code+checkpoint commit. Нельзя закрыть по одному описанию без фактической проверки.

## T13 — Tools, reasoning и images

Зависимости: T12, T09, T10.

Complete tool batch and IDs, native tool execution включая skill из pinned generation snapshot, continuation items, usage/image encoding и generation-bound projection.

Проверки: PROV03, PROV04, PROV05, TOOL10, TOOL11; методики в docs/TEST_PLAN.md и planning/acceptance.json.

Выход: evidence/T13/report.md, реализованные tests, code+checkpoint commit. Нельзя закрыть по одному описанию без фактической проверки.

## T14 — Native discovery user plugin

Зависимости: T12.

Перенести user reference semantics; fake-clock retries и all-or-nothing publication; local snapshot/variants/clamp/source/retired IDs. Связать exact admitted `openproxy-models.js` marker с compiled discovery без исполнения JS. No model hardcode.

Проверки: DISC01, DISC02, DISC03, DISC04, DISC05, DISC06, DISC07, DISC08, DISC09, DISC10; методики в docs/TEST_PLAN.md и planning/acceptance.json.

Выход: evidence/T14/report.md, реализованные tests, code+checkpoint commit. Нельзя закрыть по одному описанию без фактической проверки.

## T15 — Models, variants и admission

Зависимости: T13, T14.

Effective model selection/variant validation, unknown metadata diagnostics, context+output admission and explain; no production automatic model fallback.

Проверки: UI02; методики в docs/TEST_PLAN.md и planning/acceptance.json.

Выход: evidence/T15/report.md, реализованные tests, code+checkpoint commit. Нельзя закрыть по одному описанию без фактической проверки.

## T16 — Первый live OpenProxy gate

Зависимости: T15.

Проверить credentials presence без logging; bounded live text+tool loop на выбранной модели, записать exact deployment/model/variant. Missing env → block только этот task.

Проверки: PROV08; методики в docs/TEST_PLAN.md и planning/acceptance.json.

Выход: evidence/T16/report.md, реализованные tests, code+checkpoint commit. Нельзя закрыть по одному описанию без фактической проверки.
