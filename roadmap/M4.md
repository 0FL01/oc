# M4 — DCP и MCP

Порядок определяется planning/tasks.json. Читать только текущую задачу и её профильный контракт; задачи с external blocker не блокируют независимые offline ветки. Все тесты ниже — требуемые реализации/запуски, не уже полученные результаты.

## T17 — DCP projection и storage

Зависимости: T02, T04, T13.

Core context plan и durable compression generation, DCP policy module, bounded history references; immutable raw transcript.

Проверки: DCP01, DCP08; методики в docs/TEST_PLAN.md и planning/acceptance.json.

Выход: evidence/T17/report.md, реализованные tests, code+checkpoint commit. Нельзя закрыть по одному описанию без фактической проверки.

## T18 — Compress range и protections

Зависимости: T17, T09.

Actual range schema, nested block preservation, anchors/protected paths/tools/users, no-gain guard; upstream-derived regression fixtures.

Проверки: DCP02, DCP03, DCP04, DCP09; методики в docs/TEST_PLAN.md и planning/acceptance.json.

Выход: evidence/T18/report.md, реализованные tests, code+checkpoint commit. Нельзя закрыть по одному описанию без фактической проверки.

## T19 — Nudges и auto-pruning

Зависимости: T18, T07.

DCP config/nudge timing/counters/manual mode; dedup и purge error inputs at compress; exact bare/pinned DCP aliases связывают один compiled module. No generic summarizer or JS hooks.

Проверки: DCP05, DCP06, DCP07; методики в docs/TEST_PLAN.md и planning/acceptance.json.

Выход: evidence/T19/report.md, реализованные tests, code+checkpoint commit. Нельзя закрыть по одному описанию без фактической проверки.

## T20 — Remote MCP codex_web

Зависимости: T02, T07, T13.

rmcp remote client, exact 2025-11-25 POST/JSON path and no-session mode, registry mapping/errors/cancel; fake server mandatory before live.

Проверки: MCP01, MCP02, MCP03; методики в docs/TEST_PLAN.md и planning/acceptance.json.

Выход: evidence/T20/report.md, реализованные tests, code+checkpoint commit. Нельзя закрыть по одному описанию без фактической проверки.

## T21 — Local MCP stdio

Зависимости: T20, T10.

Child lifecycle/stdout/stderr, enabled:false no spawn, argv retained; pinned fake server. Не включать browser/Node автоматически.

Проверки: MCP04, MCP05; методики в docs/TEST_PLAN.md и planning/acceptance.json.

Выход: evidence/T21/report.md, реализованные tests, code+checkpoint commit. Нельзя закрыть по одному описанию без фактической проверки.
