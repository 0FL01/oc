# M2 — Config и tools

Порядок определяется planning/tasks.json. Читать только текущую задачу и её профильный контракт; задачи с external blocker не блокируют независимые offline ветки. Все тесты ниже — требуемые реализации/запуски, не уже полученные результаты.

## T07 — User config и permissions

Зависимости: T02, T05.

Parser/provenance/native profile, selected-only credentials и central permissions; global/Location roots, ordered AGENTS, bounded immutable skill/primary-agent/command catalogs, domain merge и exact native-plugin classification. Unknown/unsupported отклонять до side effects; не исполнять commands/plugins и не активировать future modules здесь.

Проверки: CFG01, CFG02, CFG03, CFG04, CFG05, CFG06, CFG07, CFG08, TOOL09; методики в docs/TEST_PLAN.md и planning/acceptance.json.

Выход: evidence/T07/report.md, реализованные tests, code+checkpoint commit. Нельзя закрыть по одному описанию без фактической проверки.

## T08 — Read и search

Зависимости: T07.

read/glob/grep, stable sorting/pagination, bounded bytes и safe location resolution; source fixtures.

Проверки: TOOL01; методики в docs/TEST_PLAN.md и planning/acceptance.json.

Выход: evidence/T08/report.md, реализованные tests, code+checkpoint commit. Нельзя закрыть по одному описанию без фактической проверки.

## T09 — Единый apply_patch

Зависимости: T08.

Parser Add/Update/Delete/Move включая empty files, preflight/permissions/preimages, per-file commits/partial outcome; no write/edit registry entries.

Проверки: TOOL02, TOOL03, TOOL04; методики в docs/TEST_PLAN.md и planning/acceptance.json.

Выход: evidence/T09/report.md, реализованные tests, code+checkpoint commit. Нельзя закрыть по одному описанию без фактической проверки.

## T10 — Shell supervisor

Зависимости: T07, T04.

Process group/cwd/env policy, concurrent drains, bounded retained output, deadline/cancel/kill/reap. Не persistent shell manager.

Проверки: TOOL05, TOOL06; методики в docs/TEST_PLAN.md и planning/acceptance.json.

Выход: evidence/T10/report.md, реализованные tests, code+checkpoint commit. Нельзя закрыть по одному описанию без фактической проверки.

## T11 — Webfetch

Зависимости: T07.

GET/text extraction, SSRF actual-dial/redirect guards, timeout/size limits, metadata sources и explicit test loopback exception.

Проверки: TOOL07, TOOL08; методики в docs/TEST_PLAN.md и planning/acceptance.json.

Выход: evidence/T11/report.md, реализованные tests, code+checkpoint commit. Нельзя закрыть по одному описанию без фактической проверки.
