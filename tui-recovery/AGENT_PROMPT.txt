Продолжай текущий T44 с актуального Git HEAD, без reset и без нового progress engine. Сначала прочитай tui-recovery/RECON.md, T44_CONTRACT_AMENDMENT.md и IMPLEMENTATION_GUIDE.md, затем progress/NOW.md и текущий diff. Внеси reviewed amendment в активный T44 goal: собственные goldens Rust не доказывают pixel parity; R3/R4 остаются unverified до исправления и парных кадров. Исторические reports не переписывай.

Выполни V00–V09 с существующим checkpoint/commit workflow: pinned original v2.0.12 reference, одинаковые fixtures/state/terminal profile; responsive async submit и понятная безопасная MCP диагностика; реальные DTO title/model names/usage/history parts; full-height viewport/sidebar/conditional devtools; настоящие modal Commands/Select model; keymap/focus/multiline Unicode editor; Markdown tables и одинаковые live/replay tool cards. Не хардкодь screenshot text/models/Free/Context в production, не скрывай required MCP failure и не подменяй tool/runtime результаты для capture.

VIS01–VIS24 из ACCEPTANCE.json — обязательные проверки, все пока NOT_RUN. Проверяй raw PTY input и application effects, а не только вызовы helper. Исправь Ctrl/Alt modifier mask, ожидание app.submit внутри UI input handler, discovered .opencode root escape; остальные negative tests — SAFETY_REGRESSIONS.md. Сохрани Rust2024,4crates,application ownership,permissions/durability/no-JS-production и актуальный T43/T45 subagent scope. Отсутствующие backend actions не рисуй как работающие: mapping/owner decision или честный blocked full-parity gate.

Original executable допускается в отдельной reference среде с Bun/Node; production запрет этого не запрещает. Нет runnable reference/одинакового profile — BLOCKED_REFERENCE, не «pixel-perfect по своим expected». После capture применяй comparator; он не заменяет capture/independent review. Финал: реальные upstream+Rust PNG/styled-cell dumps/diffs, commit/commands/exit codes, все failed/ignored/blocked attempts, fresh full qualification после изменений. T44 verified не означает READY до остальных обязательных product/live gates. Начни с проверки статического findings на текущем SHA и воспроизводимого V00/V01; один slice за раз, после каждого точный checkpoint.
