## Result
Runner-neutral correction completed: active execution docs no longer require GPT/Luna, Codex CLI, a fixed slash command, provider/model or authoring-agent tool API. Prompt path is `prompts/AGENT_GOAL.txt`; OPS05 is assigned to T00; product model discovery and `OC_TEST_MODEL` separation remain unchanged.
## Checks
`python3 scripts/check_docs.py` — exit 0, 31 tasks, 82 acceptance specifications, objective length 697. `python3 -m unittest discover -s scripts -p 'test_*.py' -v` — exit 0, 24 tests. `python3 scripts/progress.py check` — exit 0. `sha256sum -c SHA256SUMS` — exit 0. Task IDs/dependency edges unchanged; scoped active-surface neutrality scan found no hits; `git diff --cached --check` — exit 0.
## Risks
No Rust crate exists in this package, so no product build or Rust acceptance was run. Historical/source-derived Codex references and GPT-named user fixtures remain intentionally outside the active runner contract.
## Next
Stage final checkpoint/manifest updates, inspect the complete staged diff, commit the runner-neutral package correction, and push `agent/oc-rust-port`.
