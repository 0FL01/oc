## Result
T26 PTY qualification done. Real oc tui under openpty: 9/9 PTY tests (smoke, unicode/paste, resize, tiny, cancel, panic restore, ssh-like terms, long history + paging, slow consumer, no-tty refusal) + 1 unit test. Fixed 3 real product bugs: turn-release drain, bottom-align follow, key-drain bound. Evidence evidence/T26/report.md.
## Checks
fmt, clippy -D warnings, workspace green (pty 9/9), build locked, check_docs — all exit 0.
## Risks
SSH-like is local (TERM/size variants), no real sshd; >1024B single-write paste stranding is upstream edge semantics, tests pace chunks.
## Next
T27 live campaign with the harness; T16 stays blocked per note.
