# T44 — source-backed horizontal tab geometry

## Result

Code/test commit `1ca34aa` models the pinned upstream adaptive horizontal
tab window, widths, overflow marker rectangles and clipped hit testing.
Current one-tab renderer consumes the geometry with add capability disabled;
no inert `+` or false session creation is advertised. Four pure layout
regressions and one single-tab render regression passed. Source and diff
review found no actionable discrepancy in the reviewed cases. Factual
paired capture and details: `evidence/tui/recovery-v08-tab-geometry-report.md`
and `evidence/tui/recovery-v08-tab-geometry-01/`.

## Checks

`cargo test --locked -p oc-tui` 189 passed. Full serial workspace suite
0 failures (existing opt-in live ignores); workspace fmt, all-target Clippy
with `-D warnings`, locked build and diff checks PASS. Both pinned original
and Rust execute the equivalent configured Reader/read fixture and real SGR
group disclosure. One-tab title x=0–31,y=0 matches in all 32 styled cells.
Whole frame is still DIFFERENT (completed 211/4,800 styled cells,
5,028/647,040 PNG pixels), no VIS PASS.

## Risks

No native retained multi-tab deck or sessionless Home yet; the pure model
alone does not qualify add-tab, multi-tab, restart, or the required full
VIS01–VIS24/V08–V09. The initial native `NewSession` eagerly creates a
durable session whereas upstream add only opens a Home placeholder. Keep
the add slot disabled until the application owns the correct lifecycle.
T44 remains active.

## Next

Implement and qualify the application-owned sessionless Home acceptance
and bounded retained-tab state before enabling `+`/tab selection; pair its
actual PTY actions with pinned original. Preserve error/permission,
Location-switch, recovery and busy-state guarantees, and rerun full visual
qualification on the final SHA.
