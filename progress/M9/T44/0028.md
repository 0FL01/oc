# T44 — mouse disclosure of real exploration results

## Result

Code `0e85958` adds click-to-expand/collapse of adjacent read/glob/grep
exploration groups. Geometry/viewport-aware hit testing targets only visible
header text; panel clicks and drag-selection cannot toggle it. Expanded member
renders as `→ Read fixture-note.txt` without standalone-result gap, matching
the pinned original's observed text/position in real PTY. Scroll anchoring
retains a clickable header for long results. Failed/denied/unknown/cancelled or
truncated results remain separate; `/cards` still reads application-owned
results. State resets on session/page/workspace replacement. No agent ID
is invented when native config lacks all profiles.

## Checks

Serial `CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=1 cargo test --locked
--workspace --no-fail-fast --quiet` PASS (zero failures, existing live tests
ignored), including 174 oc-tui unit tests. `cargo fmt --all -- --check`,
workspace clippy with `-D warnings`, `cargo build --locked`, Node syntax,
docs/progress checks and diff check PASS.

Pinned original (`2670273ff17da96f85c5826ced57aa1b368754fa`, executable
SHA `2b0825721cb12f9bca3d5099588087d557a21ed2b5b56efebea3f17dc5f79e6a`)
and Rust ran independently with 120x40 shared fixture/profile under real SGR
mouse input. Immutable attempts `evidence/tui/recovery-v08-read-group-click-{01,02,03}/`:
01 exposed native event routing dropping clicks; 02 exposed expanded row
spacing/text difference; 03 on final code has `EXECUTED` and
`provider_contract=true` for both sides, both pass expanded/recollapsed
predicates. See `evidence/tui/recovery-v08-read-group-click-report.md` for
commands and full provenance. Whole-frame comparator remains DIFFERENT:
expanded 4598/4800 cells and 11296/647040 pixels; no VIS gate closed.

## Risks

Full T44 pixel parity is still OPEN: blank-cell foreground, chrome, footer
agent identity and elapsed times differ. Original has implicit Build when the
fixture has no explicit profile; native generation honestly has zero profiles
and no effective default. Do not forge one or equate text row match with
styled-frame equality. The upstream `session.grouping=none` setting and full
read/search/permission/restart matrix have not been qualified.

## Next

Complete S05/S06/S08 and S07 resource qualification and remaining VIS01–VIS24
with paired final-code screens and negative tests. For footer identity compare,
verify equivalent upstream effective profile and explicitly configure a real
native profile rather than synthesizing Build from rendered text.
