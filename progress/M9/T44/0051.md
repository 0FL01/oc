## Result

Pinned original v2.0.12 registers `session.tab.close` in Commands and binds `ctrl+x w`; native now routes the actual palette selection and two-key chord through the existing CAS-backed close action. Bare Home, busy and read-only routes are refused; owner failure retains draft and modal. Real mouse position after accepted keyboard close now re-hit-tests the painted add control for hover only, not for another action. Code/test/runner commit `ec7eb4e`. Paired Reader/tools 120×40 attempts and 160×48 Commands diagnostic, source/hashes/conditions and limitations: `evidence/tui/recovery-v08-tab-close-key-report.md`. Both sides' actual add and keyboard-close PTY predicates, provider contract and unchanged provider counts pass. Final keyboard-closed frame differs only in two real elapsed digits (2/4800 styled cells, 122/647040 pixels); Commands frame still differs 300/7680 cells. The comparator exits 1 DIFFERENT, not a parity PASS.

## Checks

Full serialized locked workspace tests passed (225 TUI tests, 23 pty_t42, 35 binary unit tests, zero failures; existing opt-in live ignores); fmt, all-target workspace Clippy -D warnings, locked build, Node syntax, documentation and progress structure, diff checks PASS. Real PTY+SQLite tests verify keyboard and palette action, busy refusal, no root deletion, reopen from Sessions and restart. Independent diff review found no actionable P1/P2 bug. Executable/original/fixture/terminal locks and actual input/protocol/grid/PNG/VT retained under both keyboard-close attempts; earlier five-cell mismatch and final two-cell result remain visible.

## Risks

T44 remains active; mandatory VIS01–24, V08–V09/S07 not closed. Commands and Models contain other genuine action/catalog differences, error/replay and width cases still require qualification. Dynamic elapsed time, original/native version and unrelated Home randomness are not frozen or spoofed. Pre-existing `.opencode/` untracked and untouched.

## Next

RECON pinned Rename session dialog and actual owner-backed title update, implement with tests and paired interaction rather than adding an inert palette row. Continue other mandatory dialog/model, error, replay and pixel gates, retaining failed paired evidence and source locks.
