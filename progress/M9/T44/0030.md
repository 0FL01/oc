# T44 — V07 S06 pending navigation and unknown replay

## Result

Commit `5c91948` adds a real-PTY PendingSubmit refusal/cancel test and extends
crash recovery to render the durable unknown shell outcome in transcript and
`/cards`. No production defect reproduced. Actual PTY refuses session picker
and Location switch before acceptance, retains draft, provider/turn count zero,
reaps owned MCP child on Esc. Existing unit injection covers old receipt and
stale turn/tool events; streaming A→B→A PTY covers Location-bound data and
global/local replacement. See `evidence/tui/recovery-v07-s06-report.md`.

## Checks

New PTY and extended crash tests PASS; related state/streaming/replay targeted
tests PASS. Workspace all-target clippy, fmt check, serialized workspace tests
(zero failures, existing opt-in live ignores), build and diff check PASS.

## Risks

Refused switch does not deliver an old delta to a new generation; the latter
is a state-injection check, not a real-PTY cross-generation delivery. Unknown
card uses reopened application and TUI TestBackend, not a second binary PTY.
No paired upstream visual pass. T44 active; S08, S07 and VIS01–VIS24 open.

## Next

Qualify S08 old patch preimage/no-follow and partial-commit guarantees, then
owner permissions/subagent cancellations, before S07 resource measures and
full paired T44 frames; record actual commands and negative results.
