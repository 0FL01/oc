# T44 — V07 S05 MCP attach negative qualification

## Result

New real-binary fake-MCP regression in code commit `0abe005` tests disabled
zero-probe, visibly degraded failed authorized attach with no leaked endpoint or
provider tool, and repair followed by a new explicit invocation. No production
defect reproduced: a server ID `required` does not create a fatal `required`
config mode. Current D13 policy degrades that server visibly while the turn
continues; neither failed nor disabled tools reach the provider. Detailed
scenario and source paths in `evidence/tui/recovery-v07-s05-report.md`.

## Checks

Focused new test PASS (1); existing pending/diagnostic PTY tests PASS (5),
disabled stdio child test PASS (1), remote protocol/bearer suite PASS (24, one
live ignored) and disabled stdio adapter PASS (1). `cargo fmt --all -- --check`,
workspace all-target clippy `-D warnings`, serialized full workspace tests (zero
failures, existing live ignores), `cargo build --locked`, diff check PASS.

## Risks

This qualifies the tested S05 scenarios, not all possible MCP endpoints. No
fatal-required mode exists in the current config schema; current explicit
degradation is not a hidden policy bypass. Not a paired visual parity pass.
T44 stays active; S06/S08, S07 resources, remaining VIS01–VIS24 are open.

## Next

Exercise S06 PendingSubmit Location/session switch refusal through real PTY,
then late event isolation and A→B→A persistence on current SHA; continue
S08/S07 and paired visual qualification without relaxing frozen outcomes.
