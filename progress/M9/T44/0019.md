# T44 catalog smoke and safe-status checkpoint

## Result

Base `73f3f12`. Read-only live `/v1/models` GET with current process inherited **product** key returned HTTP 200 with the selected ID under both native and JS-style User-Agent. Fresh release using that environment and real product config reached Home/exit 0 on isolated data; no prompt was submitted. The owner's release instead reports 401/403 under their zsh. No claim that the keys or shell environments match. Exposed the exact denied status (401 versus 403) through safe typed TUI categories; no fallback model or Responses call was added.

## Checks

Test-first adapter compile failure exit 101 before adding the distinct 403 variant. Final 19 adapter discovery tests, one targeted actual-binary startup test, eight release-binary PTY fixture cases, serialized full workspace tests (five existing ignored), fmt, clippy, release/debug builds, docs/journal/diff checks exit 0. Fresh actual-profile release Home/exit 0 and terminal restored. Two external GETs returned 200; no paid turn, secret/raw body/header capture or authoring config read. Full commands and limits: `catalog-smoke-report.md`.

## Risks

The owner's specific catalog status and server-side policy remain unknown; a working Responses route does not imply catalog listing. Independent T44 resource/safety and all pixel/VIS qualification still open. No weakened validation, static invented model or accepted 401/403.

## Next

The owner's same-shell fresh release category or provider-side status-only catalog trace distinguishes 401 from 403 without sharing credentials. Fix request construction only if that trace demonstrates a mismatch with the owner-supplied discovery contract; otherwise catalog authorization/policy must be corrected at OpenProxy, or an owner-verified explicit configured model must be separately assessed against A03. Independently proceed with V07 S03/S05/S06/S08 and measured resources, then paired VIS01–VIS24; never claim READY from these smoke checks.
