# T44 — V08 paired explicit agent and metadata

## Result

Code/test and opt-in capture fixture commit `2bdef13` introduces a real
configured `Reader` primary profile on both TUI implementations, verifies
its selected name/color and the actual instruction sentinel in Responses
requests, and fixes native metadata flex-gap foreground. Factual independent
attempts and comparison: `evidence/tui/recovery-v08-profile-report.md`,
`evidence/tui/recovery-v08-profile-{01,02,03}/`. Final Home border and
metadata rectangles match, as does the sampled Home example in attempt 03;
Home full frame still differs only on the real application version row.

## Checks

Full serial workspace tests PASS (TUI 184, existing opt-in live ignores),
workspace fmt, all-target clippy -D warnings, build, Node syntax,
docs/progress and diff checks PASS. Attempt 01 correctly rejected mismatched
display-case, attempts 02/03 real fake-provider read and group disclosure
execute both sides; full comparator exit 1, no VIS PASS.

## Risks

The pair verifies selected ID, system instruction, color, model and read,
not all upstream/native permission policies. Home is 118/4,800 styled cells
different on the native 0.1.0 vs reference 2.0.12 version row; changing
the actual binary version to spoof pixels is not a valid parity fix.
Session frame remains 209/4,800 different (real add-tab missing, duration,
version, other chrome). Full multi-tab lifecycle requires a genuine Home
slot before durable session creation; an inert `+` or eager NewSession
intent would mislead. VIS01–VIS24, V08–V09 and S07 residuals remain open;
T44 stays active.

## Next

Continue truthful multi-tab/application fresh-session acceptance architecture
or another evidenced source-backed VIS gap; keep real version identity and
unmasked paired comparator, repeat capture after code change.
