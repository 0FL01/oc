## Result

Commit `2d22a81` preserves a provider-reported generation context pair separately from complete billed-turn usage across runtime, durable history projection and TUI. The first tool-call round of the paired fixture omits usage; the final round reports `(6000,763)`. Native now displays the measured `6.8K (3%)` in completed and reopened sessions without inventing the missing tool-round billed tokens or `tok/s`. Pinned v2.0.12/native real two-root restart in `evidence/tui/recovery-v08-context-usage-01/`; factual scope in `evidence/tui/recovery-v08-context-usage-report.md`. T44 remains ACTIVE; no whole-frame or VIS PASS.

## Checks

Final `CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=1 cargo test --locked --workspace --no-fail-fast --quiet` PASS zero failures (206 TUI, 191 adapter library, existing opt-in live ignores); workspace fmt, all-target Clippy `-D warnings`, locked build, docs/progress structure and diff check PASS. Paired original/native 120x40 Reader/read fixture has `provider_contract=true`, `TAB_RESTART_CHECKS_PASS` both sides, natural quit and replay without new provider requests. Both restored session footer rectangles `(88,38,32,1)` compare exactly (0/32 styled-cell differences) and show `6.8K (3%)`. Full reopened old/second still DIFFERENT at 114/4800 and 115/4800 styled cells, with 51/647040 and 113/647040 pixels; original/native Home 6/4800 cells differ by real version. Runner exit 1 is comparator difference, not an interaction failure.

## Risks

No separate in-flight generation context event is implemented; live *completed* turns reconcile from durable pages, and full usage stays unknown if any round omits it. Remaining blank-space foreground mismatches at `(3..4,3)` and `(5..115,34)` account for 113 restored-session styled differences; real timer glyphs account for the visible PNG differences. VIS01–VIS24 and V08/V09/S07 remain open. Do not manufacture a billed usage, timer, Home example or version to force equality. `.opencode/` pre-existed and was never touched.

## Next

Test-first correct the proven unpainted prompt/transcript blank foreground mismatch against pinned source, repeat immutable paired captures; extend VIS05 geometry matrix 80x24/120x40/160x48 and widths 43/44/119/120/121 with scroll/draft predicates, then other mandatory dialogs/error/replay outcomes. Keep T44 active until every frozen VIS is verified.
