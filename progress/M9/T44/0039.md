# T44 — atomic new root and Location binding

## Result

Code/test commit `0980a33` fixes two independently reproduced storage
failures on the future fresh-tab lifecycle: event insert or Location pref
insert failure used to strand a durable root session. Root/event and bound
root/event/Location pref now commit in one SQLite transaction. Tests confirm
failure rollback, clean retry, idempotent same-Location, rejection of other
Location and rejection of an unbound duplicate. Evidence:
`evidence/tui/recovery-v08-session-binding-report.md`.

## Checks

Targeted storage (19) and runtime integration (46) passed; full serialized
workspace passed zero failures (TUI 189, existing opt-in live ignores).
Workspace fmt, all-target Clippy `-D warnings`, locked build, diff, docs and
progress checks PASS. No new paired capture: storage-only change; most recent
paired frame in `evidence/tui/recovery-v08-tab-geometry-01/` is DIFFERENT.

## Risks

Still no transactional create+accept-first-turn, sessionless Home composer,
retained tab deck or actual clickable `+`. Existing native `NewSession`
still eagerly creates a session. Do not claim VIS PASS or full T44 parity.
T44 remains active; `.opencode/` remains untouched.

## Next

Build the application-owned fresh-turn path with exact admission and
sessionless Home selection, no root on rejection and atomic session/binding/
turn/message on durable acknowledgement. Preserve permission, recovery,
busy and Location guarantees; then add bounded tab views, real click actions
and pinned-original paired PTY evidence. All VIS01–VIS24/V08–V09 remain open.
