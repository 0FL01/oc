# T44 — sessionless Home and atomic first-turn acceptance

## Result

Code/test commit `2447063` introduces a genuinely sessionless bare Home and
`/new`, an application-owned fresh submission, exact Home selection, and a
single atomic commit of root/Location/choice/turn/user message before the
acceptance receipt. Failed admission or injected SQLite failure leaves no
candidate root. Home `/location` switches validate before publishing and do
not create a root; explicit attached-session behavior remains. The test-first
startup regression was fixed without weakening its safe error category.
Report and both immutable paired attempts:
`evidence/tui/recovery-v08-sessionless-home-report.md`.

## Checks

Full serialized locked workspace PASS, zero failures (TUI 194, runtime
integration 56; existing opt-in live ignores). Workspace fmt, all-target
Clippy `-D warnings`, locked build, docs/progress checks, and diff check PASS.
Real PTY + SQLite tests cover abandoned and accepted Home, Location switches,
bad config refusal, and explicit-session compatibility. Paired 120×40 pinned
original/native fake-provider/read/SGR-click capture executed successfully on
both sides; final full frames are DIFFERENT (Home 157/4800 styled cells,
Session 205/4800) and do not constitute VIS PASS.

## Risks

This is not the retained-tab feature: `/new` has no returnable tab view and
the `+` remains unpainted rather than inert. Random Home examples, real app
versions and elapsed times differ. Other required VIS cases and S07 remaining
metrics remain open; T44 stays active. Old untracked `.opencode/` untouched.

## Next

Build bounded retained tab routes with sessionless Home slot, preserve each
tab's draft/history/scroll, define mouse add/select from shared adaptive
geometry, guard pending/active turn and Location/restart, then capture original
and Rust interaction across sizes. Continue unresolved VIS01–VIS24 and V08–V09
with actual independent paired evidence; do not claim full pixel parity until
all gates are verified.
