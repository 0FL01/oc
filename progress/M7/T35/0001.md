## Result

T35 active on base 9daa55c. Actual-binary configured-workspace regressions now
pass 6/6: global/project AGENTS isolation, selected primary prompt, literal
single-pass command, pinned skill body, legacy/agent permission denial, precise
malformed/unknown diagnostics and cross-Location session refusal. User-required
DCP @latest binds the fixed compiled module without JS; exact shared authoring
goal marker is ignored with warning/no capability. AUD18 oracle repairs integrated.

## Checks

Initial RED is evidence/T35/regression.md. Latest parent command `cargo fmt --all
&& cargo test --locked -p oc --test configured_workspace -- --nocapture` exit 0,
6 passed in .36 s. Delegated narrow checks: discovery AUD18 9 pass, adapters 185
pass/3 external ignored, config/defs/DCP unit 117 pass; fmt/clippy/diff checks pass.
These delegated runs precede final integrated workspace gates.

## Risks

T35 not finished until parent runs all integrated suites and reviews the full diff.
Model-visible `compress`, actual model nudge and compression graph remain T36.
Actual TUI panels/selection remain T39; T35 supplies behavior to the shared app.
No live probe of the user's configured provider was made before T42 qualification.
Known authoring goal marker is intentionally no-op, not a supported plugin host.

## Next

Run targeted adapter/config/discovery/runtime suites and full workspace gates;
review exact config/defs/discovery/composition/runtime/application diff and update
native-plugin docs/fixtures. Record report and commits, finish T35, then start T36
without pausing. Current changes include production code, configured_workspace.rs,
evidence and generated T35 progress; Git status is authoritative.
