## Result

T31 started on `e4c7036`; F01 reproduced on the actual executable. Added
`crates/oc/tests/application.rs` with isolated HOME/XDG/project and bounded fake
Responses endpoint. Normal binary never contacted that endpoint. No runtime
implementation changed yet. Audit fragments were already integrated once in
`e4c7036`; do not merge them again.

## Checks

`cargo test --locked -p oc --test application -- --nocapture` exited 101:
`binary never contacted configured endpoint`, 0 passed / 1 failed. See
`evidence/T31/regression.md`. Explicit model limits were added to the fixture
after this run; not rerun yet. No downstream assertion is reported as passed.

## Risks

All audited product gaps remain unresolved. CLI/TUI use MockProvider; UI owns
storage writes and masks errors. Existing Runtime has later-task protocol,
durability and tool issues; wiring it is not evidence those issues are fixed.
Only temp fixtures/fake endpoints are authorized until P0 repairs. No live probe.

## Next

Keep T31 active. Reuse CoreApp's typed inbox/events as the single application
interface with an adapter-owned Runtime worker. Add a shared composition factory
for ordered source config/catalog/provider/Runtime, use it from run/tui, switch
default storage to own XDG namespace, and remove frontend persistence. Run the
new AUD01 test to green, add AUD02 CLI→PTY→restart request-history evidence, then
targeted/workspace checks before finish. Changed files: test and evidence above,
plus generated T31 start/checkpoint state. HEAD is `e4c7036`.
