## Result

T34 in progress on base HEAD 4e07cc7; no implementation commit yet.
Provider typed input/complete terminal/cancellation/bounds/options integrated.
Runtime now uses canonical output items and call_id, journals wire items in
existing turns.result, and atomically commits tool outcome with replayable output.
Actual binary two tool rounds plus separate-process restart preserves opaque
reasoning without stdout/stderr exposure. Image modality is explicitly rejected
by text-only CLI profile. No audit merge rerun, live calls or credentials used.

## Checks

Initial runtime red: evidence/T34/regression.md (exit 101).
Latest `cargo fmt --all && cargo test --locked -p oc --test responses && cargo
test --locked -p oc-adapters --test runtime && cargo test --locked -p oc-adapters
--lib provider::tests` exit 0: 3/3 binary (.37 s), 16/16 runtime (2.85 s),
13/13 provider (1.60 s). Binary cancellation tests exercise silent headers/body
under 1 s and delta before terminal. git diff --check exit 0.
Earlier narrow failures exposed provider-key mismatch and attach-before-accept
contract regression; implementation corrected, original runtime assertions kept.

## Risks

Not final qualification: round exhaustion and MCP attach cancellation regression
remain, as do full direct-consumer fixture tests, binary CLI/PTY/crash smoke,
workspace checks and final diff review. Obsolete string assembler/test still
exist unused by runtime and should be removed/replaced with real typed bound
evidence. Full archive loading and DCP/tool graph projection remain T36/T40.
No READY claim. Provider byte budgets are accounting limits, not measured RSS.

## Next

Continue T34: add AUD11 runtime non-completion/partial-call/max-round tests and
AUD12 MCP attach cancel evidence; replace dormant flattened assembler, check
runtime input bound/error diagnostics and config headers redaction. Then run
updated actual-binary smoke, targeted consumers and applicable workspace gates;
review diff and create report/commit/finish only when complete. Changed paths:
adapters application/composition/config/dcp/provider/runtime/storage/tools and
integration fixtures; oc CLI/bootstrap/headless and application/PTY/durability/
responses tests; generated progress files. Git is authoritative for full list.
