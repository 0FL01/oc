## Result

T36 active verified implementation slice on base 1aac158. Actual `oc` now exposes
glob/grep/compress, executes all through permission + durable intent, sends DCP
anchors/nudges, commits multi-range blocks/tool projection/wire outcome/nudge
reset atomically, and continues from the smaller projection after restart.
Raw history remains unchanged. No live calls, credentials, dependencies or
generic plugin runtime. This is not task/goal completion until final gates/report.

## Checks

Initial actual-binary RED is in evidence/T36/regression.md. Latest targeted runs:
actual binary dcp_runtime 2/2 pass (including deterministic SIGKILL at SQLite
durability sync); adapter runtime 26/26 pass; dcp_atomic 4/4 pass; DCP config
tests 4/4 pass; workspace all-target clippy -D warnings pass before the final
three review fixes. Focused FIFO regression now passes. Final full workspace
tests/clippy/build/docs remain to rerun after those last fixes.

## Risks

T36 does not close later T39 UI controls or T40 full-archive/lifetime bounds.
`showCompression`, notification channel and commands display are parsed/pinned
for T39; model DCP path is qualified here. Exact semantic summary quality cannot
be proven offline, but structural facts/tool graph/protections/no-replay are.
Live T27 remains product-blocked until T42.

## Next

Run final full workspace gates after current review fixes; review full diff,
document DCP/tool bounds and exact supported config, write report mapping F07
and AUD19-AUD21, commit implementation, finish T36, then immediately start T37.
Changed source: oc-core context protection; adapters application/composition/DCP/
files/runtime/storage/tools; runtime/atomic and actual-binary tests; progress.
Git status is authoritative. Audit fragments must not be merged again.
