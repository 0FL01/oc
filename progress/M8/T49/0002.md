## Result
T49 offline model identity/footer and title-attribution fixes pass all workspace checks; evidence/T49/report.md records the results. Live OpenProxy Muse completion remains blocked: actual product-binary smoke failed, then the scoped native Responses adapter diagnostic returned unauthorized for the supplied test API credentials.

## Checks
Workspace fmt, clippy -D warnings, tests, build, oc --help, check_docs and diff --check pass. T27 external guard remains unchanged. Dedicated test-campaign reservations 18 + 2 + 2 = 22 (including a diagnostic that failed locally before network); no automatic retry/reset.

## Risks
No successful external Muse generation or correlation with the owner's provider dashboard can be claimed. Awaiting the existing ten-second title watchdog still delays terminal TurnFinished; early emission was reverted because it broke session title publication and busy ownership. The pre-existing .opencode/ remains unmodified.

## Next
Preserve offline corrections and reservation records. Once working provider authorization is available and a separate bounded campaign is explicitly approved, requalify a single selected Muse turn against provider-side correlated route evidence. Do not rerun this one-shot campaign with the same rejected key.
