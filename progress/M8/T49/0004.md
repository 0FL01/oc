## Result
The owner-requested single bounded OpenProxy check with exact Responses model cx/gpt-6-luna returned typed unauthorized (HTTP 401), just like the separate prior Muse adapter diagnostic. Neither model produced a successful live completion. Offline fixes and title attribution remain verified; evidence/T49/report.md is updated.

## Checks
The new ignored one-shot test compiled and passed its default offline preflight, targeted clippy, fmt, check_docs and git diff --check. The actual one-shot returned 401 in 0.18 seconds, without raw response or credential disclosure. Existing workspace fmt/clippy/tests/build gates passed earlier for the same production code. Four durable reservations total 18+2+2+2=24/24 generation attempts conservatively, even though an earlier two-attempt allocation had no network effect.

## Risks
401 does not establish whether the test key is invalid or the particular OpenProxy routes deny it. No live provider-side model identity or reconciliation with the original dashboard rows can be claimed. The fully reserved campaign cannot be reset or retried automatically. The original untracked .opencode/ remains untouched.

## Next
Retain the offline corrections and durable ledger. Further live qualification requires independently verified access to the relevant OpenProxy route and an explicitly reviewed new bounded campaign, not repetition under these credentials and this campaign.
