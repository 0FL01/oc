## Result
The reissued-key native adapter returned a successful, nonempty OpenProxy Responses generation for the exact wire model cx/gpt-6-luna, with provider usage present. The old rejected-key campaign remains fully reserved and unchanged.

## Checks
New fixed rotated-key campaign currently reserves 2/24 possible generation HTTP attempts. The product-binary one-shot fixture has an isolated project/HOME, no MCP, denied tools, a short prompt, 768 maximum main output and 256 title output, a 90-second watchdog and at most (8 rounds + title)*2 = 18 POST attempts. It preserves the complete slash-containing wire ID via a separate local test provider namespace. A preflight-only run verified its effective selected ID before any new POST; targeted offline tests, clippy, fmt and docs pass.

## Risks
Provider execution identity cannot be proven from self-reported Rust metadata alone, and live behavior may still fail with tools present. The next binary run has unknown external effects until it completes; its reservation is durable and will not be reset or retried automatically. Remaining campaign allowance after reservation will be 4, not a license for unbounded tests.

## Next
Run one exact rotated-key product-binary model-identity smoke under the same campaign and inspect only the sanitized test verdict and stored model ID. Then update evidence and applicable gates.
