## Result
T49: Offline tests prove main wire selection, per-turn TUI footer, and ancillary title cancellation. No live request yet.

## Checks
Targeted PTY, TUI and application tests passed; credentials are present without disclosure; dedicated reservation is initially absent.

## Risks
Upcoming one-shot Muse smoke has a 90-second watchdog and worst-case bound (8 rounds + title, two attempts each = 18 <= 24). Existing T27 external guard remains unchanged. Do not automatically rerun after a reservation or unknown external effect.

## Next
Rerun targeted offline test, then execute the ignored T49 test once with the pre-existing product test credentials. Inspect sanitized outcome and finish the regression gates.
