## Result
T49 scoped model-identity and ancillary-request attribution is complete. The live TUI footer now uses the pinned model label from the accepted turn. Real-binary PTY tests prove selected model IDs on main Responses wire, footer history identity and separate title requests; title cancellation/10-second timeout do not reclassify a completed main turn. Reissued test key enabled a successful native Responses call and a successful isolated oc run for exact cx/gpt-6-luna, followed by a successful native Responses call for the original exact ocg/muse-spark-1.3-contributor ID. Evidence/T49/report.md records outcomes without secrets or raw responses.

## Checks
Workspace fmt --check, clippy --workspace --all-targets -D warnings, cargo test --locked --workspace, cargo build --locked, oc --help, check_docs, diff --check all passed after final changes. The ignored live tests were invoked individually with the reissued credentials and passed. Original rejected-key campaign remains separately reserved at 24/24; new campaign reserves 2+18+2 = 22/24 attempts with durable create-new ledger records. No other live runs or automatic retries.

## Risks
Successful native Responses text with an exact requested ID is not independent proof of the proxy's internal executed model. The owner's old DeepSeek dashboard entries lack request/session correlation. Title generation still awaits its bounded 10-second watchdog before terminal TurnFinished; an early-event experiment was removed after it broke TUI ownership/title publication. The broader T27 coding/MCP live campaign remains fail-closed. Existing untracked .opencode/ was not modified; code changes remain uncommitted.

## Next
Keep the scoped corrections and evidence. Provider-side request IDs would be necessary to attribute the owner's historical dashboard entries, and a separate reviewed budget would be necessary for any further full live turn. Do not equate this narrow text qualification with A04 tool-roundtrip or overall GOAL readiness.
