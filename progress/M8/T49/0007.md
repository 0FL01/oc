## Result
Reissued-key campaign succeeded twice: native adapter generated text for exact cx/gpt-6-luna with usage; isolated product-binary oc completed a turn and stored the same full slash-containing model ID. Earlier key 401 errors were authorization-dependent. The original Muse model has not yet been checked with the reissued key.

## Checks
Rotated-key fixed reservations currently total 2 direct route attempts plus 18 worst-case product attempts = 20/24. Product preflight before generation selected exact cx/gpt-6-luna. The original OC_TEST_MODEL value equals the first old campaign ledger model. New offline test, fmt, clippy, docs and diff gates pass.

## Risks
The new probe tests the original provider model ID with no tools for one bounded generation, not the full TUI or server-side route identity. Its response may fail for model access/reasoning despite the successful Luna route. Reserve two additional attempts before sending and never automatically repeat unknown external effects. The remaining allowance will be 2/24 after reservation.

## Next
Run exactly one bounded no-tools native Responses attempt on the exact original OC_TEST_MODEL wire ID using the reissued key and this same campaign; record typed status without raw response. Then summarize tested boundaries and update task evidence.
