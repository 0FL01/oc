# T44 V07 S07 — real streamed reasoning and frozen parts (2026-09-25)

This supplements [the previous equal-context PTY report](recovery-v07-s07-stream-followup.md), not its measurements. The current real `oc tui` PTY resource scenario now gives each 0/3,000-archived-message fixture the **same two actual Responses requests**: first, a public reasoning delta and an application-executed `read` of a seeded project file; then, after the tool result, the previously held progressive Markdown answer. Both complete JSON request vectors are compared byte-for-byte as `serde_json::Value` (no field removed or normalized). The 3,000 older messages remain durably stored but explicitly pruned from the provider's active projection in both requests. The test checks the returned file content, durable read outcome, unchanged file, exactly one read tool operation plus the preseeded bash record, settled streamed answer and original PTY scroll/resize/paste/dialog/resource/cleanup assertions.

The tool-start event freezes public reasoning/tool parts while the next provider response is held for the intermediate open-fence frame. The opt-in frame sampler must now see **nonzero live reasoning and frozen-part peaks**; all live-current counters must return to zero after the turn. The second provider request contains the real `function_call_output`, not an invented tool result. No shell command is executed by the model in this scenario.

Targeted command `CARGO_BUILD_JOBS=3 RUST_TEST_THREADS=2 TMPDIR=/home/opencode/.cache/opencode-tmp/opencode/oc-test-bench-20260924 cargo test --locked -p oc --test pty_t39 s07_pty_equal_view_archive_resource_samples -- --exact --nocapture` passed (1/1). Its later serial rerun reported the following observed samples, **not performance thresholds**:

| Older stored messages | RSS/PSS/HWM (KiB) | Post-ready CPU ticks / elapsed (ms) | Retained bytes / window rows | Frames / draw sum / max (ns) | Sampled worker receiver peak / overwritten | Live text peak / reasoning peak (bytes) / frozen part peak | Markdown cache current/peak (bytes) |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 0 | 34,852 / 32,613 / 34,852 | 100 / 1,923 | 16,814 / 152 | 28 / 897,440,142 / 41,950,711 | 5 / 0 | 124 / 37 / 2 | 7,683 / 7,683 |
| 3,000 | 34,992 / 32,780 / 34,992 | 102 / 2,037 | 16,814 / 152 | 28 / 937,190,166 / 45,722,281 | 4 / 0 | 124 / 37 / 2 | 7,683 / 7,683 |

`cargo fmt --all -- --check`, affected-crate all-target Clippy with warnings denied, and `git diff --check` passed on the changed source. The final serialized code-change gate — fmt; `CARGO_BUILD_JOBS=3 RUST_TEST_THREADS=2 TMPDIR=/home/opencode/.cache/opencode-tmp/opencode/oc-test-bench-20260924 cargo test --locked --workspace --no-fail-fast` (zero failures; optional live tests ignored); `CARGO_BUILD_JOBS=3 cargo clippy --locked --workspace --all-targets -- -D warnings`; `CARGO_BUILD_JOBS=3 cargo build --locked`; docs/journal and diff checks — **PASS**, full output `/home/opencode/.local/share/opencode/tool-output/tool_0d72dda9f001ePhAWP9Bzh5o1f`. **S07/VIS23/T44 remain OPEN**: no actual allocator peak, terminal-input queue/age, or full-frame styled/PNG parity is measured here; frame/receiver samples do not guarantee continuous peaks, and startup CPU remains outside the post-ready delta. Final-SHA V09 qualification is separate.
