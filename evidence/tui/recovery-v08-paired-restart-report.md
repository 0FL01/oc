# T44 — paired two-tab restart, Home route and real version-row padding

## RECON and fix

Pinned v2.0.12 `packages/tui/src/context/route.tsx:31-35` defaults a standalone launch without an explicit route to **Home**. A genuine paired restart exposed a native mismatch: after two completed root sessions, selecting the first tab, quitting through Ctrl+D and restarting, upstream showed Home with both original tabs while Rust selected the old tab from its persisted preference. `b18442c` now restores the bounded real tab deck but opens sessionless Home on bare launch when a slot is available. No root, turn, provider request or preference write is created on restore. Explicit `--session` still wins; when all 16 real slots are occupied, native preserves the selected real view and all IDs with an honest capacity notice rather than hiding a root. A saved full deck with one unreadable view opens Home with 15 valid tabs, never overwriting omitted IDs. Native PTY+SQLite tests cover selection, both histories, no new root/provider, corrupted view, 16-slot edge, explicit authority, Location isolation and CAS.

Paired Home `y=38` also proved the real native version (`0.1.0`) muted the **entire blank row**, unlike upstream's white canvas and muted glyphs. `crates/oc-tui/src/shell.rs` now styles only the actual native version glyphs. A TestBackend test failed before and passed after the correction. The production version string remains truthful, not spoofed to donor `2.0.12`. The final independent Home entry rectangle `x=0..111,y=38` and synthetic tab header `x=0..69,y=0` both compare **0 styled-cell differences**.

## Reproducible real-original/real-Rust qualification

`scripts/tui_capture/capture.mjs --tab-restart true` and the test-only PTY bridge run the original and Rust independently under isolated per-side HOME/XDG/data, a shared fixture project (this scenario only reads its file) and pinned terminal profile. The bridge retains each side's state, project path and loopback server between two launches. After one actual read/tool roundtrip, the runner clicks the functional add control, submits a second real turn with a distinct title/answer, selects the original tab, captures before exit, sends Ctrl+D and verifies **natural exit code 0** (never mistakes `stop` for quit). It relaunches with the same data, captures restored Home, clicks each tab and asserts its own history reappears without provider calls. The runner retains per-generation PTY input, protocol and VT hashes, actual styled cells, PNGs, full comparators and failed attempts. It checks exactly four valid transcript requests/completions and two title requests/completions per side before exit and no increase afterward. The bridge's natural-exit grace prevents a PTY EIO/waitpid race; a failed first attempt to exit gracefully remains in evidence.

Exact final command (run from repository root with a fresh output path):

```sh
node scripts/tui_capture/capture.mjs \
  --reference /home/opencode/.cache/opencode-tmp/opencode/t44-reference/package/bin/opencode \
  --oc /home/opencode/ai/oc/target/debug/oc --build-oc true \
  --geometry true --sample tools --sidebar hide --agent-profile true \
  --columns 120 --rows 40 --tab-click true --tab-restart true \
  --output /home/opencode/ai/oc/evidence/tui/recovery-v08-paired-restart-qualified-03
```

Final lock: pinned reference commit `2670273ff17da96f85c5826ced57aa1b368754fa`, executable SHA-256 `2b0825721cb12f9bca3d5099588087d557a21ed2b5b56efebea3f17dc5f79e6a`; native binary SHA-256 `7761e7a772eb0ebe9739e2d8acbd584fa41141877c41359c3b83326145170126` after `--build-oc true`, source manifest in `capture.lock.json` (source HEAD `463a2e4` plus dirty diff; implementation later committed as `b18442c`); fixture SHA-256 `546109431a6bb5d0edfe5bf1d8884fa0471983985afbe4767b70579f560f1624`; common terminal environment `e3cf33539f0e1d6485c01217ef4f632171a7de480eea7bdd9f9c598ea70f45be`. Both report `provider_contract=true` and `TAB_RESTART_CHECKS_PASS` with identical 4/2 request counts before/after restart. The final pair reports **DIFFERENT**, not pixel parity: restored Home 27/4800 styled cells and 1247/647040 PNG pixels; first restored session 201/4800 cells, second 204/4800 cells. Changed Home example choice is independent randomness; the actual application version still differs. Session footers also differ (upstream recorded context `6.8K (3%)`, Rust fallback `shift+tab agents`), plus measured elapsed time and Location text. No masks, fake clock or fabricated model/agent were used.

Immutable attempts kept: `recovery-v08-paired-restart-01` (initial flow), `-02` (first two-history flow), `-03` (revealed actual upstream Home default), `-qualified` (aligned route), `-final` (version padding), `-qualified-02` (overstrict bridge PTY exit race), and `-qualified-03` (source/test final qualified interaction). Runner exit `1` on final pair is the truthful whole-frame `DIFFERENT`, not failure of restart predicates. During implementation a workspace run failed the V03 geometry script's obsolete expectation of an active tab on bare restart; after requiring Home, then clicking the actual second saved wrapped-history tab, targeted and complete reruns passed.

## Gates and remaining outcomes

`CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=1 cargo test --locked --workspace --no-fail-fast --quiet` PASS zero failures (205 TUI tests; existing opt-in live tests ignored). `cargo fmt --all -- --check`, `cargo clippy --locked --workspace --all-targets -- -D warnings`, `cargo build --locked`, `node --check scripts/tui_capture/capture.mjs`, `python3 scripts/check_docs.py`, `python3 scripts/progress.py check`, and `git diff --check` pass on this slice. V08/V09 and VIS01–24 remain open: full frame differs and this is only the 120×40 two-root restart case, not the mandated size/state/dialog matrix. The missing context display comes from incomplete multi-round provider usage: do not equate final-round usage with full billed turn usage. Real `0.1.0` vs donor version, random examples, clocks and attempt-dependent Location strings require honest comparison policy, not patched screenshot strings. The existing untracked `.opencode/` was never accessed.
