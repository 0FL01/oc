# T44 — paired VIS05 wrapped-row resize diagnosis

**Diagnostic slice, not VIS05/V08/V09 or full T44 PASS.** Pinned original: OpenCode v2.0.12 source `2670273ff17da96f85c5826ced57aa1b368754fa`, executable SHA-256 `2b0825721cb12f9bca3d5099588087d557a21ed2b5b56efebea3f17dc5f79e6a`. Final immutable paired capture: `vis05-reflow-20260924-qualified/`, native executable SHA-256 `9cefeeaeb21f12669d89344e84fcd159b428f31757534f29bbdea4083de4b9fb`, initial source HEAD `cdd0cd6` with locked dirty diff `5b6516bc0e1a822612e6d98bbf1ea7368de9a8867bd7c9c44523b2f494375819`, fixture SHA-256 `d8a8a24d5639214a6708d97e6336fec6cb3d7e43fe4691fbb9a2e50230d48fc5`. Terminal profiles, per-frame environment IDs, source/tool hashes, input bytes, separate HOME/data, fake-provider protocol, styled grids, PNGs, VT and comparison reports are preserved under that attempt. No real credentials or real provider calls.

The opt-in `rows-reflow` fixture has 90 numbered code rows; rows 000–040 include a source space and a 90-character word which wraps at 80 columns but not 160. Both PTYs completed the same provider contract, preserved the draft and visible cursor, and actually scrolled. At 160×48 away the first visible marker is `ROW-041` at y=1; after resize to 80×24 **both original and native** show `ROW-014` at y=2; growing back restores `ROW-041` at y=1; Down advances the first marker to `ROW-042`. The first-marker change on shrink is *observed upstream behavior*, not a defect to "correct" by forcing semantic anchoring. The original's physical-row offset is the relevant resize contract in this scenario, with already documented short-line behavior in `vis05-current-report.md`. The test harness records the independent per-side anchors and fails if Down does not advance, rather than silently treating an unchanged frame as success.

Initial attempt `-01` exposed five style-only differences in the narrow `scroll-shrink` grid: original retained the **source separator** space after `ROW-014`…`ROW-018`, while native word-wrap discarded it. A first experimental synthetic code-tail space in `-02` colored unrelated blank cells on the wide frames; it was removed, not accepted as a parity shortcut. Production code now retains only source-space color on code-fence word breaks via a code-only wrapper, leaving ordinary prose wrapping unchanged. The wide sidebar's single bold title-adjacent blank was also a source separator at the title's wrap, **not** a painted last-row tail; the first implementation in `-02` introduced a second mismatch, then the final renderer preserved only the actual title separator. TestBackend regressions check narrow/wide code styles in both full and indexed transcript paths, plain text, sidebar title and Unicode no-separator case. `-03` measured four exact scrolled grids; `-qualified` repeats those with a strengthened Down predicate and the final runner hash. All attempts are immutable, including failed experiments.

In `-qualified`, `scroll-away`, `scroll-shrink`, `scroll-grow`, and `scroll-down-one` have **0 styled-cell and cursor differences**; away/grow/down also have **0 PNG pixel differences**. Shrink has **0/1920 styled-cell differences but 32/258816 PNG pixels differ**, confined to the final pixel column x=673, y=352–383; this is unresolved and not masked. Pinned and re-pinned completed footer frames still differ by 20/7680 styled cells in actual duration/tok-s fields. Home differs in six native-vs-donor version glyphs (`0.1.0` vs `2.0.12`); the app version was not falsified. Runner exit **1** and full-frame `DIFFERENT` remain the authoritative status. Nor does this test qualify Markdown/table reflow, every VIS05 geometry, Commands/Models dialogs or exact final parity.

Reproduce into a **new** attempt directory:

```sh
node scripts/tui_capture/capture.mjs --reference /home/opencode/.cache/opencode-tmp/opencode/t44-reference/package/bin/opencode --oc /home/opencode/ai/oc/target/debug/oc --build-oc true --geometry true --sample rows-reflow --scroll-resize true --agent-profile true --columns 160 --rows 48 --devtools false --output /home/opencode/ai/oc/evidence/tui/recovery-v03/NEW-REFLOW-ATTEMPT
```

On final code, `CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=1 cargo test --locked --workspace --no-fail-fast --quiet` passed with 216 TUI tests and the existing opt-in live ignores. Workspace fmt, Clippy all targets `-D warnings`, locked build, Node syntax, docs/progress and diff checks passed. One concurrent nonserialized TUI run saw the pre-existing catalog latency test exceed its time limit (213 passed, 1 failed); the serialized full workspace run passed without changing the threshold. Next: investigate the unresolved narrow PNG edge without masks; continue pinned Commands/Models and error/replay fixtures and the remaining VIS/V08–V09/S07 gates.
