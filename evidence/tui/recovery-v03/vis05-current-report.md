# T44 — VIS05 current-code paired resize/scroll diagnosis

This is a **diagnostic slice, not VIS05 or full-frame parity PASS**. The pinned original is OpenCode v2.0.12 source `2670273ff17da96f85c5826ced57aa1b368754fa`, executable SHA-256 `2b0825721cb12f9bca3d5099588087d557a21ed2b5b56efebea3f17dc5f79e6a`. The final independent original/native run is `vis05-fresh-20260924-final/`: native executable SHA-256 `e61221aefe736bf15f8771bf1d4d86addb2f1b958f56b791c1175c8281ab830e`, initial code HEAD `0f546a5` plus source dirty hash `5598a71eb6574500038b42528ea688c8362e984fa138b620eb094efd64c30856`, fixture SHA-256 `51135a45831e2bc8202681ffcb602409475d7a9a8bf93a6dd176947e24d91d65`. The per-size environment IDs, terminal profile, input events, actual original/native grids and PNGs, protocol, and comparator results are in `capture.lock.json` and the two per-side directories. Both sides executed the fake provider contract and the scroll/resize state predicates; runner exit **1** means whole-frame `DIFFERENT`, not an equal-state or behavioral PASS.

RECON discovered that the old native scroll input in the test harness used Up/Down, which recalled prompt history and destroyed the draft; actual SGR wheel events scroll the transcript without changing the draft. Attempt `-01` retains the failure. Attempt `-02` reproduced a real resize jump: after actual scroll at 160×48 the first visible marker was `ROW-041`; shrinking to 80×24 native jumped to `ROW-065` while original retained `ROW-041`. The bounded TUI viewport now preserves the top displayed row on resize when not pinned; its test first reproduced the jump, then covered shrink/grow, caret/draft, pinned bottom, one-line scroll and top clamp. Attempt `-03` exposed a stale test predicate expecting a fixed **bottom** marker instead of the pinned original’s fixed **top** marker. Attempts `-04` through `-07` identified/removed unequal pre-matrix scroll positions, a one-sided clamp test changing the PTY size before the paired paste capture, and another test-harness cursor predicate applied after a paste chip. All attempts remain immutable; the runner does not overwrite them. The final runner places the native-only clamp after the paired matrix and records its checks separately, then restores the 160×48 profile before the paired paste.

Current paired original/native matrix uses real PTY resize and checks row markers, a retained `scroll-draft`, and a visible cursor at its end at **80×24, 120×40, 160×48, 43/44/119/120/121×48, 120×80** and back to 160×48; the per-side `geometry-checks.json` files agree on 13, 29, 37, 37, 37, 37, 37, 37, 69, 37 visible markers and on sidebar breakpoints. The scroll pinned/away/shrink/grow/one Down/re-pin sequence agrees on top markers `53/41/41/41/42/53`, and the shrink frame is **0/1920 styled-cell differences** with equal cursor. Its PNG still differs by 32 edge pixels at rightmost column; an equal grid alone is not a full frame pass. The 90-line fixture has one short line per marker, so preservation of a *semantic* anchor when preceding text reflows to additional rows across width changes is unqualified; this remains a specific follow-up test, not an inferred guarantee. The 80×24 `>20` marker predicate is NOT_APPLICABLE, but actual marker/caret state was captured.

Further source-backed corrections from paired evidence: metadata now chooses full/short/omitted provider against the pinned `prompt/metadata.tsx` terminal width budget, so width 44 does not paint a clipped provider prefix; sidebar data and path paint muted glyphs while blank cells retain canvas FG. The original/native wide `scroll-away` difference fell from **1610/7680** styled cells in `-09` to **1/7680** in `-10`, a single blank title-adjacent style cell. Narrow 43/44 frames now differ at the measured elapsed footer text (final 44×48: **5/2112** cells); wide 160×48 pinned frames also differ in measured elapsed/tok-s text plus that one blank cell (final **21/7680**). The Home native version remains the actual `0.1.0`, not spoofed to donor `2.0.12`. Independent Home examples and runtime durations remain dynamic. No masks, frozen durations or baseline changes were used.

Reproduce on a fresh output name: `node scripts/tui_capture/capture.mjs --reference /home/opencode/.cache/opencode-tmp/opencode/t44-reference/package/bin/opencode --oc /home/opencode/ai/oc/target/debug/oc --build-oc true --geometry true --sample rows --matrix true --scroll-resize true --agent-profile true --columns 160 --rows 48 --devtools false --output /home/opencode/ai/oc/evidence/tui/recovery-v03/NEW-ATTEMPT`. Final code: `CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=1 cargo test --locked --workspace --no-fail-fast --quiet` PASS (TUI 212 tests, opt-in live tests ignored); fmt, all-target workspace Clippy `-D warnings`, locked build, Node syntax, docs/progress and diff checks PASS. VIS05 and other VIS01–24/V08–V09/S07 remain open because the paired full grids/PNGs still differ and not all scenarios are qualified. Next: semantic row anchoring under wrapping, narrow width changing prompt height, dialogs/error/replay states, exact remaining sidebar blank and keyed scroll behavior; do not infer full parity from this slice.
