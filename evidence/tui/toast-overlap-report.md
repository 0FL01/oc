# VIS27 — copied-toast overlap (paired PTY evidence, 2026-09-25)

**Result:** At 121×40 with a visible sidebar and a long chat title underneath `Copied to clipboard`, the final attempt `toast-overlap-20260925-05` passes the original/native overlap predicates and matches the pinned original in **all 84 styled cells** of the toast rectangle. This is a local VIS27 rendering result, not full-frame pixel parity or the final T44 gate.

The requirement is [T44_CONTRACT_AMENDMENT.md:62–78](../../tui-recovery/T44_CONTRACT_AMENDMENT.md): title letters must not survive in the toast padding/interstices; the raised background and side borders must remain. The immutable paired captures are [`toast-overlap-20260925-01` through `-05`](toast-overlap-20260925-05/capture.lock.json). Each has `capture.lock.json`, original/native `toast-overlap-checks.json`, `toast-overlap-before` and `toast-overlap-toast` styled grids/PNGs, and their whole-frame comparator JSON. The pinned reference source commit is `2670273ff17da96f85c5826ced57aa1b368754fa` (opencode v2.0.12); captures use the same fixture SHA-256 `2d73511167c58ab919f9a8f940118a55a88aede6e7bc480d8609599bdf2df3f2` and 121×40 terminal profile. The test first records the underlying long title, then uses two real PTY mouse clicks for selection/copy and captures the toast at x=91, y=1, width=28, height=3 (zero-based cells).

| Attempt | Native state observed | Overlap checks (original / native) | Before: differing styled cells / differing PNG pixels | Toast: differing styled cells / differing PNG pixels | Differing styled cells **inside toast rectangle** |
| --- | --- | --- | ---: | ---: | ---: |
| [`-01`](toast-overlap-20260925-01/capture.lock.json) | Pre-fix existing binary: title letters remain on raised padding | PASS / FAILED (`blank_padding=false`) | 142 / 7,456 | 158 / 7,765 | 52/84 |
| [`-02`](toast-overlap-20260925-02/capture.lock.json) | Interior cleared: symbols/background fixed; blank-cell foreground differs (`#eeeeee` native vs `#ffffff` original), side-border bold remains | PASS / PASS | 142 / 7,448 | 166 / 5,498 | 60/84 |
| [`-03`](toast-overlap-20260925-03/capture.lock.json) | Blank-cell foreground `#ffffff`: only the two left side-border bold modifiers differ inside toast | PASS / PASS | 142 / 7,455 | 108 / 5,505 | 2/84 |
| [`-04`](toast-overlap-20260925-04/capture.lock.json) | Full clear: six side-border backgrounds differ (`#0a0a0a` native vs `#141414` original) | PASS / PASS | 141 / 7,398 | 111 / 6,120 | 6/84 |
| [`-05`](toast-overlap-20260925-05/capture.lock.json) | Interior clear plus removal of border modifiers: original border backgrounds preserved | PASS / PASS | 142 / 7,443 | 106 / 5,493 | **0/84** |

Counts in the two middle columns are read from each attempt's `toast-overlap-{before,toast}.{grid,png}-diff.json`: grid comparators check 4,840 cells and PNG comparators check 652,800 pixels per frame. The last column compares `upstream/toast-overlap-toast.cells.json` to `oc/toast-overlap-toast.cells.json` cell-for-cell within the recorded `toast_rect`, including symbol, foreground, background, width and modifiers. In `-01` the painted native padding still contains title characters despite its raised background; in `-05` every toast-region styled cell is identical. All four available full frames (`home`, `session-wide-completed`, `toast-overlap-before`, `toast-overlap-toast`) remain **DIFFERENT in both grid and PNG in every attempt**. The remaining frame differences include independently rendered sidebar title truncation and real elapsed-time values; regional equality does not change the whole-frame verdict. The `-01` failed-state diagnostic has no paired original frame and is marked BLOCKED by the comparators; its original/native before and toast comparisons above are retained.

In every paired attempt both sides reached a provider baseline of **3 requests, 3 completed, 0 invalid**, unchanged through the copy steps. The capture records **one OSC 52 introducer since the mouse clicks** on each side and the toast/selected-word predicates, but labels the clipboard destination `NOT_VERIFIED_BY_PTY`: the system clipboard write is not established by these captures. Each `capture.lock.json` records runner `exit_code: 1`, including `-05`, because full-frame comparisons fail; `-01` additionally records the native overlap predicate failure. Failed and intermediate immutable attempts are retained.

Provenance limits: every lock identifies native HEAD `6c581de119cc42a30528bb77ee34d65903b047b8` with a dirty source manifest/diff and an existing `target/debug/oc` executable. Native executable SHA-256 differs by attempt (`-01` `dd7db3e4…`, `-02` `e8ffa3ca…`, `-03` `bb628b3a…`, `-04` `b1f4ba32…`, `-05` `280fa980…`). The parent manually rebuilt before captures `-02` through `-05`; the capture locks explicitly say **“Existing binary; build/source association not attested by this run”** and cannot independently prove that association. The locked reference executable reports `opencode v2.0.12`.

The first serialized full development gate on this dirty tree failed only `pty::ui05_interrupted_exit_is_nonsuccess`: its expected signal-derived exit status 130 was observed as 0; this test has no toast or clipboard path. A targeted rerun with unchanged code passed. A subsequent serialized `cargo fmt --all -- --check`, `CARGO_BUILD_JOBS=3 RUST_TEST_THREADS=2 TMPDIR=/home/opencode/.cache/opencode-tmp/opencode/oc-test-bench-20260924 cargo test --locked --workspace --no-fail-fast`, all-target workspace Clippy `-D warnings`, locked build, docs/progress/Node syntax and diff checks all passed, with **0 test failures** and 301 `oc-tui` tests; full output: `/home/opencode/.local/share/opencode/tool-output/tool_0d88c7fe7001ZhNOgHpO7V06PE` (initial failed output `tool_0d887358d001eIPRP7DmRtE7KM`). This does not qualify VIS27 or final-code-SHA V09: full paired frames still differ and the external clipboard destination remains unverified.
