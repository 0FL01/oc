# Accepted-prompt model switch — paired T44 diagnostic, 2026-09-25

**Partial behavior match, not VIS09/V09 PASS.** Pinned v2.0.12 `component/prompt/index.tsx:1274–1306` commits the selected model with the next prompt, `core/src/session/session.ts:90–102` publishes the changed model reference, and `packages/tui/src/util/model.ts:10–20` derives a visible switch label from the *current* catalog. Native now records only validated previous/new public model references with the next **accepted** user message in a single storage transaction, immediately before that message. The picker alone creates no transcript row. The application projects a public display name from the current catalog for the live event and each replay query; the journal does not store that name, provider options or credentials. Model-switch rows are excluded from provider/DCP context and do not create a model call.

| Immutable source-built paired attempt (120×40; Reader/tools; actual provider fixture) | Original/native action checks | Final full styled grid + PNG | Observation |
| --- | --- | --- | --- |
| [`model-switch-20260925-01`](model-switch-20260925-01/capture.lock.json) | Both `MODELS_INTERACTION_PASS`: no notice on picker selection, one notice after next accepted prompt, four transcript requests and one title request, correct selected model; no invalid requests | `models-second-turn` **DIFFERENT 524/4800 cells**, PNG DIFFERENT | Native notice had the wrong row margin, padding and style (at x3,y11 versus pinned x6,y12), shifting the following transcript. Retained failed comparator, not a PASS. |
| [`model-switch-20260925-02`](model-switch-20260925-02/capture.lock.json) | Both `MODELS_INTERACTION_PASS` with the same real-action, no-early-notice and exactly-one-next-turn predicates | `models-second-turn` **DIFFERENT 7/4800 cells**, cursor same, PNG DIFFERENT | After rendering the notice at the pinned position/style, its *entire row* and following transcript geometry match; differences are two groups of independently measured elapsed digits at y10 and y22. Every other full-frame scenario in the capture also remains DIFFERENT. |

The two attempts preserve full original/native VT, styled-cell grids, PNGs, per-stage comparator JSON, application/provider logs and fixture/build/source hashes. Pinned reference executable SHA-256 `2b0825721cb12f9bca3d5099588087d557a21ed2b5b56efebea3f17dc5f79e6a`; the `-02` dirty-source Rust binary SHA-256 is `5356ea7af654b86eee2b57e36972426c334e46c550dd40bc54121b5bdae58ffa`. Both captures exited 1 **because full-frame grid and PNG are DIFFERENT**. These are neither final-code-SHA V09 qualification nor a waiver for Integrations, Settings, wide Home version, random examples or independently measured time. Full workspace regression gate for this source slice is recorded separately.

Post-fix serial full gate **PASS**: workspace fmt, `cargo test --locked --workspace --no-fail-fast` (including transactional rollback, provider-wire, catalog-rename replay and styled notice tests), all-target Clippy `-D warnings`, locked build, docs/progress checks, JS/Python syntax and diff check. Output: `/home/opencode/.local/share/opencode/tool-output/tool_0da47871f001WMjJcqQ2ZkMAQ0`. This gate checks code health; it is not a full-frame parity PASS.
