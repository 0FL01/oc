# T44 selected catalog — real read-only smoke and status refinement

Base HEAD `73f3f12`. The owner reports a freshly rebuilt release TUI showing `Model discovery unauthorized (401/403)` while their original OpenCode 1 can use the Responses route. These are different routes; working Responses does not establish catalog authorization. The owner's private shell/key and the authoring-agent configuration were not inspected. The previously posted credential was not used.

## Observations and scope

- `references/openproxy-models.user.mjs:149-156` specifies configured baseURL plus `/models`, configured headers overwritten by Bearer apiKey and JSON Accept. Rust `discovery.rs:580-615` sends the same catalog path and reserved headers; `provider.rs:289-295,859-888` sends a separate `/responses` request. Selected project model is not statically declared. Without a successful catalog or an explicitly configured model, startup does not invent a model.
- Two **read-only external GET** requests to the configured public `/v1/models` endpoint using only the current process's inherited product `LUDKA2_API_KEY`/`LUDKA2_API_URL`, with native `oc/0.1.0` and JS-style `Bun/1.4.2` User-Agent respectively: both HTTP **200**, JSON list envelope true and selected model ID present true. The smoke checked the public expected HTTPS host before issuing a request. No body, model list, credential, Authorization value, raw response or full environment was printed or saved. There was no `/responses` request or paid model turn. This does **not** identify the key or request status in the owner's separate zsh process.
- Actual `target/release/oc` on this HEAD, with real admitted project/global **product** configuration, isolated XDG data directory, inherited selected product variables only, and no prompt, reached Home; Ctrl+C exit **0**, terminal restored. Repeated on the freshly rebuilt release with the status refinement and the same result. Authoring-agent config and untracked `.opencode/` were not read or staged.
- The owner's screen combines HTTP **401** and **403**, hiding a useful distinction. A minimal typed split now retains `DiscoveryFailure::Unauthorized` for 401 and `Forbidden` for 403 through application to static TUI copy. Other HTTP, retry, selected-ID validation, headless detail and redaction remain unchanged. The UI says neither that the key is invalid nor that Responses failed.

## Checks and attempts

- Test-first discovery adapter test expecting distinct 403 variant: `cargo test --locked -p oc-adapters reqwest_http_error_status_precedes_oversized_or_slow_body --lib -- --test-threads=1` exit **101** (the new variant did not exist). The isolated binary fixture also gained distinct 401/403 assertions for short, oversized and slow error responses; no old assertion was suppressed.
- `cargo fmt --all && cargo test --locked -p oc-adapters discovery::tests --lib -- --test-threads=1 && cargo test --locked -p oc --test recovery_startup isolated_discovery_startup_routes -- --test-threads=1 && git diff --check`: **0** (19 discovery tests, one actual-binary integration test).
- `cargo build --locked --release -p oc && python3 crates/oc/tests/support/discovery_startup.py target/release/oc && cargo fmt --all -- --check && cargo test --locked --workspace --no-fail-fast --quiet -- --test-threads=1 && cargo clippy --locked --workspace --all-targets -- -D warnings && cargo build --locked && python3 scripts/check_docs.py && python3 scripts/progress.py check && git diff --check`: **0** throughout, eight release-binary PTY routes (401/403 short/oversized/slow, absent/present), full workspace zero failures/five pre-existing ignores. Independent read-only review found no defect in the classification change. Fresh real-product-profile release startup: Home/exit **0**, terminal restored.

## Boundary and next decisive fact

The two current-process catalog 200s and original OpenCode 1's working Responses route do not explain the owner's separate `/models` 401/403. Possible differences are the effective credential, configured root/request, or catalog-route ACL/gateway policy; none is established. The exact status **401 versus 403 from the owner's fresh release in that same zsh environment**, or a provider-side status-only trace for its catalog GET, is unavailable to this process without using their private credential. Do not copy their key, dump requests, reuse a posted rotated value, auto-switch models, synthesize metadata, or bypass discovery. Product parity/VIS and the remaining V07 gates are still open.
