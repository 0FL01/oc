# T44 — selected-provider credential startup diagnostic

Base HEAD `c994afd`. This slice changes only the selected-provider missing-credential diagnostic and its isolated actual-binary startup fixture. No credential sources, model selection, or provider requests were added. The pre-existing untracked project `.opencode/` and private credential files were not accessed.

## Reproduction and result

- The owner's already-reproduced case is a selected provider with `apiKey` supplied by an environment template: with the variable absent, `config::assemble_admitted` produces `ConfigError::MissingCredential` before discovery. The owner's interactive shell environment has not been established by this fixture.
- An environment-cleared Python raw-PTY fixture now uses isolated HOME/XDG roots, an `opencode.jsonc` with `apiKey: {env:FIXTURE_KEY}`, a static `fixture/offline` model and a loopback listener. The real debug binary starts with a supplied nonempty fixture key (exit 0); absent and empty environment keys, and an empty literal `apiKey`, show **Selected provider credential missing** with static guidance to export the configured env variable in the launching shell (exit 1 each). Every PTY restores termios; no raw key, variable name, config path or provider URL appears on the startup screen. The listener receives no connection, and no turn is submitted.
- The same missing-key fixture via headless `oc run` exits 1 and retains the actionable `missing credential for provider.fixture.options.apiKey` detail. Existing malformed JSON, missing config and invalid `cli.json` cases still show **Configuration load failed** (exit 1). Typed `ConfigError::MissingCredential` is mapped through `composition::LoadFailure` to `application::SpawnFailure::MissingCredential`; TUI renders only static text, not the underlying detail. No error-string matching is involved.

## Attempts and gates

1. Before implementation, `cargo test --locked -p oc --test recovery_startup isolated_binary_startup_routes -- --nocapture`: exit **101**, missing-key fixture displayed **Configuration load failed**, as expected for the regression.
2. After implementation, targeted startup test: exit **101** because the first guidance sentence wrapped mid-assertion at 120 columns. Shortened the static action to one line; targeted test then exit **0**. Final `cargo test --locked -p oc --test recovery_startup -- --nocapture`: exit **0**, both tests passed and all four credential cases had the expected screen, status and terminal restoration. `cargo test --locked -p oc --test recovery_v03`, the `pty` V07c redaction case and 13 adapter config unit tests: exit **0** each.
3. Initial `cargo fmt --all -- --check`: exit **1** due to formatting; `cargo fmt --all`: exit **0**. Initial full serial `cargo test --locked --workspace --no-fail-fast --quiet -- --test-threads=1`: exit **101**, existing `pty::ui05_interrupted_exit_is_nonsuccess` observed status 0 instead of 130. Its focused rerun passed (exit **0**); the next full serial workspace run passed (exit **0**, five existing ignored). Clippy, build, docs and diff checks passed (exit **0**).
4. A final type refinement changed the composition cause from a boolean to a two-case enum. On that final source, `cargo fmt --all && cargo test --locked -p oc --test recovery_startup -- --nocapture && cargo test --locked --workspace --no-fail-fast --quiet -- --test-threads=1 && cargo fmt --all -- --check && cargo clippy --locked --workspace --all-targets -- -D warnings && cargo build --locked && python3 scripts/check_docs.py && git diff --check`: **0** for every command; workspace tests passed with five existing ignored.

External next step: the user's actual launching shell must export a nonempty value for its configured provider API-key variable before launching `oc`; that interactive environment was not verified here. No release-binary or real-user-profile qualification is claimed by this isolated debug PTY. No staging, commit, checkpoint or task transition was performed.

Changed paths: `crates/oc-adapters/src/composition.rs`, `crates/oc-adapters/src/application.rs`, `crates/oc-tui/src/shell.rs`, `crates/oc/tests/support/startup.py`, this report.

## Real-profile release verification (parent)

- Authorized global config, tracked Location config, source path and data-root metadata were inspected without printing credentials. The selected provider key is configured as `{env:LUDKA2_API_KEY}`; the authoring-agent process has a nonempty value, but this is **not** evidence of the owner's interactive shell. Native `oc` does not import `secrets.env`. A fresh non-interactive login `zsh -lc` with that variable removed still lacked it; the owner's interactive shell remained unobserved. Access to `secrets.env` was denied by file policy; no contents were read.
- On the base release binary, the actual configured project and real HOME with an isolated data root under the approved scratch area reached the Home screen and exited 0 when inheriting the nonempty variable. Removing only `LUDKA2_API_KEY` produced **Configuration load failed**, exit 1. Initial un-sized PTY probes emitted no drawable characters (0×0 terminal); a first corrected script attempt had a Python non-ASCII bytes-literal syntax error. A 120×40 PTY corrected both fixture errors; it confirmed the same positive/negative result without a model prompt. The actual default data root was also checked with a 120×40 PTY and inherited credential: Home, exit 0, terminal restored. A read-only SQLite quick-check succeeded, the data-root lock was free at probe time. No MCP call or Responses completion was sent.
- Parent `cargo fmt --all -- --check && cargo test --locked -p oc --test recovery_startup -- --test-threads=1 && cargo build --locked --release -p oc && git diff --check`: exit 0; two startup tests, fresh release build. On **that rebuilt release**, same real global/Location config and isolated data root: inherited credential → Home, exit 0; removed credential → **Selected provider credential missing**, exit 1; terminal restored in both cases. The rebuilt binary with the actual default data root and inherited credential reached Home and exited 0, terminal restored. Only safe boolean classifications were printed from captured VT; raw frames, keys, URLs and live catalog responses were not logged.
- Parent `cargo fmt --all -- --check && cargo test --locked --workspace --no-fail-fast --quiet -- --test-threads=1 && cargo clippy --locked --workspace --all-targets -- -D warnings && cargo build --locked && python3 scripts/check_docs.py && python3 scripts/progress.py check && git diff --check`: exit 0; five pre-existing ignored tests. Independent read-only review confirmed typed cause and safe redaction; it noted that if a selected provider simultaneously has an invalid URL and empty key, the actionable missing-key error appears first, then the URL error after supplying a key. No validation is bypassed.

**Resolution boundary:** Code now names the missing selected credential safely rather than displaying a misleading generic configuration error. The binary cannot authenticate or discover the selected model without a nonempty credential exported in its launching process. The owner must verify/export `LUDKA2_API_KEY` in their interactive shell from their trusted secret source; whether that shell currently does so is not established by the agent's environment. Do not source an unverified shell file or auto-load private authoring credentials. V07/VIS/product qualification remains open.
