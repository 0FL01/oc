# T44 V07a / S02 — config source admission

Base: `da711b4cb543a932983c46ba83f965d2964dad56` (active T44). Scope: discovered `.opencode` admission and bounded regular-file config reads. No real provider, authoring configuration, or credentials were used.

## Reproduction before repair

- `cargo test --locked -p oc-adapters v07a_ -- --test-threads=1`: exit 0, 3 passed; the original valid-config symlink regression only observed the *later* definition-root rejection and could not detect pre-admission reading.
- `cargo test --locked -p oc --test configured_workspace v07a_ -- --test-threads=1`: exit 101; isolated actual binary hung on project `opencode.json` FIFO; the test's 10-second preflight deadline fired. The valid external discovered-root case passed despite the pre-read defect.
- `cargo test --locked -p oc --test configured_workspace v07a_binary_discovered_external_root_is_refused_even_with_fifo_config -- --test-threads=1`: exit 101; the actual binary again hung for 10 seconds on an external root's FIFO *before* the previous containment rejection. The test killed its owned child on failure. This reproduces S02 on base HEAD, without accessing external real-world files.

## Change and observations

- Canonicalize and admit all roots before reading any config source; a discovered `.opencode` must resolve within the project, whereas explicit `OPENCODE_CONFIG_DIR` may point outside. Pin each admitted root's directory descriptor and verify the opened directory matches the admitted canonical path.
- For `opencode.json/jsonc`, resolve candidate containment before opening; open the canonical in-root target with Linux `openat2` (`RESOLVE_BENEATH | RESOLVE_NO_MAGICLINKS`, `O_NOFOLLOW | O_NONBLOCK`) relative to the pinned root. Inspect the **opened descriptor** for regular-file type and length, read at most 16 MiB + 1 byte, then decode UTF-8. In-root file/root symlinks and JSONC comments remain supported; an external symlink, FIFO, or oversize file cannot become a read-to-EOF operation. The former 1 MiB cap remains on standalone native DCP/CLI config, now likewise opened relative to admitted roots and nonblocking.
- Isolated actual `oc run` checks: external symlink root with valid config, named file secret, MCP/command markers; external-root FIFO alongside a valid JSONC source; local FIFO; symlink to `/dev/null` (refused by containment); 33 MiB config (budget error). All fail before application data-directory creation or fake-provider request; no loader trap executes, no fixture secret appears in diagnostics. Positive actual binary check loads explicit external global `{file:key}` plus nested in-project `.opencode` symlink with JSONC comment/trailing comma, submits to a fake Responses peer and completes the title request.
- Isolated bare `oc` on a real PTY, with external discovered-root FIFO and fake provider, displays the configuration failure screen within five seconds, exits after `q`, creates no application data root and sends no provider request. This is a TUI preflight observation, not an interactive model-turn qualification.

## Checks and attempts

- `cargo fmt --all && cargo test --locked -p oc-adapters v07a_ -- --test-threads=1 && cargo test --locked -p oc --test configured_workspace v07a_ -- --test-threads=1`: exit 0 (3 unit, 3 binary at that point).
- `cargo fmt --all && cargo test --locked -p oc --test pty v07a_ -- --test-threads=1 && cargo clippy --locked --workspace --all-targets -- -D warnings`: exit 101 (PTY 1 passed; clippy caught missing unsafe-block comment in composition). After fixing, `cargo fmt --all -- --check && cargo clippy --locked --workspace --all-targets -- -D warnings`: exit 101 twice (first PTY unsafe-block comments, then the comment placement). Subsequent `cargo fmt --all -- --check && cargo clippy --locked --workspace --all-targets -- -D warnings`: fmt exit 1 because one line required wrapping; `cargo fmt --all && cargo clippy --locked --workspace --all-targets -- -D warnings`: exit 0.
- `cargo test --locked --workspace --no-fail-fast --quiet -- --test-threads=1 && cargo build --locked && git diff --check`: first attempt exceeded the tool's 240-second limit (no final process exit status; build/diff not reached). Same command with 600-second limit: exit 0; workspace tests passed (five pre-existing ignored cases), build and diff check passed. This was before adding the final positive actual-binary case.
- After that test addition: `cargo fmt --all && cargo test --locked -p oc --test configured_workspace v07a_ -- --test-threads=1 && cargo test --locked -p oc --test pty v07a_ -- --test-threads=1 && cargo clippy --locked --workspace --all-targets -- -D warnings && git diff --check`: exit 0 (4 binary cases, 1 bare PTY; clippy/diff clean). Earlier 3 adapter unit cases passed after implementation. No live provider testing.

Changed paths: `crates/oc-adapters/src/composition.rs`, `crates/oc/tests/configured_workspace.rs`, `crates/oc/tests/pty.rs`, this report. No status/goal/progress edits, staging or commit. The pre-existing untracked project `.opencode/` was not opened.

## Open safety work (V07b/c)

This is S02 source-config admission, not a full filesystem/security qualification. Independent review found the separate definition/AGENTS.md path-based reads and nested definition symlink directories were not yet qualified by the pinned **config** descriptors; the confirmed definition escape and its repair are recorded below. `{file:}` substitution has separate bounded no-follow reads; its concurrent ancestor-path mutation is not qualified by these regression tests. Native `openat2` needs a kernel that supports the syscall; failure is an explicit config-open error. A symlink to `/dev/null` was rejected before device open; creating a device node inside an admitted fixture was not attempted. S01 and S03–S08 (in-flight MCP cancellation, modifier/event handling, terminal control sequences, MCP error policy, late events/Location, measured resource growth and cross-contract regressions) remain for V07b/c. No claim of full T44 visual parity or live provider readiness.

## Independent-review extension — definition-root escape, repaired in V07a

Reviewer finding: `defs::merge_definition_root` accepted the admitted pathname but `load_kind` enumerated `root.dir/{skills,agents,commands}` with `read_dir`, following an intermediate symlink outside. `read_plain` then checked only a lexical parent and file type before an unbounded `read_to_string`. Admission of `.opencode` itself did not confine these indirect reads.

Test-first reproductions against the V07a implementation before this extension:

- `cargo test --locked -p oc --test configured_workspace v07a_binary_external_definition_subdirectories_never_reach_provider -- --test-threads=1`: exit 101. The fake Responses request **contained external skill metadata** and expanded `/outside` to the external Markdown command body; a local valid sibling also loaded.
- `cargo test --locked -p oc --test configured_workspace v07a_binary_external_agent_subdirectory_cannot_select_primary -- --test-threads=1`: exit 101. An outside `agents/outsider.md` became the selected developer prompt and reached the fake provider instead of failing preflight.
- `cargo test --locked -p oc-adapters v07a_racing_definition_subdirectory_never_reads_outside -- --test-threads=1`: exit 101. Atomic replacement of `skills` between an in-root and external directory loaded an external skill. Fixtures were isolated synthetic paths, no authoring/private configuration read.

Repair: `composition` passes its already-pinned global/local root fds to the definition loader. A common Linux `openat2` helper confines opens beneath that descriptor. Before opening, definition paths are canonicalized to permit **in-root** symlinks; paths resolving outside are diagnosed without opening contents. Actual file type and size come from the opened fd, not `symlink_metadata` followed by a pathname read. Directory listing reopens `/proc/self/fd/<pinned subdirectory>` rather than a mutable `.opencode/skills` pathname, with a bounded 4,097-entry scan; Markdown bodies are capped at the existing 4 MiB total-definition budget even if files grow while read. `O_NONBLOCK` protects FIFO/device paths. Explicit external global remains admitted as its own declared root, with unchanged provenance/order and path-based diagnostics. Composition also reads admitted `AGENTS.md` from its pinned root and hands bounded bytes to the instruction assembler; the direct legacy instruction reader uses a nonblocking bounded regular-file open.

Post-fix evidence:

- First `cargo fmt --all && cargo test --locked -p oc-adapters v07a_ -- --test-threads=1 && cargo test --locked -p oc --test configured_workspace v07a_ -- --test-threads=1`: exit 101 at compilation; used unavailable `Result::is_err_or`. Replaced with supported `!is_ok_and` without changing test expectations.
- `cargo fmt --all && cargo test --locked -p oc-adapters defs::tests -- --test-threads=1 && cargo test --locked -p oc-adapters v07a_ -- --test-threads=1 && cargo test --locked -p oc --test configured_workspace v07a_ -- --test-threads=1 && cargo test --locked -p oc --test pty v07a_ -- --test-threads=1`: exit 0; 11 definitions tests, 7 V07a adapter tests, 6 actual headless binary tests, one bare PTY. Covers external skills/agents/commands absent from provider input; selected outside agent rejected before data root/provider; 80-iteration atomic symlink-swap race; regular in-root symlink + explicit external global agent; FIFO and 4 MiB+1 oversized definition with valid sibling; concurrent instruction-link race.
- `cargo fmt --all -- --check && cargo clippy --locked --workspace --all-targets -- -D warnings && git diff --check`: exit 0.
- `cargo test --locked --workspace --no-fail-fast --quiet -- --test-threads=1 && cargo build --locked && git diff --check`: first attempt exit 101 because unrelated PTY `v05_raw_unicode_multiline_focus_and_one_durable_submit` timed out waiting for cursor `(17, 11)`; other targets passed, build did not execute in that attempt. `cargo test --locked -p oc --test pty_t39 v05_raw_unicode_multiline_focus_and_one_durable_submit -- --test-threads=1`: exit 0. Repeated full workspace/build/diff command: exit 0 (five previously ignored tests, no changed test thresholds). That PTY test was not edited.

Additional paths changed for extension: `crates/oc-adapters/src/admitted_fs.rs`, `crates/oc-adapters/src/defs.rs`, `crates/oc-adapters/src/lib.rs`, and the already listed `composition.rs`, `configured_workspace.rs`, this report. No GOAL/status/progress edits or staging/commit; untracked project `.opencode/` remains untouched.

Final focused review added assertions that headless diagnostics name all three rejected external definition directories with `outside admitted root`, and that a stable in-root `AGENTS.md` symlink contributes its text before the race. The direct instruction reader again deduplicates **before** opening a repeated file. After these assertions: `cargo fmt --all && cargo test --locked -p oc-adapters v07a_ -- --test-threads=1 && cargo clippy --locked --workspace --all-targets -- -D warnings && git diff --check && git status --short && git rev-parse HEAD`: exit 0 (7 adapter tests, clippy/diff clean; HEAD unchanged). `cargo fmt --all && cargo test --locked -p oc --test configured_workspace v07a_binary_external_definition_subdirectories_never_reach_provider -- --test-threads=1 && cargo clippy --locked --workspace --all-targets -- -D warnings && git diff --check`: exit 0. After deduplication: `cargo fmt --all && cargo test --locked -p oc-adapters defs::tests -- --test-threads=1 && cargo test --locked -p oc --test configured_workspace v07a_binary_external_definition_subdirectories_never_reach_provider -- --test-threads=1 && cargo clippy --locked --workspace --all-targets -- -D warnings && cargo fmt --all -- --check && git diff --check`: exit 0 (11 definition tests, 1 actual binary test). Workspace PASS above preceded only these additional assertions and duplicate-open prevention; their applicable targeted checks passed.
