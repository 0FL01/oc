# T44 / VIS28 — running scanner diagnostic (2026-09-25)

**Status: diagnostic only; VIS28 and T44 are not PASS.** Immutable paired PTY attempts are in `evidence/tui/scanner-animated-20260925-{01,02,03,04,05,06,07}/`, `scanner-fallback-20260925-{01,02}/`, `scanner-cancel-20260925-{01,02}/`, and `scanner-fallback-cancel-20260925-01/`. Primary final locks: [animated `-07`](scanner-animated-20260925-07/capture.lock.json), [source-built animated `-06`](scanner-animated-20260925-06/capture.lock.json), [source-built fallback `-02`](scanner-fallback-20260925-02/capture.lock.json). Each directory contains `capture.lock.json`, `commands.json`, original/native `scanner-checks.json`, `inputs.json`, `protocol.json`, styled-cell/VT/text/PNG captures and the comparisons available for that attempt. The recorded capture runner **exited 1** in every attempt, including animated `-07`, fallback `-02`, and cancel `-02`. On `-07`, cancel `-02`, and both fallback runs, both binaries satisfy the scanner behavior predicates, but the *running* full-frame grid and PNG comparisons fail; fallback `-02` alone has a completed frame whose grid **and** PNG are EQUAL. Runner exit 1 is neither a scanner-behavior failure nor a full VIS28 PASS. Animated `-06` instead records an unmatched native start hold and an invalid grid comparison for that stage.

## Paired observations

The full workspace/tooling gate is **PASS** (recorded output: [tool_0d81cfd2c001wN5g5eZLL5p9Nv](/home/opencode/.local/share/opencode/tool-output/tool_0d81cfd2c001wN5g5eZLL5p9Nv)); this is separate from the paired-capture runner's exit 1 and the still-open VIS28/T44 visual gate.

The final animated `scanner-animated-20260925-07`, earlier animated `-05`/`-06`, and cancel `scanner-cancel-20260925-02` runs use 120×40, dark `opencode`, sidebar hidden, truecolor, animations enabled; `scanner-fallback-20260925-{01,02}` and `scanner-fallback-cancel-20260925-01` use animations disabled with the same dimensions/profile settings. Their `capture.lock.json` records original source `2670273ff17da96f85c5826ced57aa1b368754fa`, pinned upstream executable SHA-256 `2b0825721cb12f9bca3d5099588087d557a21ed2b5b56efebea3f17dc5f79e6a`, paired fixture SHA-256 `3dce22b803ba229615462b8c3f89aabe19b9697baabcf9849d3d697fcbf63759`, and per-pair shared environment IDs. The terminal frontend is xterm.js 6.0.0 with Chromium 145.0.7632.6, DejaVu Sans Mono 14, Unicode 11, `xterm-256color` and `COLORTERM=truecolor`. No clocks or full-frame differences were masked.

Both sides of `-07` (as previously in `-05`) report `SCANNER_PASS`, `provider_contract=true`: forward progression, end fade, reverse progression, start fade, held request, no early completion, release acknowledgement, scanner disappearance, completed read and valid roundtrip. In source-built animated `-06`, upstream reports `SCANNER_PASS`; native reports `SCANNER_UNMATCHED_PHASE` because `all_phases_observed=false`, despite the other progression, release and valid-roundtrip predicates being true and `provider_contract=true`. `-02` cancel reports `SCANNER_PASS`/`provider_contract=true` on both sides, including disappearance while the provider was still held, durable input, interrupted frame, no completed read/transcript and no post-cancel answer after release. Both animations-disabled completion runs `fallback -01/-02` and fallback-cancel `-01` likewise report `SCANNER_PASS` on both sides for their single fallback stage and the applicable completion/cancel predicates. These are real local fixture-backed `/v1/responses` streaming requests recorded in both `upstream/protocol.json` and `oc/protocol.json`, not an injected visual-only response: the completion runs record an initial transcript request, a subsequent transcript request with one tool result and a title request (three valid requests per side); cancel runs record a held transcript request (plus upstream's title request), with no extra post-cancel transcript. This is **not** a live OpenProxy qualification.

At the four phase-matched animated stages (`scanner-forward`, `scanner-end-hold`, `scanner-reverse`, `scanner-start-hold`) of **both** final `-07` and earlier `-05` and cancel `-02`, `capture.lock.json` records `phase_match: EXACT_INDICATOR`; comparing complete `{symbol, fg, bg, width, modifiers}` cells at scanner coordinates `x=3..10, y=38` yields **8/8 equal styled cells at each stage**. Source-built `-06` records exact matches for forward, end hold and reverse only; its native start hold is `UNMATCHED_PHASE` after three inexact candidates (best `color_distance: 56`), so its [start-hold grid comparator](scanner-animated-20260925-06/scanner-start-hold.grid-diff.json) is `INVALID` (`Incomparable frames: different scenario`), even though the candidate PNG comparator says `FAIL`/`DIFFERENT`. The runner's subsequent exact-signature-first phase sampling permits the short start hold to be requested without waiting to sample two distinct fade colors; the full native stage-order/progression predicates and paused styled cells are still checked. The animations-disabled `scanner-fallback` stage also has **8/8 equal styled cells** at `x=3..10, y=38` in fallback `-01/-02` and fallback-cancel `-01` (fallback `-01` does not label its stage `EXACT_INDICATOR`; fallback `-02` and fallback-cancel `-01` do). These regional matches do not erase the running whole-frame differences below.

Full-frame comparisons below are exact counts from each named `*.grid-diff.json` and `*.png-diff.json`, listed as **different styled cells / 4,800 checked; different decoded RGBA pixels / 647,040 checked**. Except fallback `-02` completed **0 / 0** (comparator `PASS`, runner `EQUAL` in both modes), every listed entry has comparator status `FAIL` and runner status `DIFFERENT`; the incomparable `-06` start hold is described above rather than counted as a valid grid comparison:

| Frame | Animated `-07` (final) | Animated `-05` (prior) | Cancel `-02` | Fallback `-02` (final) | Fallback `-01` (prior) | Fallback-cancel `-01` |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `home` | 46 / 1,834 | 27 / 1,247 | 6 / 298 | 6 / 298 | 27 / 1,247 | 27 / 1,247 |
| `scanner-forward` | 35 / 4,265 | 35 / 4,263 | 35 / 4,263 | — | — | — |
| `scanner-end-hold` | 35 / 3,020 | 35 / 2,996 | 35 / 2,928 | — | — | — |
| `scanner-reverse` | 35 / 2,897 | 35 / 3,085 | 35 / 3,092 | — | — | — |
| `scanner-start-hold` | 35 / 2,919 | 35 / 3,071 | 35 / 3,031 | — | — | — |
| `scanner-fallback` | — | — | — | 32 / 1,518 | 32 / 1,518 | 32 / 1,518 |
| `scanner-completed` | 5 / 248 | 2 / 130 | — | **0 / 0 (EQUAL)** | 1 / 64 | — |
| `scanner-interrupted` | — | — | 41 / 2,099 | — | — | 47 / 2,322 |

In final `-07` (and earlier `-05`), each animated running frame's 35 differing cells are confined to the top row `x=0..34, y=0` (header/title/spinner, see each `difference_bbox_inclusive`); the `-07` completed frame differs at five cells on row 10 versus two in `-05`. The header/title/spinner animations on `y=0` run independently in the two real processes: matching the footer scanner phase at `y=38` does not synchronize the top row, and neither grid nor PNG comparison masks it. Final fallback `-02` has [running grid](scanner-fallback-20260925-02/scanner-fallback.grid-diff.json) **32/4,800 DIFFERENT** cells confined to `x=3..34, y=0` and [running PNG](scanner-fallback-20260925-02/scanner-fallback.png-diff.json) **1,518/647,040 DIFFERENT** pixels, beyond its exact eight-cell scanner region; its [completed grid](scanner-fallback-20260925-02/scanner-completed.grid-diff.json) **PASS/EQUAL** (0/4,800, cursor equal) and [completed PNG](scanner-fallback-20260925-02/scanner-completed.png-diff.json) **PASS/EQUAL** (0/647,040). The completed equality applies only to that captured state, not to the running sequence or VIS28.

**Historical Esc difference (at the captured source SHA).** In cancel `-02` and fallback-cancel `-01`, `upstream/inputs.json` and `upstream/scanner-checks.json` record two Esc inputs: the first leaves the held indicator present and changes the hint to `esc again to interrupt`, the second cancels. Native `oc/inputs.json` and `oc/scanner-checks.json` record one Esc that cancels and removes the indicator. This earlier keybinding/hint difference was a product parity gap, not a runner timeout. The later source-built correction and remaining independent title mismatch are recorded in [`two-escape-report.md`](two-escape-report.md); those captures do not change these immutable observations or close VIS28.

## Earlier attempts and provenance boundary

Final fallback `-02` [commands](scanner-fallback-20260925-02/commands.json) record `--build-oc true`, successful `cargo build --locked`, and runner exit 1 because running/home comparisons differ. Its [lock](scanner-fallback-20260925-02/capture.lock.json) records native source HEAD `b5e1502ddb49219ad830ae572197a59c19469a55` **plus dirty worktree diff** SHA-256 `e72b259caf269fc9e709d56eee85a5d6d39899ae97748aaa8ad7f7fac2bdc05d`, source manifest SHA-256 `2c3547b8cdf9baca9aa1880508ec68080769dbb50b8da3230288f34e225cdc9e`, and executable SHA-256 `8322f739c8cd0877c51e6fa3ad2b227ff4d2f8d57b5937ecce02b91098ecaf3e`. This is a source-built *dirty-worktree* capture, **not** an attestation that committed `b5e1502` alone produced the binary. Animated `-06` was likewise source-built; `-07` used an existing executable and does not independently attest its source association. No captured HEAD is a final code SHA.

The earlier animated `-01` timed out trying to photograph `scanner-end-hold` upstream and `scanner-reverse` native, recording `UNSTABLE_CAPTURE` because the VT grid changed during PNG capture. Animated `-02` captured completion and real provider roundtrips on both sides but marked `SCANNER_FAILED_OBSERVATION`: the runner did not establish `reverse_progression` or `all_phases_observed` on either side. Animated `-03` and `-04` subsequently record `SCANNER_PASS` for both and full-frame `DIFFERENT`; `-04` still had 43 differing styled cells at `scanner-reverse`. Cancel `-01` timed out waiting for the upstream interrupted footer, with unstable diagnostic capture and invalid animated grid comparisons; native reported scanner behavior PASS. Its single upstream Esc is insufficient evidence for an upstream cancel defect: the later `-02` run explicitly observes the two-Esc behavior. These failed/unstable early attempts are capture/observation failures, not proof that the product failed to animate or cancel. They remain immutable and are not substituted for later measurements.

The final `-07` lock records native source HEAD `b5e1502ddb49219ad830ae572197a59c19469a55` **with dirty worktree diff** SHA-256 `d749d4c51d086abd2efcba353512d09e13bc936c2b9e98b858c18f697ec4844d`, source manifest SHA-256 `2c3547b8cdf9baca9aa1880508ec68080769dbb50b8da3230288f34e225cdc9e`, and executable SHA-256 `8322f739c8cd0877c51e6fa3ad2b227ff4d2f8d57b5937ecce02b91098ecaf3e`; its lock explicitly says `Existing binary; build/source association not attested by this run`. The preceding `-06` lock records `cargo build --locked` (`--build-oc true`), native HEAD `34b439d66e9cd822051205101d758673a82a14c7` with dirty diff SHA-256 `f4ce71e8a450404704f7bb85fc68a75bd816e9fe3d213085a4f995806c4d97fc`, source manifest SHA-256 `9286d9dc75f53fc2532699bff1af7163725fc42eb0a2340396e6b8928cbe592a`, and the **same executable SHA-256** `8322f739c8cd0877c51e6fa3ad2b227ff4d2f8d57b5937ecce02b91098ecaf3e`. The change from `34b439d` to `b5e1502` in the locks is a later independent owner documentation commit; it does not turn the `-07` existing-binary lock into a fresh source-build attestation. The earlier animated `-05`, cancel `-02`, and fallback-cancel `-01` locks record native source HEAD `34b439d66e9cd822051205101d758673a82a14c7` **with dirty worktree diff** SHA-256 `662166e0b74dc594b9b197de0e6e806d5f9e1f524ad032957b4ad9e2054f08cf` and source manifest SHA-256 `922666abacf00b6e62ee4660bdb8e1b80043304d3a36df7a5c361ad6004757ea`; native executable SHA-256 `affca7897d67a829ce318f48854bc0149336ec996b3f07e5bd52b146093836c3`. Their locks explicitly say `Existing binary; build/source association not attested by this run`. The earlier fallback completion lock records native HEAD `c31c0100096204081155fb30b28429126ce45425` plus dirty diff SHA-256 `ceb7e26ee0ac325d7dd2eab2a53d550612de1a0cefb77aea1908074c6b36dd16`, executable SHA-256 `d55b8aacd73dc0e6e7c4cbbe5456cb004a259e1b803e3aef97fb1115615862ba`, and the same *existing-binary* caveat. These recorded HEADs are **not** a final code SHA; no VIS28/T44 qualification is claimed.

After the PTY Clippy fixes, the full workspace gate **PASS**: `cargo fmt ... && CARGO_BUILD_JOBS=3 RUST_TEST_THREADS=2 TMPDIR=/home/opencode/.cache/opencode-tmp/opencode/oc-test-bench-20260924 cargo test --locked --workspace --no-fail-fast && CARGO_BUILD_JOBS=3 cargo clippy --locked --workspace --all-targets -- -D warnings && CARGO_BUILD_JOBS=3 cargo build --locked && python3 scripts/check_docs.py && python3 scripts/progress.py check && node --check scripts/tui_capture/capture.mjs && git diff --check` (output `/home/opencode/.local/share/opencode/tool-output/tool_0d81cfd2c001wN5g5eZLL5p9Nv`); tests report zero failures, including 293 TUI and 216 adapter unit tests. This green code/tooling gate does **not** make VIS28 or T44 PASS: final `-07` still has full-frame grid/PNG `DIFFERENT`. For this report, `git diff --check` exited 0; before the new capture-specific `.gitattributes` rules, a new scanner `.txt` failed `git diff --no-index --check /dev/null` with trailing-whitespace/blank-EOF diagnostics, and the same check exited 0 afterward. Immutable captures were not modified. Next qualification needs the remaining required paired VIS28/T44 gates; the present evidence remains diagnostic.
