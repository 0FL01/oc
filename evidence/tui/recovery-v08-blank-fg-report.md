# T44 — user padding and prompt blank foreground on a paired restart

Pinned v2.0.12 two-root restart `recovery-v08-context-usage-01` left 113 styled-only blank-cell differences per restored session at `(3..4,3)` and `(5..115,34)`. Upstream leaves the user box's two leading padding spaces and empty prompt textarea at canvas foreground `#ffffff`; native had painted them as text `#eeeeee`, while symbol, width and background matched. The original user box styles its text child, not the box padding (`routes/session/index.tsx:2339-2345`), and prompt text similarly is distinct from its raised surface (`component/prompt/index.tsx:1645-1666`). A 120×40 TestBackend regression failed before the fix, then passed for both blank regions, neighboring actual glyphs/borders, and typed input after it.

Commit `8728fe3` changes both full and bounded user-row projections at their source in `messages.rs` so padding retains truecolor canvas foreground while actual text stays theme-colored; the prompt Paragraph paints only actual input spans as text and leaves empty cells on raised canvas. The earlier intermediate renderer-specific padding rewrite was removed before the final capture. No profile color, cursor, text, history or output content changes.

Fresh immutable paired attempt `recovery-v08-blank-fg-01/` used the same actual pinned original binary SHA-256 `2b0825721cb12f9bca3d5099588087d557a21ed2b5b56efebea3f17dc5f79e6a` (source `2670273ff17da96f85c5826ced57aa1b368754fa`), fixture SHA-256 `546109431a6bb5d0edfe5bf1d8884fa0471983985afbe4767b70579f560f1624`, terminal profile `e3cf33539f0e1d6485c01217ef4f632171a7de480eea7bdd9f9c598ea70f45be`, native binary SHA-256 `cbe02710d2b6c8fe33d2bbceaed19761255a61106677057c3d6b0ffc4282c833` (lock HEAD `243487a` + dirty source hash `7ed9d2031b46e1347e5eb56c4e2df82eb70088db204a0345e99490511e9b17a1`). Same test-only Reader/read fixture, 120×40, two real turns and natural quit/restart; both sides `provider_contract=true`, `TAB_RESTART_CHECKS_PASS`, histories click-reopened with four transcript/two title requests before quit and none after. Use the previous report's command with a new `--output` path. `check_region.py` reports 0/111 styled differences for prompt blank `(5,34,111,1)` and 0/2 for user padding `(3,3,2,1)`. The full reopened-old/second frames now differ by only **2/4800** and **1/4800** styled cells, respectively, each on the measured elapsed-time glyphs at y10; PNG differences 131 and 66 of 647040 pixels. Completed first session likewise differs by 2 timer cells. Home in this attempt differs by six real version glyphs; restored Home has a different random example. Thus the independent whole-frame comparator still reports `DIFFERENT` (exit 1); **no VIS PASS** or frozen-clock claim. All failed/different captures, raw VT, provider checks and exact cell/PNG comparator results remain intact.

`CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=1 cargo test --locked --workspace --no-fail-fast --quiet` PASS zero failures (207 TUI, existing opt-in live ignores); `cargo fmt --all -- --check` initially identified two layout-only rustfmt changes, corrected with `cargo fmt --all`; final fmt, all-target workspace Clippy `-D warnings`, locked build, docs/progress checks and diff check pass. T44 stays ACTIVE; remaining VIS05 geometries, dialogs, replay states, V08/V09/S07 need independent qualification. Real version, duration and example randomness are not screenshot defects to hide.
