# T44 V07 S07 — worker-event queue sample (2026-09-25)

This supplements, rather than replaces, [the earlier S07 resource sample](recovery-v07-s07-report.md). The opt-in `OC_TUI_TEST_METRICS` collector now samples the TUI's Tokio broadcast **receiver** unread length before and after the worker-event drain and after each processed event. It also counts messages overwritten by a lagged receive. The latter is a **message count, not latency**. No metric is collected or serialized without the existing isolated test flag. A real capacity-two broadcast regression verifies that five sends cause three skipped messages and that the lagged receive stops the current drain as before.

`s07_pty_equal_view_archive_resource_samples` still drives independent actual `oc tui` PTYs with equal 200-message active tails at 120×40 and either 0 or 3,000 older messages. It retains its scroll, resize, paste, card paging, Commands, child-reaping, terminal restoration and visible-tail assertions. After the final code edit, its serial targeted run passed (one test). Observed values from that run, not limits:

| Older messages | Sampled worker receiver peak (messages) | Overwritten events | Draws / sum / max | Sampled RSS / PSS (KiB) | Retained bytes / window rows |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 0 | 0 | 0 | 17 / 572.59 ms / 43.35 ms | 28,688 / 26,397 | 20,696 / 150 |
| 3,000 | 0 | 0 | 17 / 596.66 ms / 44.68 ms | 28,152 / 25,912 | 20,696 / 150 |

The receiver has 256 slots. The reported peak is **sampled at drain boundaries**, not a continuous high-water mark. Zero is expected here: the workload does not submit a provider turn and does not exercise streaming worker events. Crossterm's private input queue length and enqueue times are not exposed by its event API, so these numbers say **nothing** about terminal-input queue occupancy or elapsed input-event lag. Live-part counters, streaming-fence churn and peak allocations in the running PTY are not established by this sample. Do not promote it to VIS23 or S07 PASS, nor treat the earlier metadata-only render allocator sample as equivalent to a live archive.

Checks on this diff: `cargo fmt --all -- --check`; `CARGO_BUILD_JOBS=3 RUST_TEST_THREADS=2 TMPDIR=/home/opencode/.cache/opencode-tmp/opencode/oc-test-bench-20260924 cargo test --locked -p oc --bin oc worker_event_metrics_count_overwritten_broadcast_events_and_stop_drain -- --nocapture` (one actual test; an earlier `--exact` invocation selected zero tests, corrected); the analogous `--test pty_t39 s07_pty_equal_view_archive_resource_samples -- --exact --nocapture` (one PASS); affected-crate Clippy with `-D warnings`; then the **serialized full locked workspace** fmt, `cargo test --locked --workspace --no-fail-fast` (zero failures, opt-in live cases ignored), workspace all-target Clippy, locked build, docs/journal structure and diff checks: PASS. Full gate output: `/home/opencode/.local/share/opencode/tool-output/tool_0d54c62a80015JkIKj7p3ezueE`. This is a code-change gate, not final T44/V09 visual qualification. The inherited untracked `.opencode/` was neither inspected nor included.
