# Selection/copy — paired diagnostic, 2026-09-25

**Result:** VIS27 paired default-Select interaction predicates PASS in attempt `-03` on pinned upstream and native `oc`; **full-frame parity FAILS** in every captured state. This is diagnostic evidence, not a VIS27 or T44 PASS. The earlier immutable attempts `selection-copy-20260925-01/` and `-02/` remain available alongside `-03/`.

## Capture and provenance

The three attempts use `scripts/tui_capture/capture.mjs --geometry true --sample tools --sidebar hide --agent-profile true --columns 120 --rows 40 --selection-copy true`; each directory contains its own `capture.lock.json`, `source-manifest.json`, `commands.json`, per-origin `selection-copy-checks.json`, styled-cell grids, text, VT, PNG, and grid/PNG diff JSON. The common fixture SHA-256 is `16a8757ab6b87d86b9db94e5b5beda3c1117cfa15997522339b86479524bc143`; runner hashes are in each lock. Profile: xterm.js 6.0.0, Chromium 145.0.7632.6, DejaVu Sans Mono 14px, 120×40, `xterm-256color`, dark opencode theme, real application wall clock. Stable capture requires the visible marker and unchanged complete styled grid/cursor for five polls 200 ms apart; completed transcript additionally requires a successful fixture response. The fake provider records **3 requests, 3 completed, 0 invalid** on each origin in `-03`, unchanged across clicks (also 3/3/0 in `-01` and `-02`).

Reference source is pinned upstream `2670273ff17da96f85c5826ced57aa1b368754fa`, executable `opencode v2.0.12` SHA-256 `2b0825721cb12f9bca3d5099588087d557a21ed2b5b56efebea3f17dc5f79e6a`. Native source at capture is **dirty HEAD** `91e278a9983ae29501df6ae29aef434efac70585` (tree `726b858fc30f9cacb546af912ddc1eb4caf689b5`), not a final code commit: `-01/-02/-03` record distinct tracked dirty-diff hashes and source manifests. Native `target/debug/oc` was an **existing binary**; capture does not attest a build/source association. Its SHA-256 changed across attempts: `-01` `21b13906b3ae7dd6336add0d32bda97cc4d0c2cd3fc6765f4dd0723d2e25bf8a`, `-02` `8fba5796984d3a8a0ac91c15c0ce526aa105b83325ebf45287e76351e2949dee`, `-03` `cc1dd3a718cd86ec82303096d93b2db3a117dc5e539d3facc51d2857caa17a3f`. `-03` records dirty-diff SHA-256 `22afb5531072a91d66f5c6177db3aa45d1b39cccf2bf50ae291d67d779e63664` and `source-manifest.json` SHA-256 `6eb19ad9a09e457f55f912cf28b406afbd35802704835bd4381b76192f84fe0c`. No final-source SHA or rebuilt-binary claim follows from these locks.

## Executed paired behavior

The runner located a unique painted word in the completed transcript (word at zero-based x=5, y=8), sent SGR mouse clicks at one-based column 9, row 9 and compared stable captured screens. Default mode was Select, with no earlier toast. On both origins in `-03`, double click highlighted the word at columns 5–12, and the selected run extended through columns 13–18; the copy toast appeared once at x=93, y=2. Between click sequences the old toast disappeared on both origins. Triple click highlighted the word and extended the line through column 40 (columns 13–40 outside the word); the toast appeared again. Both sides have the same captured word styles: initial fg `#eeeeee`/bg `#0a0a0a`, selected fg `#0a0a0a`/bg `#eeeeee`, no modifiers. The `-03` `selection-copy-checks.json` files and lock mark all applicable completed-before-click, double, toast-expiration and triple predicates true; `line_extends_beyond_word` is intentionally null for double click. Prefix-only OSC 52 observations are **1 after double and 2 after triple per origin**, not a decoded system-clipboard readback. Terminal OSC 52 writes/introducers do not establish that an OS clipboard changed.

Attempt history matters: `-01` upstream checks PASS but native `between-clicks-toast-expired` FAILS (`toast_absent: false` after the runner's 12-second observation window; the native info toast was configured for 30 seconds); other captured stages continued as diagnostics. After the native info-toast duration was corrected to 5 seconds, `-02` checks PASS on both origins, but the native word used `reversed` modifiers rather than the source fg/bg swap; triple selection also included columns 2–4 outside the source line region. `-02` double/triple grids differed by 16/41 cells. After matching the source double/triple styles and region, `-03` checks PASS on both origins, and those selection-style differences no longer appear in the paired grid diffs. Earlier artifacts have not been rewritten or promoted into parity evidence.

## Full-frame comparison (all DIFFERENT)

Each grid compared all **4,800** styled cells of a 120×40 frame and cursor (no cursor mismatch); each PNG comparison examined all **647,040** decoded RGBA pixels of a 1011×640 image. Table entries are differing cells / differing pixels; each of the eight comparators per attempt exited 1 (`capture.lock.json` exit code 1). Nothing was masked or region-cropped.

| Attempt | Home | Completed session | Double selection | Triple selection |
| --- | ---: | ---: | ---: | ---: |
| `-01` | 6 / 298 | 2 / 126 | 16 / 941 | 41 / 526 |
| `-02` | 6 / 298 | 2 / 126 | 16 / 941 | 41 / 526 |
| `-03` | **45 / 2,037** | **2 / 122** | **2 / 122** | **2 / 122** |

In `-03` the completed/double/triple grid failures are precisely two **symbol** cells at zero-based `(38,10)` and `(39,10)`; the corresponding PNG failures are 122 pixels within inclusive `[320,163,335,172]`. The displayed footer elapsed readings are genuinely different (`128ms` upstream versus `143ms` native); they were not frozen or masked. Home differs by 45 cells (inclusive grid bbox `[41,21,117,38]`, PNG bbox `[345,338,992,620]`): the visible prompt suggestions differ and the bottom-right version reads `2.0.12` versus `0.1.0`. Home is not a selection frame, but remains part of the full-frame capture gate. See `selection-copy-20260925-03/{home,session-wide-completed,selection-double,selection-triple}.{grid,png}-diff.json` for the exact comparison results and `upstream/` and `oc/` for raw frames. A matched local selection region does not turn a DIFFERENT full frame into PASS.

## Independent native PTY coverage and limits

`crates/oc/tests/pty_t39.rs::vis27_real_pty_osc52_select_and_manual_clipboard_modes` is a **separate native-only real-PTY test**, not an upstream/native pixel comparator. It exercises first click (no copy), double word selection, triple line selection, drag selection, visible feedback, no additional provider traffic, history preservation and terminal recovery. It also launches a new native PTY with project `terminal.copy=manual`: drag paints selection without auto-copy/toast; right press emits the copy feedback. Its helper decodes **complete OSC 52 terminal output** and checks destination bytes/count (three Select writes, one Manual write), rather than reading the operating-system clipboard. The paired capture only counts OSC 52 introducers. The native test and all workspace tests passed in the latest serialized gate (`tool_0d7c792810013f6YZkEah3W04a`); all-target Clippy then found a mechanical new-test lint, fixed in the test, and fmt/Clippy/build/docs/progress/Node syntax/diff checks subsequently passed. No OS clipboard readback is established.

After the `-03` capture, review found that a successfully published reload could change `terminal.copy` while a later retained-tab selection refresh failed; the view could retain the old copy mode. The owner-receipt path now applies the published mode to all views before fallible refresh and clears selections, while a changed Location with no verified replacement view disables copy until a fresh catalog. Targeted reload, tab, and fail-closed tests passed; a final serialized `cargo fmt --all -- --check`, `CARGO_BUILD_JOBS=3 RUST_TEST_THREADS=2 TMPDIR=/home/opencode/.cache/opencode-tmp/opencode/oc-test-bench-20260924 cargo test --locked --workspace --no-fail-fast`, all-target workspace Clippy `-D warnings`, locked build, docs/progress/Node syntax and diff checks passed (full gate output `tool_0d7d32c23001M43JDKUXlvvY8F`). This follow-up is not part of the `-03` captured binary.

**Qualification:** paired VIS27 selection/copy behavior is supported by `-03` predicates, but VIS27/T44 full parity remains **OPEN / NOT PASS**: all four grid and four PNG full-frame comparisons differ; the locked native binary is not attested to a final clean source revision. Subsequent qualification needs a source-associated final binary and passing required complete-frame/interaction gates. The locks also classify these captures as `DIAGNOSTIC_BASELINES_ONLY`; reasoning and MCP error/stall scenarios were not executed here.
