# T44 V06a Markdown — implementation and evidence

Base: `98e81eedea4963bc16aae1d944210f9da65c18c1`, tracked worktree clean at start; private untracked `.opencode/` excluded from all operations. Active T44; no journal/commit action for this slice.

## Reproduction

- `cargo test --locked -p oc-tui v06a_table_fixture_escaped_pipes_unicode_and_bounded_wrap -- --nocapture`: exit 101 on base renderer after the test was added. At 42 columns, the fixture header remained `| Инструмент | Назначение |`, delimiter `| --- | --- |` was printed literally, and each tool line wrapped as prose without a grid.
- First parser spike `cargo test -p oc-tui v06a_table_fixture_escaped_pipes_unicode_and_bounded_wrap -- --nocapture`: exit 0, added exact `pulldown-cmark 0.13.0` lock; further existing Markdown tests exposed lost code newlines and CommonMark blank-block offsets. `cargo test --locked -p oc-tui messages::tests -- --nocapture`: exits 101 (3, then 2, then 1 affected tests); after fixes, exit 0 (12/12). Parser diagnostic offset probe was temporary and removed.

## Code

`oc-tui` pins native `pulldown-cmark =0.13.0` with GFM Tables. Event-driven rendering replaces the manual heading/list/inline/fence parser. The existing syntax-theme highlighter remains a limited language subset. Tables use measured terminal-cell widths, header/row separators, inline code styles, soft-wrapped cells and grapheme-aware CJK/emoji wrapping. Captured dark-profile grid/border/padding roles and the 80/120-column label allocation were derived from the pinned running reference, not a screenshot resize. Controls in model Markdown, including CSI/OSC/BEL, are converted to inert visible replacement characters.

The screen now asks for **only visible rows**, measuring bounded parts before materializing the viewport. The per-session completed-part cache keys by `(part position, seq, content revision hash, width, theme mode)`, replacing a dirty revision and clearing on session switch. No cached history grows without bound: 240 entries / 512 KiB of span text, max 512 rendered rows per Markdown part (a visible preview-limit marker on overflow); full history remains application-owned. Streaming unclosed fences/headings/lists and dirty-part/width/theme invalidation have Rust unit coverage. Width changes select fresh geometry without reparsing the immutable transcript on every frame when the active page fits cache.

## Checks and attempts (all exits retained)

- Reproduction and parser spike above. An offset diagnostic test was removed after the CommonMark blank-line fix. An early `cargo clippy --locked -p oc-tui --all-targets -- -D warnings` failed on an 8-argument helper; signature corrected. An ANSI regression initially asserted against a joined multiline test string (the test's own newline); corrected to check each rendered row. A table-style spike forced all cell text white; captured styled-cell analysis showed this would erase code/heading theme colors, so the implementation preserves inline colors and only gives the border/padding their captured roles. These failed attempts are not parity evidence.
- `cargo fmt --all -- --check`, `cargo test --locked -p oc-tui --lib --quiet` (135 passed), `cargo clippy --locked -p oc-tui --all-targets -- -D warnings`, `git diff --check`: exit 0 after final code. Includes tests for the 2-column/11-tool-row fixture, escaped pipe, narrow/long CJK/ZWJ emoji cells, incomplete code stream, ANSI, 10,000 blank lines, cache revisions and 240-part viewport.
- Full chain `cargo fmt --all -- --check && cargo test --locked --workspace --no-fail-fast --quiet -- --test-threads=1 && cargo clippy --locked --workspace --all-targets -- -D warnings && cargo build --locked && python3 scripts/check_docs.py && git diff --check`: exit 0 on the last run; five existing ignored tests. The immediately preceding full-chain run exited 101: unrelated existing `pty_t39::v05_raw_unicode_multiline_focus_and_one_durable_submit` cursor predicate timed out waiting for `(17, 11)`. No test, timeout or baseline was changed. Exact test rerun (`RUST_BACKTRACE=1 cargo test --locked -p oc --test pty_t39 v05_raw_unicode_multiline_focus_and_one_durable_submit -- --exact --nocapture`) exit 0; the complete serial `pty_t39` target (18/18) exit 0; full serial workspace chain then exit 0. The intermittent failure remains recorded, with no proven environmental cause.
- All 16 capture commands used `node scripts/tui_capture/capture.mjs --reference /home/opencode/.cache/opencode-tmp/opencode/t44-reference/package/bin/opencode --oc /home/opencode/ai/oc/target/debug/oc --build-oc true --geometry true --sample table --sidebar hide --columns {80|120} --rows {24|40} --output evidence/tui/recovery-v06a/paired-<attempt>`. Every capture process exited **1** because the full-frame comparators correctly found differences, not because provider requests failed. Attempts in order: `paired-120x40`, `paired-120x40-final`, `paired-80x24-final`, `paired-80x24-adjusted`, `paired-120x40-adjusted`, `paired-80x24-qualified`, `paired-80x24-style`, `paired-120x40-style`, `paired-80x24-last`, `paired-120x40-last`, `paired-80x24-v06a`, `paired-120x40-v06a`, `paired-80x24-recheck`, `paired-120x40-recheck`, `paired-80x24-final-code`, `paired-120x40-final-code`. Each directory preserves raw VT, styled cells, PNGs, protocol/inputs, source manifest, environment/capture lock and independent grid/PNG comparator reports. The diagnostic-only `scripts/tui_capture/check_region.py` reads already captured grids; it cannot turn an unequal full frame into a pass.

## Final paired capture on this code

- Original executable `opencode v2.0.12`, SHA-256 `2b0825721cb12f9bca3d5099588087d557a21ed2b5b56efebea3f17dc5f79e6a`, pinned UI commit `2670273ff17da96f85c5826ced57aa1b368754fa`. Rust binary `oc 0.1.0`, SHA-256 `0b382adfef230a0f1eb25e07001ebcc420fd0f78b3eb909554cbade4fbded3a4`; source HEAD `98e81eedea4963bc16aae1d944210f9da65c18c1` plus dirty diff SHA-256 `f0029f5004432b30a83045aab77c924645a0065d0f8a2c484bd27db07b9674f7`, source manifest SHA-256 `72239afa77641dfef03db06d0e576871f69e5d8c2cf3eddaa4e3febac79a9946`. Both final locks use fixture SHA-256 `2c3e6ae556935ce85de2854ab4e192097cede6ca85128b95eaaa593a78b1ac41`, xterm.js 6 + Unicode11 + Chromium/DejaVu Mono profile, same project and normal provider protocol within each pair, isolated per-side HOME/data. Both origins `EXECUTED`, `provider_contract=true`, normal transcript response completed. Screenshots and styled grids are independent outputs of the actual two PTYs.
- `paired-80x24-final-code`: capture/comparators exit 1; session grid **1054/1920 different cells**, PNG **5469/258816 different pixels**. Diagnostic captured table rectangle `(x=6,y=1,w=70,h=7)` is **490/490 equal styled cells** (symbols, colors, modifiers and width), but this is the bottom of the table only; other table rows are above the viewport.
- `paired-120x40-final-code`: capture/comparators exit 1; session grid **1653/4800 different cells**, PNG **5454/647040 different pixels**. Captured table rectangle `(x=6,y=1,w=112,h=24)` is **2688/2688 equal styled cells**, including all 11 tool rows, header, box-drawing, inline code and blanks. The entire frame is still unequal (title/prompt/agent footer and real elapsed-time fields, among other chrome). These region results are diagnostic evidence only; they do not qualify VIS13 or VIS14.

## Remaining V06a and qualification gaps

VIS13/VIS14 and R4 remain **OPEN**: exact full-frame parity failed, escaped pipes/CJK/emoji/long wrapped cells and incremental streaming were checked with Rust tests, not a paired live streaming capture of those additional inputs. The fixture provider emits one text delta; source-language highlighting remains a small heuristic subset rather than upstream tree-sitter grammars. Extremely long Markdown is explicitly preview-limited to 512 rendered rows; pagination to the remainder of a single oversized part and independent RSS/PSS qualification are outstanding. `visible_transcript` still measures bounded history rows each frame (without Markdown tokenization for cached parts), and byte accounting covers span text rather than all allocations. V06b reasoning/tool semantics and durable replay qualification were not performed here. No readiness/pixel-parity claim; historical reports and task state unchanged.

## Independent review correction (same uncommitted slice; later code/captures)

The section above records the state **before** the review; the following is the later result. Six tests were added before corrections. `cargo test --locked -p oc-tui v06a_ -- --nocapture` exited **101** with failures reproducing: assistant's whole-row 512-line cap hiding the preview marker/footer; an 800-line user block stopping at row 512; 12-cell table overflow on width-two graphemes; raw OSC/BEL from reasoning/user/chips/metadata; 6,483,880 parsed source bytes across 80 cumulative unclosed-fence deltas; and the missing trailing intentional blank line in fenced code. A further regression for code syntax style on later Markdown pages failed (exit **101**) before the fence continuation fix. No earlier capture was overwritten.

`visible_transcript` now counts bounded blocks/pages and emits only intersecting wrapped rows. User body is segmented on grapheme boundaries with width-dependent source budgets and remains scrollable to its tail, with border/chips/padding intact. Completed assistant text is segmented into per-part source pages (at most 4 KiB / 128 source lines per page); each page is separately cached by `(seq, row index, page index, page content hash, width, theme)`. The footer is a separate visible block, never cut off by a Markdown row cap. An opening fence is restored at a continued page so later code remains syntax-colored. A live part exceeding 16 KiB renders a cached prefix and an explicit preview-limit marker; its full durable answer is available after completion, when paged rendering can scroll through it. The per-part parser also caps direct decode at 16 KiB. Wider-than-cell graphemes become a visible `…` instead of displacing a table border. The transcript sanitizes provider controls in reasoning/user/chips/footer and tool rows as well as Markdown. Closing fences discard only their structural newline, retaining earlier code blank lines.

Review qualification: `cargo fmt --all && cargo test --locked -p oc-tui v06a_ -- --test-threads=1 && cargo clippy --locked -p oc-tui --all-targets -- -D warnings && git diff --check` exited **0**, 13 targeted tests. One preceding targeted chain passed 141 oc-tui tests but exited **101** at clippy's `too_many_arguments`; the helper was reduced to seven arguments. Final `cargo fmt --all -- --check && cargo test --locked --workspace --no-fail-fast --quiet -- --test-threads=1 && cargo clippy --locked --workspace --all-targets -- -D warnings && cargo build --locked && python3 scripts/check_docs.py && git diff --check` exited **0** throughout, five existing ignored tests. The direct 80-delta open-fence test asserts at most `80 × 16 KiB` parsed input, and the actual viewport test asserts fewer than 20 parses across the same 80 deltas; both pass. This bounds decode work, not end-to-end allocations or refresh CPU.

Fresh paired original/Rust runs, with the **same fixture and terminal profile per pair** and rebuilt binary, were captured **after** the above final code at `paired-80x24-review-fixed/` and `paired-120x40-review-fixed/`. Commands were the original `node scripts/tui_capture/capture.mjs --reference /home/opencode/.cache/opencode-tmp/opencode/t44-reference/package/bin/opencode --oc /home/opencode/ai/oc/target/debug/oc --build-oc true --geometry true --sample table --sidebar hide --columns 80 --rows 24 --output evidence/tui/recovery-v06a/paired-80x24-review-fixed` and the same command with `120 --rows 40` and `paired-120x40-review-fixed`. Both capture commands exited **1** solely because their post-capture full-frame grid/PNG comparators report DIFFERENT; the two bridge origins were EXECUTED with `provider_contract=true`, completed responses, per-side isolated roots, captured raw VT/PNG/styled cells and lock. The capture locks identify HEAD `98e81eedea4963bc16aae1d944210f9da65c18c1` + dirty diff SHA-256 `0ceef3f5ccb4a650c6c5003ed60cd763b208a442cb8222d83f1ddaaab72fcc99`, source manifest SHA-256 `4bd165441265b089878c2a0fd82f21c4c9da1e1553c8817bd6469daa08990375`, rebuilt binary SHA-256 `9ab29deb2be3defb6ffda5138405b1f4d6d179d2e7c466e2f561d022f6ab8a0c`, fixture SHA-256 `2c3e6ae556935ce85de2854ab4e192097cede6ca85128b95eaaa593a78b1ac41`, pinned upstream SHA-256 `2b0825721cb12f9bca3d5099588087d557a21ed2b5b56efebea3f17dc5f79e6a` and the xterm.js 6/Unicode11 Chromium + DejaVu profile.

- `paired-80x24-review-fixed`: completed-session full-frame grid **1054/1920** different cells, PNG **5485/258816** different pixels. Post-capture diagnostic `python3 scripts/tui_capture/check_region.py ... --rect 6 1 70 7` found **490/490** matching styled table cells (visible table bottom).
- `paired-120x40-review-fixed`: completed-session full-frame grid **1653/4800** different cells, PNG **5476/647040** different pixels. Post-capture diagnostic `--rect 6 1 112 24` found **2688/2688** matching styled table cells (whole visible tool table). These region checks are diagnostic only: neither full frame matches.

Remaining V06a limitations: source paging at 4 KiB boundaries restores fenced code context but does not preserve arbitrary cross-page Markdown constructs (e.g. a very large table or list); an individual Markdown page may show a preview-limit marker if its expanded structure exceeds 512 rows. The live prefix cannot scroll beyond 16 KiB until the turn has durable history. The viewport still scans the bounded history source on each draw and stores only cached span bytes in `retained_bytes`; no independent RSS/PSS or long-lived soak was done. Streaming fixture extensions (escaped pipe/CJK/emoji/long cells through actual paired PTYs), language grammar parity and full-frame parity remain unverified. VIS13/VIS14 and R4 remain **OPEN**; V06b reasoning/tool semantics and replay qualification remain separate. No journal, staging, commit, historical report or gate was changed.

### Final-code correction and paired evidence (supersedes earlier review-fixed pair as latest)

An additional failing test (`cargo test --locked -p oc-tui v06a_completed_synthetic_part_without_footer_can_reach_tail -- --nocapture`, exit **101**) reproduced a completed text segment surrounding a tool: it also has `seq = MAX`/no footer, so inferring “live” from that identity hid its tail. The viewport now explicitly identifies the one still-streaming row from TUI state; completed frozen segments use the paged path. The 14 targeted V06a tests and targeted clippy then exited **0**. The final full chain `cargo fmt --all -- --check && cargo test --locked --workspace --no-fail-fast --quiet -- --test-threads=1 && cargo clippy --locked --workspace --all-targets -- -D warnings && cargo build --locked && python3 scripts/check_docs.py && git diff --check` exited **0** at every stage (143 oc-tui unit tests, five existing ignored workspace tests).

New immutable `paired-80x24-review-final/` and `paired-120x40-review-final/` were produced by the same reference/Rust command and profile as above, changing only `--output`. Both completed captures executed the normal provider contract. Both capture commands exited **1** because the post-capture full-frame comparators correctly report differences. The final locks pin HEAD `98e81eedea4963bc16aae1d944210f9da65c18c1` + dirty diff SHA-256 `9b84b91f625e93518d6d557ad6299ee4a926fa76ee281f7887d1aa5a2f5f7443`, source manifest SHA-256 `9c1c58aae17b900b12e003c901869676b5f4e6356188f8b817022e97403012a2`, Rust executable SHA-256 `e25205bba0c4819247c778fbb6bd75498f5876652c35ef6ce7d687f744372c0b`, fixture SHA-256 `2c3e6ae556935ce85de2854ab4e192097cede6ca85128b95eaaa593a78b1ac41`. Latest completed-session full-frame differences: 80×24 **1053/1920** styled cells and **5429/258816** pixels; 120×40 **1652/4800** styled cells and **5416/647040** pixels. Diagnostic region comparisons run after capture found **490/490** equal styled table cells at `(6,1,70,7)` and **2688/2688** at `(6,1,112,24)` respectively; whole-frame parity remains **FAILED**, VIS13/VIS14 **OPEN**.

Worst-case limit remaining: cache stores at most 240 completed pages/512 KiB of span text; at extremely narrow widths or with many tiny pages, eviction can cause cached pages to be reparsed on later frames. Paged source splitting also resets cross-page list/table context. These and unmeasured full-refresh CPU/RSS are outstanding V06a performance/fidelity gaps, not green gates. Earlier capture directories remain intact; no staging, commit or progress update occurred.

## Second independent review: indexed semantic pages (later uncommitted code)

The section above describes the preceding code; this is the **later** correction. Test-first `cargo test --locked -p oc-tui v06a_ -- --test-threads=1` initially exited **101** with three reproduced failures: a 150-body-row escaped-pipe/CJK/emoji table lost its continuous grid at the 128-line source boundary, a 280-item ordered list acquired extra blank rows, and 241 parts caused **482** Markdown parses in one frame (count pass then viewport pass). An added 100-row wrapped-cell scroll-count test exited **101** (206 estimated vs 406 actual rows at width 42); a 127-line fence followed by literal table text also exited **101** when the literal code became a grid. A >16 KiB single-line completed part initially failed a tail-access test; that test's first arbitrary 450-row minimum was corrected to a viable >300 before it passed. None of the initial failures was reclassified as a pass.

`crates/oc-tui/src/messages.rs` now builds a source-offset/row-height index once per part revision+width, bounded to 32 indexed parts and 2 MiB of index metadata (in addition to 240 cached styled pages / 512 KiB text). The row-count pass scans source/indexes and does **not** invoke the Markdown parser for off-screen assistant pages; only viewport-intersecting pages render. The second frame reuses the visible parsed pages; 241- and 10,000-part stress tests assert fewer than 32 initial parses, no extra second-frame parses, and width invalidation only on visible pages. A rendered page corrects its lightweight estimated height in the index; at most two viewport recounts settle word-wrap differences. Per-page table slices carry real header/alignment as parser context, conceal that context on continuation, preserve inter-row borders and a single bottom border, and share column widths measured over the complete table's actual cells (including escaped pipes, inline code, CJK and emoji). Ordered-list continuation restores its ordinal even when source markers all say `1.`. Page breaks inside fences retain code context; table-looking lines inside code cannot become GFM tables. The source splitter preserves a completed single line beyond 16 KiB at grapheme/word boundaries. The fixture's entire viewport row order/count at 74 and 114 cells, a 150-row styled table, 100 wrapped rows at 42/74, 700 body rows beyond 512, and original short 11-row table all have passing tests.

Failures during implementation remain visible: an earlier full workspace chain passed tests but clippy exited **101** for `manual_is_multiple_of`/test clones; the next full chain passed tests but clippy exited **101** for `too_many_arguments`; both were corrected. `paired-80x24-indexed-pages/` and `paired-120x40-indexed-pages/` were captured after an early indexed implementation and both runner commands exited **1** (full-frame DIFFERENT): their completed-session styled grids differed **1744/1920** and **4554/4800** cells, PNGs **35569/258816** and **98110/647040** pixels. The newly added full-fixture viewport test reproduced a one-row height-estimate/blank spacing mismatch (exit **101**); the seek index now reconciles a rendered page's exact height and repeats bounded viewport selection. Later `paired-80x24-indexed-final/` and `paired-120x40-indexed-final/` were also captured (runner exit **1** each, DIFFERENT full frame), but precede the last fenced-literal fix and are retained as intermediate evidence. Earlier attempts and original reports remain intact.

Final-code checks: `cargo fmt --all -- --check && cargo test --locked --workspace --no-fail-fast --quiet -- --test-threads=1 && cargo clippy --locked --workspace --all-targets -- -D warnings && cargo build --locked && python3 scripts/check_docs.py && git diff --check` exited **0** end-to-end (152 `oc-tui` unit tests, five existing ignored tests workspace-wide). `cargo test --locked -p oc-tui v06a_ -- --test-threads=1` exited **0**, 23 focused tests. The final source remains HEAD `98e81eedea4963bc16aae1d944210f9da65c18c1` plus dirty diff SHA-256 `fd3d14f949ed3b15a69b338cc8c648642f75a01e811f01174911a8db1ae2494a`, source manifest SHA-256 `d1aead6019f9a94be1504eecf0b5ade65c2c266820db891fe064221be681b826`, built binary SHA-256 `c4edf25f3707ffd3ea2fbc7f9f6b9c811008c5f1fc7b3faada0b51d1871cafea` (from the latest capture locks).

The **latest** immutable actual PTY pairs `paired-80x24-indexed-qualified/` and `paired-120x40-indexed-qualified/` were captured after the final checks with pinned original v2.0.12 executable, the final rebuilt Rust binary, identical fake-provider table fixture SHA-256 `2c3e6ae556935ce85de2854ab4e192097cede6ca85128b95eaaa593a78b1ac41` and identical profile within each pair (xterm.js 6/Unicode11, Chromium/DejaVu Sans Mono, dark/hidden sidebar). Each side executed normally with `provider_contract=true`; each runner **exited 1** because the **post-capture** full-frame comparators report DIFFERENT. Completed-session 80×24: **1056/1920** styled cells, **5508/258816** pixels differ; diagnostic already-captured table region `(6,1,70,7)` matches **490/490** styled cells. Completed-session 120×40: **1652/4800** styled cells, **5432/647040** pixels differ; diagnostic table region `(6,1,112,24)` matches **2688/2688** styled cells. Diagnostic regions do not close VIS13/VIS14: full-frame parity, additional live PTY Unicode/escaped-pipe/wrapping inputs, durable replay, grammar parity and V06b reasoning/tool semantics remain **OPEN**.

Boundaries still outstanding in V06a: the lightweight structural index recognizes the tested GFM table/list/fence forms, not every CommonMark nested construct (e.g. deeply nested list/blockquote and pipe-looking text in unusual contexts); a single **table cell** larger than the direct 16 KiB parser cap or 512 rendered rows on one page can still be preview-limited, and a very long unsplittable inline construct may lose styling at its page seam. Plain completed lines and multi-page tables are accessible in the tested ranges, but this exceptional table-cell case must not be called complete. The 2 MiB index and 512 KiB styled-span budgets exclude transient source-line offsets, cloned page descriptors, and unmeasured allocator/RSS/PSS. The viewport still scans bounded loaded history/indexes on each frame; no independent long-soak measurement was run. No staging, commit, task progress, private config access, gate edits or historical-report edits occurred.

## Third independent audit: forward progress, long cell, nested table

This section supersedes the above **code-state** description, retaining every earlier attempt/capture. The three audit cases were reproduced with new tests before fixes: `v06a_oversized_grapheme_splitter_makes_progress` on `format!("a{}\n", "\u{0301}".repeat(5000))` timed out with `timeout 5s cargo test --locked -p oc-tui v06a_oversized_grapheme_splitter -- --nocapture`, observed `probe_exit_status=124`; `v06a_large_table_cell_scrolls_as_grid_through_its_real_tail` failed (exit **101**, table bottom/grid missing at 42 cells); and `v06a_blockquoted_150_row_table_preserves_parser_structure_and_scroll_count` failed (exit **101**, **155 indexed vs 306 full-renderer rows**). The same grapheme test under `timeout 10s` after the correction exited **0** with `probe_exit_status=0`. The large-cell test exposed an additional missing synthetic header, then an incorrect arbitrary >512-row expectation at width 74 (the actual cell wraps to >300 rows at that width); the former was fixed, the latter corrected to assert >512 only at width 42 and >300 at width 74. An intermediate targeted clippy run exited **101** on `type_complexity`; a named `LongCellSegments` structure fixed it. None of these failures is claimed as a passing baseline.

The source grapheme splitter now consumes the first complete grapheme even when it exceeds the ordinary 4096-byte page target, guaranteeing forward progress without splitting its Unicode cluster. Structural table recognition carries one or more blockquote prefixes into the existing parser-backed table slices. A row with an oversized final cell remains one **logical** table row: the index keeps the real header, label, escaped-pipe-aware cell boundary and suffix, divides only cell content into width-dependent bounded source fragments, and gives each fragment to pulldown-cmark as a table continuation. Synthetic header/row context is concealed on later slices; the visible grid keeps one label, one row separator, real tail and final border. Tests at widths 42/74 scroll through the 22.5 KiB cell and count **all 4500 `word` occurrences**, exactly one label and one horizontal separator, with no preview marker or prose outside the grid. A combined long-cell/blockquoted-table test also reaches its next real row and closing border. The 150-row quoted table matches the full renderer's row count and sampled styled rows through scrolling. The short original fixture/table styled-cell behavior remains covered.

Final-code checks: `cargo fmt --all -- --check && cargo test --locked --workspace --no-fail-fast --quiet -- --test-threads=1 && cargo clippy --locked --workspace --all-targets -- -D warnings && cargo build --locked && python3 scripts/check_docs.py && git diff --check` **exit 0** end-to-end (156 `oc-tui` unit tests, five existing ignored workspace tests); `cargo test --locked -p oc-tui v06a_ -- --test-threads=1` **exit 0** (27 focused tests). HEAD remains `98e81eedea4963bc16aae1d944210f9da65c18c1` with uncommitted source diff SHA-256 `9c125c16f4a6f7a98bdf5da293c203664061a06a980bb3cd15cbc3c5eabefca9`, source manifest SHA-256 `2d2655b428f00307a17e4374f8ab586564d9024f2e51b5d330a55a18391bfbc5`, Rust executable SHA-256 `c2e891d6cb6ef47c95f1e55b468f7aa15a6b7f3e36d9bd6493a8f76afcf2cb2c` (latest capture lock). No changes outside `crates/oc-tui/src/messages.rs` since the previous review apart from this append-only report and new immutable capture artifacts.

Fresh paired actual PTY artifacts **after** those checks are `paired-80x24-audit3/` and `paired-120x40-audit3/`. Each command used the same original/Rust executables, fake-provider table fixture SHA-256 `2c3e6ae556935ce85de2854ab4e192097cede6ca85128b95eaaa593a78b1ac41`, xterm.js 6/Unicode11 Chromium/DejaVu profile and hidden sidebar within its pair; original and Rust executed with `provider_contract=true`. Both capture commands exited **1** because post-capture full-frame comparators remain DIFFERENT. Completed-session 80×24: **1054/1920** styled cells and **5464/258816** PNG pixels different, while the diagnostic table rectangle `(6,1,70,7)` is **490/490 equal styled cells**. Completed-session 120×40: **1652/4800** styled cells and **5428/647040** pixels different, diagnostic table rectangle `(6,1,112,24)` **2688/2688 equal styled cells**. The paired fixture is still the normal 11-row table, not the three newly tested stress inputs; the latter have Rust viewport tests, not external paired PTY qualification. VIS13/VIS14, full-frame parity, live/replay and V06b remain **OPEN**.

Actual limits: the long-cell slices preserve this test's table semantics and content, but their word-wrap at a slice seam is not proved cell-for-cell identical to an unlimited upstream long-cell render. Structural recognition does not cover arbitrary nested Markdown shapes or every inline syntax state crossing a cell fragment; a **single grapheme** larger than the direct 16 KiB parser budget can still produce an explicit preview-limit marker, with complete source retained in history rather than silently disappearing. Transient indexing allocations and real RSS/PSS have not been measured. No staging, commit, progress change, private workspace config access, historical-report rewrite or gate change occurred.

## Final independent review: escaped label pipe on long-cell continuation

Test-first `v06a_blank_table_label_respects_escaped_and_inline_pipes` exited **101** on the prior implementation: blanking `| a \| b | ` yielded `|    |   | `, exposing **three** raw pipe characters instead of the two structural delimiters. The new 22.5 KiB escaped-label viewport regression also exited **101** (empty bottom at width 42: continuation parsed the value as another column). After changing **only** the synthetic row-label blanking logic, the focused regression initially failed one test-only top-of-scroll assertion because a one-row viewport at maximum scroll contains the transcript's top padding; the assertion was corrected to use an eight-row viewport, retaining all content, style and scroll checks. No earlier tests were weakened.

`blank_table_label` now tracks backslash escapes and inline-code state while replacing non-delimiter label text with whitespace. It emits only actual table column delimiters: neither `\|` nor a pipe inside a single-backtick code span becomes a bare structural `|` in later visual slices. The viewport regression covers widths **42 and 74**, the original `a | b` label exactly once, **all 4,500 `word` occurrences** in the real second cell, `TAILCELL`, the next real row, exactly **two** horizontal row separators, visible grid and body token style, and zero preview-limit markers. The unit test checks escaped and inline pipe prefixes directly. This is a bounded correction to synthetic continuation; no title/DTO/permissions/V06b semantics were changed.

On the final source SHA, `cargo fmt --all -- --check`, 29 targeted `v06a_` tests, the serial `cargo test --locked --workspace --no-fail-fast --quiet -- --test-threads=1` (158 `oc-tui` unit tests and five existing workspace ignores), `cargo clippy --locked --workspace --all-targets -- -D warnings`, `cargo build --locked`, `python3 scripts/check_docs.py`, and `git diff --check` all exited **0**. HEAD remains `98e81eedea4963bc16aae1d944210f9da65c18c1`; latest capture locks identify uncommitted source diff SHA-256 `78c12f9b947a2d7c614db1856084e9d271fb0751709bcf9688c6bda583e65f46`, source manifest SHA-256 `827a4ef4fa79ef0b61026a422a0f8f1158bbebaf9daea4b3ff7d53e83313dd38`, rebuilt Rust binary SHA-256 `b271b7f1d5540b872a29f1ccfcf9061d227cac4b73174b2e8371a668fa631f44`.

Fresh immutable actual original/Rust PTY pairs `paired-80x24-escaped-label/` and `paired-120x40-escaped-label/` were captured **after** the code/checks with the same normal 11-row fake-provider fixture SHA-256 `2c3e6ae556935ce85de2854ab4e192097cede6ca85128b95eaaa593a78b1ac41`, hidden sidebar and identical terminal/renderer profile within each pair. Both sides completed with `provider_contract=true`. Both capture commands exited **1** because their **post-capture whole-frame** grid/PNG comparisons remain DIFFERENT: completed-session 80×24 **1054/1920** cells and **5502/258816** pixels; 120×40 **1655/4800** cells and **5473/647040** pixels. Diagnostic post-capture table rectangles match **490/490** styled cells and **2688/2688** styled cells respectively. The 22.5 KiB escaped-pipe input is covered by Rust viewport tests, **not** by those reference PTY pairs; VIS13/VIS14 and full-frame parity remain **OPEN**. Earlier capture artifacts remain untouched. No staging, commit, task progress update, private config access or historical-report rewrite occurred.
