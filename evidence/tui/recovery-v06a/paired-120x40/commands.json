[
  {
    "argv": [
      "/usr/bin/node",
      "/home/opencode/ai/oc/scripts/tui_capture/capture.mjs",
      "--reference",
      "/home/opencode/.cache/opencode-tmp/opencode/t44-reference/package/bin/opencode",
      "--oc",
      "/home/opencode/ai/oc/target/debug/oc",
      "--build-oc",
      "true",
      "--geometry",
      "true",
      "--sample",
      "table",
      "--sidebar",
      "hide",
      "--columns",
      "120",
      "--rows",
      "40",
      "--output",
      "/home/opencode/ai/oc/evidence/tui/recovery-v06a/paired-120x40"
    ],
    "role": "capture runner invocation",
    "exit_code": 1
  },
  {
    "argv": [
      "git",
      "rev-parse",
      "HEAD"
    ],
    "exit_code": 0,
    "stdout": "98e81eedea4963bc16aae1d944210f9da65c18c1\n",
    "stderr": ""
  },
  {
    "argv": [
      "git",
      "rev-parse",
      "HEAD^{tree}"
    ],
    "exit_code": 0,
    "stdout": "d359756595c49a240ab928de418c0878ba9b1a91\n",
    "stderr": ""
  },
  {
    "argv": [
      "git",
      "diff",
      "--binary"
    ],
    "exit_code": 0,
    "stdout": "diff --git a/Cargo.lock b/Cargo.lock\nindex 8a2f402..21d8ebb 100644\n--- a/Cargo.lock\n+++ b/Cargo.lock\n@@ -1375,6 +1375,7 @@ dependencies = [\n  \"crossterm\",\n  \"oc-adapters\",\n  \"oc-core\",\n+ \"pulldown-cmark\",\n  \"ratatui\",\n  \"serde\",\n  \"serde_json\",\n@@ -1522,6 +1523,17 @@ dependencies = [\n  \"windows\",\n ]\n \n+[[package]]\n+name = \"pulldown-cmark\"\n+version = \"0.13.0\"\n+source = \"registry+https://github.com/rust-lang/crates.io-index\"\n+checksum = \"1e8bbe1a966bd2f362681a44f6edce3c2310ac21e4d5067a6e7ec396297a6ea0\"\n+dependencies = [\n+ \"bitflags\",\n+ \"memchr\",\n+ \"unicase\",\n+]\n+\n [[package]]\n name = \"quinn\"\n version = \"0.11.12\"\n@@ -2504,6 +2516,12 @@ version = \"1.20.1\"\n source = \"registry+https://github.com/rust-lang/crates.io-index\"\n checksum = \"b6f5e870be6c3b371b77fe0ee0bafb859fa4964b4404c27de1d380043c4dda20\"\n \n+[[package]]\n+name = \"unicase\"\n+version = \"2.9.0\"\n+source = \"registry+https://github.com/rust-lang/crates.io-index\"\n+checksum = \"dbc4bc3a9f746d862c45cb89d705aa10f187bb96c76001afab07a0d35ce60142\"\n+\n [[package]]\n name = \"unicode-ident\"\n version = \"1.0.26\"\ndiff --git a/crates/oc-tui/Cargo.toml b/crates/oc-tui/Cargo.toml\nindex 9ed0886..b98ac44 100644\n--- a/crates/oc-tui/Cargo.toml\n+++ b/crates/oc-tui/Cargo.toml\n@@ -21,6 +21,7 @@ unicode-normalization = \"0.1\"\n unicode-script = \"0.5\"\n unicode-segmentation = \"1.12\"\n unicode-width = \"0.2\"\n+pulldown-cmark = { version = \"=0.13.0\", default-features = false }\n \n [lints]\n workspace = true\ndiff --git a/crates/oc-tui/src/app.rs b/crates/oc-tui/src/app.rs\nindex b089aea..e130fd1 100644\n--- a/crates/oc-tui/src/app.rs\n+++ b/crates/oc-tui/src/app.rs\n@@ -256,6 +256,7 @@ pub struct TuiState {\n     input: String,\n     editor: crate::editor::Editor,\n     window: HistoryWindow,\n+    markdown_cache: std::cell::RefCell<crate::messages::MarkdownCache>,\n     live_text: String,\n     /// Reasoning text streamed for the active turn (never persisted).\n     live_reasoning: String,\n@@ -334,6 +335,7 @@ impl TuiState {\n             input: String::new(),\n             editor: Default::default(),\n             window: HistoryWindow::new(),\n+            markdown_cache: std::cell::RefCell::new(Default::default()),\n             live_text: String::new(),\n             live_reasoning: String::new(),\n             live_parts: Vec::new(),\n@@ -423,6 +425,7 @@ impl TuiState {\n         self.input.clear();\n         self.editor.clear();\n         self.window = HistoryWindow::new();\n+        *self.markdown_cache.get_mut() = Default::default();\n         self.live_text.clear();\n         self.live_reasoning.clear();\n         self.live_parts.clear();\n@@ -819,6 +822,7 @@ impl TuiState {\n     /// window caps.\n     pub fn retained_bytes(&self) -> usize {\n         self.window.retained_bytes()\n+            + self.markdown_cache.borrow().retained_bytes()\n             + self.live_text.len()\n             + self.live_reasoning.len()\n             + self\n@@ -892,12 +896,13 @@ impl TuiState {\n     /// projection used for scroll metrics and plain-text assertions.\n     pub fn transcript_lines(&self, width: u16, terminal_width: u16) -> Vec<Line> {\n         let theme = Theme::dark();\n-        crate::messages::transcript(\n+        crate::messages::transcript_with_cache(\n             &self.transcript_rows(),\n             theme,\n             width,\n             terminal_width,\n             |agent| self.agent_color(agent),\n+            Some(&self.markdown_cache),\n         )\n     }\n \ndiff --git a/crates/oc-tui/src/messages.rs b/crates/oc-tui/src/messages.rs\nindex 6622f3b..4b1ab5c 100644\n--- a/crates/oc-tui/src/messages.rs\n+++ b/crates/oc-tui/src/messages.rs\n@@ -21,6 +21,8 @@\n //!   (`routes/session/index.tsx:1934-1985`).\n \n use ratatui::style::{Color, Modifier, Style};\n+use pulldown_cmark::{CodeBlockKind, Event, Options, Parser, Tag, TagEnd};\n+use std::{cell::RefCell, collections::VecDeque, hash::{Hash, Hasher}};\n \n use crate::history::HistoryRow;\n use crate::styled::{self, Line, Span};\n@@ -40,6 +42,53 @@ pub const FOOTER_MODEL_MIN_WIDTH: u16 = 28;\n pub const FOOTER_DURATION_MIN_WIDTH: u16 = 36;\n /// `InlineToolRow` icon column width (`message-parts.tsx:18,213-219`).\n pub const INLINE_ICON_WIDTH: usize = 2;\n+const MAX_MARKDOWN_ROWS: usize = 512;\n+const MAX_CACHED_BYTES: usize = 512 * 1024;\n+\n+struct CachedBlock {\n+    revision: u64,\n+    width: u16,\n+    theme: crate::theme::ThemeMode,\n+    lines: Vec<Line>,\n+    bytes: usize,\n+}\n+\n+/// Per-session completed-part cache, bounded independently of history. The\n+/// content hash is the part revision; width and theme invalidate geometry.\n+#[derive(Default)]\n+pub(crate) struct MarkdownCache {\n+    blocks: VecDeque<CachedBlock>,\n+    bytes: usize,\n+    #[cfg(test)]\n+    parses: usize,\n+}\n+\n+impl MarkdownCache {\n+    fn render(&mut self, text: &str, theme: &Theme, width: u16) -> Vec<Line> {\n+        let mut hasher = std::collections::hash_map::DefaultHasher::new();\n+        text.hash(&mut hasher);\n+        let revision = hasher.finish();\n+        if let Some(position) = self.blocks.iter().position(|b| b.revision == revision && b.width == width && b.theme == theme.mode()) {\n+            let block = self.blocks.remove(position).expect(\"cache index\");\n+            let lines = block.lines.clone();\n+            self.blocks.push_back(block);\n+            return lines;\n+        }\n+        #[cfg(test)] { self.parses += 1; }\n+        let lines = markdown_block(text, theme, width);\n+        let bytes: usize = lines.iter().flat_map(Line::spans).map(|s| s.content().len()).sum();\n+        if bytes <= MAX_CACHED_BYTES {\n+            while self.bytes.saturating_add(bytes) > MAX_CACHED_BYTES || self.blocks.len() >= 240 {\n+                if let Some(old) = self.blocks.pop_front() { self.bytes -= old.bytes; } else { break; }\n+            }\n+            self.bytes += bytes;\n+            self.blocks.push_back(CachedBlock { revision, width, theme: theme.mode(), lines: lines.clone(), bytes });\n+        }\n+        lines\n+    }\n+\n+    pub(crate) fn retained_bytes(&self) -> usize { self.bytes }\n+}\n \n /// Kind of one user-message chip (`routes/session/index.tsx:2346-2393`).\n #[derive(Debug, Clone, Copy, PartialEq, Eq)]\n@@ -119,6 +168,13 @@ pub fn transcript(\n     width: u16,\n     terminal_width: u16,\n     agent_color: impl Fn(Option<&str>) -> Color,\n+) -> Vec<Line> {\n+    transcript_with_cache(rows, theme, width, terminal_width, agent_color, None)\n+}\n+\n+pub(crate) fn transcript_with_cache(\n+    rows: &[HistoryRow], theme: &Theme, width: u16, terminal_width: u16,\n+    agent_color: impl Fn(Option<&str>) -> Color, cache: Option<&RefCell<MarkdownCache>>,\n ) -> Vec<Line> {\n     let mut out = Vec::new();\n     for row in rows {\n@@ -130,6 +186,7 @@ pub fn transcript(\n                 width,\n                 terminal_width,\n                 &agent_color,\n+                cache,\n             )),\n             \"tool\" => {\n                 if let Some(card) = &row.tool {\n@@ -273,6 +330,7 @@ fn assistant_block(\n     width: u16,\n     terminal_width: u16,\n     agent_color: &impl Fn(Option<&str>) -> Color,\n+    cache: Option<&RefCell<MarkdownCache>>,\n ) -> Vec<Line> {\n     let mut out = Vec::new();\n     if let Some(reasoning) = &row.reasoning {\n@@ -281,7 +339,10 @@ fn assistant_block(\n     }\n     if !row.text.trim().is_empty() {\n         out.push(Line::plain(\"\"));\n-        out.extend(markdown_block(&row.text, theme, width));\n+        out.extend(match cache {\n+            Some(cache) => cache.borrow_mut().render(&row.text, theme, width),\n+            None => markdown_block(&row.text, theme, width),\n+        });\n     }\n     if let Some(meta) = &row.meta\n         && let Some(footer) = footer_line(\n@@ -377,7 +438,7 @@ fn markdown_block(text: &str, theme: &Theme, width: u16) -> Vec<Line> {\n     };\n     let pad = Span::plain(\" \".repeat(MESSAGE_PADDING));\n     let mut out = Vec::new();\n-    for line in styled::wrap_lines(&markdown(text, theme), inner) {\n+    for line in markdown_at_width(text, theme, inner) {\n         let mut spans = vec![pad.clone()];\n         spans.extend(line.spans().iter().cloned());\n         out.push(Line::new(spans));\n@@ -469,214 +530,202 @@ fn tokens_per_second(meta: &AssistantMeta) -> Option<f64> {\n     Some(output / (streamed_ms as f64 / 1_000.0))\n }\n \n-/// Markdown subset renderer (`message-parts.tsx:156-171`, default\n-/// `markdownMode === \"rendered\"` conceals markers, `routes/session/index.tsx:228`).\n-///\n-/// Supported: ATX headings, unordered/ordered list items, blockquotes,\n-/// fenced code blocks (``` / ~~~ with an info string), horizontal rules,\n-/// paragraphs, and inline code/bold/italic/links. Everything else keeps its\n-/// source text (tables render as paragraph rows, an unclosed fence streams as\n-/// code); nothing is dropped.\n+/// CommonMark + GFM tables, with upstream's concealed markers and grid tables\n+/// (`message-parts.tsx:159-170`). The public unbounded projection is for\n+/// inspection; the screen uses `markdown_at_width` with a measured cell width.\n pub fn markdown(text: &str, theme: &Theme) -> Vec<Line> {\n-    let mut out = Vec::new();\n-    let mut lines = text.split('\\n');\n-    while let Some(raw) = lines.next() {\n-        let line = raw.trim_end_matches('\\r');\n-        if let Some((fence, lang)) = open_fence(line) {\n-            let mut body = Vec::new();\n-            for next in lines.by_ref() {\n-                let next = next.trim_end_matches('\\r');\n-                if is_fence_close(next, fence) {\n-                    break;\n-                }\n-                body.push(next.to_string());\n-            }\n-            out.extend(code_lines(lang, &body, theme));\n-            continue;\n-        }\n-        if line.trim().is_empty() {\n-            out.push(Line::plain(\"\"));\n-            continue;\n-        }\n-        let trimmed = line.trim_start();\n-        if let Some(level) = heading_level(trimmed) {\n-            let content = trimmed[level..].trim_start();\n-            out.push(Line::new(inline(content, theme, MarkdownToken::Heading)));\n-            continue;\n-        }\n-        if let Some(rest) = trimmed\n-            .strip_prefix(\"- \")\n-            .or_else(|| trimmed.strip_prefix(\"* \"))\n-            .or_else(|| trimmed.strip_prefix(\"+ \"))\n-        {\n-            out.push(list_line_with_marker(\"- \", rest, theme));\n-            continue;\n-        }\n-        if let Some((marker, rest)) = ordered_marker(trimmed) {\n-            out.push(list_line_with_marker(marker, rest, theme));\n-            continue;\n-        }\n-        if let Some(rest) = trimmed.strip_prefix('>') {\n-            let rest = rest.strip_prefix(' ').unwrap_or(rest);\n-            out.push(Line::new(inline(rest, theme, MarkdownToken::BlockQuote)));\n-            continue;\n-        }\n-        if is_horizontal_rule(trimmed) {\n-            out.push(Line::styled(\n-                trimmed,\n-                Style::default().fg(theme.markdown(MarkdownToken::HorizontalRule)),\n-            ));\n-            continue;\n-        }\n-        out.push(Line::new(inline(trimmed, theme, MarkdownToken::Text)));\n-    }\n-    out\n+    markdown_at_width(text, theme, usize::MAX)\n }\n \n-/// Fence opener: at least three backticks or tildes plus an optional info\n-/// string; returns the fence char and the (lowercased later) language token.\n-fn open_fence(line: &str) -> Option<(char, Option<&str>)> {\n-    let trimmed = line.trim_start();\n-    let fence = trimmed.chars().next()?;\n-    if fence != '`' && fence != '~' {\n-        return None;\n-    }\n-    let run = trimmed.chars().take_while(|ch| *ch == fence).count();\n-    if run < 3 {\n-        return None;\n-    }\n-    let info = trimmed[run..].trim();\n-    let lang = info\n-        .split_whitespace()\n-        .next()\n-        .filter(|item| !item.is_empty());\n-    Some((fence, lang))\n+/// Model text must never emit raw terminal control sequences, even inside a\n+/// code/table event. Keep printable Unicode and make controls visible as U+FFFD.\n+fn safe_text(text: &str) -> String {\n+    text.chars().map(|c| if c.is_control() { '�' } else { c }).collect()\n }\n \n-/// Fence closer: same fence char, at least three, nothing else on the row.\n-fn is_fence_close(line: &str, fence: char) -> bool {\n-    let trimmed = line.trim();\n-    trimmed.chars().count() >= 3 && trimmed.chars().all(|ch| ch == fence)\n+#[derive(Default)]\n+struct TableDraft {\n+    rows: Vec<Vec<Line>>,\n+    row: Vec<Line>,\n+    header: bool,\n }\n \n-/// ATX heading level; returns the byte length of the `#… ` marker.\n-fn heading_level(line: &str) -> Option<usize> {\n-    let hashes = line.chars().take_while(|ch| *ch == '#').count();\n-    if hashes == 0 || hashes > 6 {\n-        return None;\n-    }\n-    let bytes = hashes;\n-    let rest = line.get(bytes..)?;\n-    if rest.starts_with(' ') || rest.is_empty() {\n-        Some(bytes)\n-    } else {\n-        None\n+fn markdown_at_width(text: &str, theme: &Theme, width: usize) -> Vec<Line> {\n+    let mut options = Options::empty();\n+    options.insert(Options::ENABLE_TABLES);\n+    let mut out = Vec::new();\n+    let mut spans = Vec::new();\n+    let mut stack = Vec::new();\n+    let mut lists: Vec<Option<u64>> = Vec::new();\n+    let mut code: Option<(String, String)> = None;\n+    let mut table: Option<TableDraft> = None;\n+    let mut previous_end = 0;\n+    let mut depth = 0usize;\n+    let mut truncated = false;\n+    for (event, range) in Parser::new_ext(text, options).into_offset_iter() {\n+        if out.len() >= MAX_MARKDOWN_ROWS { truncated = true; break; }\n+        if depth == 0 && !matches!(event, Event::End(_)) {\n+            let gap = &text[previous_end..range.start];\n+            out.extend(std::iter::repeat_with(|| Line::plain(\"\"))\n+                .take(gap.bytes().filter(|c| *c == b'\\n').count()\n+                    .saturating_sub(usize::from(previous_end == 0 || !text[..previous_end].ends_with('\\n')))));\n+        }\n+        match event {\n+            Event::Start(tag) => {\n+                depth += 1;\n+                match &tag {\n+                    Tag::Table(_) => table = Some(TableDraft::default()),\n+                    Tag::TableHead => { if let Some(t) = &mut table { t.header = true; } },\n+                    Tag::TableRow => { if let Some(t) = &mut table { t.row.clear(); } },\n+                    Tag::TableCell => spans.clear(),\n+                    Tag::CodeBlock(kind) => {\n+                        let lang = match kind {\n+                            CodeBlockKind::Fenced(info) => info.split_whitespace().next().unwrap_or(\"\").to_string(),\n+                            CodeBlockKind::Indented => String::new(),\n+                        };\n+                        code = Some((lang, String::new()));\n+                    }\n+                    Tag::List(start) => lists.push(*start),\n+                    Tag::Item => {\n+                        let ordered = lists.last_mut().and_then(Option::as_mut);\n+                        let (marker, token) = match ordered {\n+                            Some(n) => { let marker = format!(\"{n}. \"); *n += 1; (marker, MarkdownToken::ListEnumeration) },\n+                            None => (\"- \".into(), MarkdownToken::ListItem),\n+                        };\n+                        spans.push(Span::styled(marker, Style::default().fg(theme.markdown(token))));\n+                    }\n+                    Tag::Heading { .. } => stack.push(MarkdownToken::Heading),\n+                    Tag::BlockQuote(_) => stack.push(MarkdownToken::BlockQuote),\n+                    Tag::Strong => stack.push(MarkdownToken::Strong),\n+                    Tag::Emphasis => stack.push(MarkdownToken::Emphasis),\n+                    Tag::Link { .. } => stack.push(MarkdownToken::LinkText),\n+                    Tag::Image { .. } => stack.push(MarkdownToken::ImageText),\n+                    _ => {}\n+                }\n+            }\n+            Event::End(end) => {\n+                match end {\n+                    TagEnd::TableCell => { if let Some(t) = &mut table { t.row.push(Line::new(std::mem::take(&mut spans))); } },\n+                    TagEnd::TableHead | TagEnd::TableRow => {\n+                        if let Some(t) = &mut table {\n+                            if !t.row.is_empty() { t.rows.push(std::mem::take(&mut t.row)); }\n+                            if end == TagEnd::TableHead { t.header = false; }\n+                        }\n+                    }\n+                    TagEnd::Table => {\n+                        if let Some(mut t) = table.take() {\n+                            if !t.row.is_empty() { t.rows.push(t.row); }\n+                            out.extend(render_table(&t.rows, theme, width));\n+                        }\n+                    }\n+                    TagEnd::CodeBlock => {\n+                        if let Some((lang, body)) = code.take() {\n+                            let body = body.trim_end_matches('\\n');\n+                            if !body.is_empty() {\n+                                for line in body.split('\\n') {\n+                                    if out.len() >= MAX_MARKDOWN_ROWS { truncated = true; break; }\n+                                    let highlighted = code_lines(Some(&lang), &[safe_text(line)], theme);\n+                                    out.extend(styled::wrap_line_limited(&highlighted[0], width, MAX_MARKDOWN_ROWS + 1 - out.len()));\n+                                }\n+                            }\n+                        }\n+                    }\n+                    TagEnd::List(_) => { lists.pop(); },\n+                    TagEnd::Paragraph | TagEnd::Heading(_) | TagEnd::Item => {\n+                        if !spans.is_empty() {\n+                            out.extend(styled::wrap_line_limited(&Line::new(std::mem::take(&mut spans)), width, MAX_MARKDOWN_ROWS + 1 - out.len()));\n+                        }\n+                        if matches!(end, TagEnd::Heading(_)) { stack.pop(); }\n+                    }\n+                    TagEnd::Strong | TagEnd::Emphasis | TagEnd::Link | TagEnd::Image | TagEnd::BlockQuote(_) => { stack.pop(); }\n+                    _ => {}\n+                }\n+                depth = depth.saturating_sub(1);\n+                if depth == 0 {\n+                    let trailing = text[range.clone()].bytes().rev().take_while(|c| *c == b'\\n').count();\n+                    out.extend(std::iter::repeat_with(|| Line::plain(\"\"))\n+                        .take(trailing.saturating_sub(1)));\n+                    previous_end = range.end;\n+                }\n+            }\n+            Event::Text(value) => {\n+                if let Some((_, body)) = &mut code { body.push_str(&value); }\n+                else {\n+                    let token = if table.as_ref().is_some_and(|t| t.header) { MarkdownToken::Heading }\n+                        else { stack.last().copied().unwrap_or(MarkdownToken::Text) };\n+                    spans.push(Span::styled(safe_text(&value), Style::default().fg(theme.markdown(token))));\n+                }\n+            }\n+            Event::Code(value) => spans.push(Span::styled(safe_text(&value), Style::default().fg(theme.markdown(MarkdownToken::Code)))),\n+            Event::SoftBreak | Event::HardBreak => {\n+                if !spans.is_empty() { out.extend(styled::wrap_line_limited(&Line::new(std::mem::take(&mut spans)), width, MAX_MARKDOWN_ROWS + 1 - out.len())); }\n+            }\n+            Event::Rule => out.push(Line::styled(\"───\", Style::default().fg(theme.markdown(MarkdownToken::HorizontalRule)))),\n+            Event::Html(value) | Event::InlineHtml(value) => spans.push(Span::styled(safe_text(&value), Style::default().fg(theme.markdown(MarkdownToken::Text)))),\n+            _ => {}\n+        }\n     }\n+    if out.len() > MAX_MARKDOWN_ROWS { truncated = true; }\n+    if truncated {\n+        out.truncate(MAX_MARKDOWN_ROWS - 1);\n+        out.push(Line::styled(\"… [Markdown preview limited]\", Style::default().fg(theme.text_muted())));\n+    } else if text.ends_with('\\n') { out.push(Line::plain(\"\")); }\n+    if out.is_empty() { out.push(Line::plain(\"\")); }\n+    out\n }\n \n-fn ordered_marker(line: &str) -> Option<(&str, &str)> {\n-    let digits = line.chars().take_while(char::is_ascii_digit).count();\n-    if digits == 0 {\n-        return None;\n-    }\n-    let rest = &line[digits..];\n-    let punctuation = rest.chars().next()?;\n-    if punctuation != '.' && punctuation != ')' {\n-        return None;\n+/// Grid style + one-cell horizontal padding from upstream TextPart's\n+/// `tableOptions={{style:\"grid\",cellPaddingX:1}}`. Widths use terminal cells.\n+fn render_table(rows: &[Vec<Line>], theme: &Theme, available: usize) -> Vec<Line> {\n+    let count = rows.iter().map(Vec::len).max().unwrap_or(0);\n+    if count == 0 { return Vec::new(); }\n+    let border = Style::default().fg(theme.border());\n+    let mut widths = (0..count).map(|column| rows.iter().filter_map(|row| row.get(column))\n+        .map(|cell| cell.spans().iter().map(styled::span_width).sum::<usize>()).max().unwrap_or(1).max(1)).collect::<Vec<_>>();\n+    let usable = available.saturating_sub(count * 3 + 1).max(count);\n+    if widths.iter().sum::<usize>() > usable {\n+        widths.fill(1);\n+        let extra = usable.saturating_sub(count);\n+        // Give the shorter label column room before assigning the remaining\n+        // cells to the long description; never allocate beyond the viewport.\n+        for _ in 0..extra.min(available) {\n+            let col = (0..count).filter(|&i| widths[i] < rows.iter().filter_map(|row| row.get(i))\n+                .map(|cell| cell.spans().iter().map(styled::span_width).sum()).max().unwrap_or(1))\n+                .min_by_key(|&i| widths[i]).unwrap_or(count - 1);\n+            widths[col] += 1;\n+        }\n     }\n-    let after = rest[punctuation.len_utf8()..].strip_prefix(' ')?;\n-    Some((&line[..digits + punctuation.len_utf8() + 1], after))\n-}\n-\n-fn is_horizontal_rule(line: &str) -> bool {\n-    let trimmed = line.trim();\n-    let marker = trimmed.chars().next().unwrap_or(' ');\n-    (marker == '-' || marker == '*' || marker == '_')\n-        && trimmed.chars().count() >= 3\n-        && trimmed.chars().all(|ch| ch == marker || ch == ' ')\n-        && trimmed.chars().filter(|ch| *ch == marker).count() >= 3\n-}\n-\n-/// List item: the source marker keeps the `listItem`/`listEnumeration` token\n-/// color (the concealed bullet glyph of `@opentui/core` is not in this\n-/// repository, so the real source marker is rendered instead of an invented one).\n-fn list_line_with_marker(marker: &str, rest: &str, theme: &Theme) -> Line {\n-    let ordered = marker.chars().next().is_some_and(|ch| ch.is_ascii_digit());\n-    let token = if ordered {\n-        MarkdownToken::ListEnumeration\n-    } else {\n-        MarkdownToken::ListItem\n-    };\n-    let mut spans = vec![Span::styled(\n-        marker,\n-        Style::default().fg(theme.markdown(token)),\n-    )];\n-    spans.extend(inline(rest, theme, MarkdownToken::Text));\n-    Line::new(spans)\n-}\n-\n-/// Inline subset: `` `code` ``, `**strong**`, `*emphasis*` / `_emphasis_`,\n-/// `[text](url)`. Unsupported markers stay literal.\n-fn inline(text: &str, theme: &Theme, base: MarkdownToken) -> Vec<Span> {\n-    let plain_style = Style::default().fg(theme.markdown(base));\n-    let chars: Vec<char> = text.chars().collect();\n-    let mut spans: Vec<Span> = Vec::new();\n-    let mut plain = String::new();\n-    let mut index = 0;\n-    while index < chars.len() {\n-        let ch = chars[index];\n-        if ch == '`'\n-            && let Some(end) = find_char(&chars, index + 1, '`')\n-            && end > index + 1\n-        {\n-            flush(&mut spans, &mut plain, plain_style);\n-            spans.push(Span::styled(\n-                chars[index + 1..end].iter().collect::<String>(),\n-                Style::default().fg(theme.markdown(MarkdownToken::Code)),\n-            ));\n-            index = end + 1;\n-            continue;\n+    let separator = |left: &str, middle: &str, right: &str| {\n+        let mut parts = vec![Span::styled(left, border)];\n+        for (i, width) in widths.iter().enumerate() {\n+            if i > 0 { parts.push(Span::styled(middle, border)); }\n+            parts.push(Span::styled(\"─\".repeat(width + 2), border));\n         }\n-        if ch == '*' && chars.get(index + 1) == Some(&'*') {\n-            if let Some(end) = find_double(&chars, index + 2)\n-                && end > index + 2\n-            {\n-                flush(&mut spans, &mut plain, plain_style);\n-                spans.push(Span::styled(\n-                    chars[index + 2..end].iter().collect::<String>(),\n-                    Style::default().fg(theme.markdown(MarkdownToken::Strong)),\n-                ));\n-                index = end + 2;\n-                continue;\n-            }\n-        } else if ch == '*' || ch == '_' {\n-            if let Some(end) = find_char(&chars, index + 1, ch)\n-                && end > index + 1\n-            {\n-                flush(&mut spans, &mut plain, plain_style);\n-                spans.push(Span::styled(\n-                    chars[index + 1..end].iter().collect::<String>(),\n-                    Style::default().fg(theme.markdown(MarkdownToken::Emphasis)),\n-                ));\n-                index = end + 1;\n-                continue;\n+        parts.push(Span::styled(right, border));\n+        Line::new(parts)\n+    };\n+    let mut out = vec![separator(\"┌\", \"┬\", \"┐\")];\n+    for (index, row) in rows.iter().enumerate().take(MAX_MARKDOWN_ROWS) {\n+        if out.len() >= MAX_MARKDOWN_ROWS { break; }\n+        let wrapped: Vec<Vec<Line>> = widths.iter().enumerate().map(|(i, &width)|\n+            row.get(i).map(|cell| styled::wrap_line_limited(cell, width, MAX_MARKDOWN_ROWS - out.len())).unwrap_or_else(|| vec![Line::plain(\"\")])).collect();\n+        let height = wrapped.iter().map(Vec::len).max().unwrap_or(1);\n+        for y in 0..height {\n+            if out.len() >= MAX_MARKDOWN_ROWS { break; }\n+            let mut parts = vec![Span::styled(\"│\", border)];\n+            for (i, cell) in wrapped.iter().enumerate() {\n+                parts.push(Span::plain(\" \"));\n+                if let Some(line) = cell.get(y) { parts.extend(line.spans().iter().cloned()); }\n+                let used = cell.get(y).map_or(0, |line| line.spans().iter().map(styled::span_width).sum::<usize>());\n+                parts.push(Span::plain(\" \".repeat(widths[i].saturating_sub(used) + 1)));\n+                parts.push(Span::styled(\"│\", border));\n             }\n-        } else if ch == '['\n-            && let Some((label, end)) = parse_link(&chars, index)\n-        {\n-            flush(&mut spans, &mut plain, plain_style);\n-            spans.push(Span::styled(\n-                label,\n-                Style::default().fg(theme.markdown(MarkdownToken::LinkText)),\n-            ));\n-            index = end;\n-            continue;\n+            out.push(Line::new(parts));\n         }\n-        plain.push(ch);\n-        index += 1;\n+        if index == 0 { out.push(separator(\"├\", \"┼\", \"┤\")); }\n     }\n-    flush(&mut spans, &mut plain, plain_style);\n-    spans\n+    out.push(separator(\"└\", \"┴\", \"┘\"));\n+    out\n }\n \n fn flush(spans: &mut Vec<Span>, plain: &mut String, style: Style) {\n@@ -685,26 +734,6 @@ fn flush(spans: &mut Vec<Span>, plain: &mut String, style: Style) {\n     }\n }\n \n-fn find_char(chars: &[char], from: usize, needle: char) -> Option<usize> {\n-    (from..chars.len()).find(|index| chars[*index] == needle)\n-}\n-\n-fn find_double(chars: &[char], from: usize) -> Option<usize> {\n-    (from..chars.len().saturating_sub(1))\n-        .find(|index| chars[*index] == '*' && chars[*index + 1] == '*')\n-}\n-\n-/// `[label](url)` starting at `start`; returns the label and the index just\n-/// past the closing `)`.\n-fn parse_link(chars: &[char], start: usize) -> Option<(String, usize)> {\n-    let label_end = find_char(chars, start + 1, ']')?;\n-    if label_end == start + 1 || chars.get(label_end + 1) != Some(&'(') {\n-        return None;\n-    }\n-    let url_end = find_char(chars, label_end + 2, ')')?;\n-    let label: String = chars[start + 1..label_end].iter().collect();\n-    Some((label, url_end + 1))\n-}\n \n /// Language for the hand-rolled code-block highlighter. Upstream highlights\n /// through tree-sitter WASM (`parsers-config.ts`, ~40 languages); this port\n@@ -1316,16 +1345,41 @@ mod tests {\n         assert!(styles[2].contains(&theme.markdown(MarkdownToken::Emphasis)));\n         assert!(styles[2].contains(&theme.markdown(MarkdownToken::LinkText)));\n \n-        // Unsupported inline markers keep their source text.\n+        // Strike is left literal without the extension; image syntax is concealed.\n         let lines = markdown(\"a ~~strike~~ b `unclosed and ![alt](u)\", theme);\n-        assert_eq!(plain(&lines[0]), \"a ~~strike~~ b `unclosed and !alt\");\n-        // A table row stays a paragraph (source text preserved).\n+        assert_eq!(plain(&lines[0]), \"a ~~strike~~ b `unclosed and alt\");\n+        // A table is a grid, with concealed Markdown delimiters.\n         let lines = markdown(\"| a | b |\\n|---|---|\\n| 1 | 2 |\", theme);\n-        assert_eq!(plain(&lines[0]), \"| a | b |\");\n-        assert_eq!(plain(&lines[1]), \"|---|---|\");\n-        assert_eq!(plain(&lines[2]), \"| 1 | 2 |\");\n+        assert!(lines.iter().any(|line| line.plain_text().contains(\"a\") && line.plain_text().contains(\"│\")));\n+        assert!(lines.iter().any(|line| line.plain_text().contains(\"1\") && line.plain_text().contains(\"2\")));\n+        assert!(!lines.iter().any(|line| line.plain_text().contains(\"---\")));\n     }\n \n+    #[test]\n+    fn v06a_table_fixture_escaped_pipes_unicode_and_bounded_wrap() {\n+        use std::path::Path;\n+        let fixture = include_str!(\"../../../tui-recovery/fixtures/transcript.md\");\n+        assert!(Path::new(\"tui-recovery/fixtures/transcript.md\").exists() || !fixture.is_empty());\n+        let theme = Theme::dark();\n+        let table = &fixture[fixture.find(\"| Инструмент\").unwrap()..fixture.find(\"Через Code Mode\").unwrap()];\n+        for width in [42, 80, 120] {\n+            let rows = markdown_block(table, theme, width);\n+            let grid: Vec<_> = rows.iter().map(Line::plain_text).collect();\n+            assert!(grid.iter().any(|r| r.contains(\"Инструмент\") && r.contains('│')), \"{width}: {grid:?}\");\n+            assert!(grid.iter().any(|r| r.contains(\"subagent\")), \"{width}: {grid:?}\");\n+            assert!(!grid.iter().any(|r| r.contains(\"| ---\")), \"{width}: {grid:?}\");\n+            assert!(rows.iter().all(|r| r.spans().iter().map(styled::span_width).sum::<usize>() <= width as usize));\n+        }\n+        let escaped = \"| Key | Value |\\n| --- | --- |\\n| a \\\\| b | 中文 🧑‍💻 emoji and a very long cell with more words to wrap |\";\n+        let rows = markdown_block(escaped, theme, 24);\n+        let text: String = rows.iter().map(Line::plain_text).collect::<Vec<_>>().join(\"\\n\");\n+        assert!(text.contains(\"a | b\"), \"{text}\");\n+        assert!(text.contains(\"中文\"), \"{text}\");\n+        assert!(text.contains(\"🧑‍💻\"), \"{text}\");\n+        assert!(rows.iter().all(|r| r.spans().iter().map(styled::span_width).sum::<usize>() <= 24));\n+    }\n+\n+\n     /// Code blocks: fenced language selection, token colors for the supported\n     /// subset, and the code-block color for unknown languages.\n     #[test]\ndiff --git a/crates/oc-tui/src/styled.rs b/crates/oc-tui/src/styled.rs\nindex 7385680..51bb6e2 100644\n--- a/crates/oc-tui/src/styled.rs\n+++ b/crates/oc-tui/src/styled.rs\n@@ -9,6 +9,8 @@\n \n use ratatui::style::Style;\n use ratatui::text::Text;\n+use unicode_segmentation::UnicodeSegmentation;\n+use unicode_width::UnicodeWidthStr;\n \n /// A piece of text with a single style.\n #[derive(Clone, Debug, Default, PartialEq, Eq)]\n@@ -186,9 +188,14 @@ pub fn span_width(span: &Span) -> usize {\n /// cell boundary. Leading spaces of a continuation row are dropped, exactly\n /// like a terminal text wrap.\n pub fn wrap_line(line: &Line, width: usize) -> Vec<Line> {\n+    wrap_line_limited(line, width, usize::MAX)\n+}\n+\n+/// At most `limit` wrapped rows; used for bounded Markdown previews.\n+pub fn wrap_line_limited(line: &Line, width: usize, limit: usize) -> Vec<Line> {\n     let max = width.max(1);\n-    let mut out: Vec<Vec<(char, Style)>> = Vec::new();\n-    let mut current: Vec<(char, Style)> = Vec::new();\n+    let mut out: Vec<Vec<(String, Style)>> = Vec::new();\n+    let mut current: Vec<(String, Style)> = Vec::new();\n     let mut used = 0usize;\n     // Index just past the last space run in `current`; a wrap can break there.\n     let mut break_at: Option<usize> = None;\n@@ -196,22 +203,23 @@ pub fn wrap_line(line: &Line, width: usize) -> Vec<Line> {\n     // the indentation of the source line.\n     let mut continuation = false;\n     for span in line.spans() {\n-        for ch in span.content().chars() {\n-            let cells = char_width(ch);\n+        for glyph in span.content().graphemes(true) {\n+            if out.len() >= limit { break; }\n+            let cells = UnicodeWidthStr::width(glyph);\n             if used + cells > max && !current.is_empty() {\n                 match break_at.take() {\n                     Some(index) => {\n                         let mut rest = current.split_off(index);\n-                        while rest.first().is_some_and(|(ch, _)| *ch == ' ') {\n+                        while rest.first().is_some_and(|(ch, _)| ch == \" \") {\n                             rest.remove(0);\n                         }\n                         let mut head = std::mem::take(&mut current);\n-                        while head.last().is_some_and(|(ch, _)| *ch == ' ') {\n+                        while head.last().is_some_and(|(ch, _)| ch == \" \") {\n                             head.pop();\n                         }\n                         out.push(head);\n                         current = rest;\n-                        used = current.iter().map(|(ch, _)| char_width(*ch)).sum();\n+                        used = current.iter().map(|(ch, _)| UnicodeWidthStr::width(ch.as_str())).sum();\n                     }\n                     None => {\n                         out.push(std::mem::take(&mut current));\n@@ -220,17 +228,17 @@ pub fn wrap_line(line: &Line, width: usize) -> Vec<Line> {\n                 }\n                 continuation = true;\n             }\n-            if ch == ' ' && current.is_empty() && continuation {\n+            if glyph == \" \" && current.is_empty() && continuation {\n                 continue;\n             }\n-            current.push((ch, span.style()));\n+            current.push((glyph.to_string(), span.style()));\n             used += cells;\n-            if ch == ' ' {\n+            if glyph == \" \" {\n                 break_at = Some(current.len());\n             }\n         }\n     }\n-    out.push(current);\n+    if out.len() < limit { out.push(current); }\n     out.into_iter()\n         .map(|cells| Line::new(coalesce(cells)))\n         .collect()\n@@ -246,13 +254,13 @@ pub fn wrap_lines(lines: &[Line], width: usize) -> Vec<Line> {\n }\n \n /// Merge consecutive cells with the same style back into spans.\n-fn coalesce(cells: Vec<(char, Style)>) -> Vec<Span> {\n+fn coalesce(cells: Vec<(String, Style)>) -> Vec<Span> {\n     let mut spans: Vec<Span> = Vec::new();\n     for (ch, style) in cells {\n         match spans.last_mut() {\n-            Some(span) if span.style() == style => span.content.push(ch),\n+            Some(span) if span.style() == style => span.content.push_str(&ch),\n             _ => spans.push(Span {\n-                content: ch.to_string(),\n+                content: ch,\n                 style,\n             }),\n         }\n",
    "stderr": ""
  },
  {
    "argv": [
      "git",
      "ls-files",
      "-z",
      "--cached",
      "--others",
      "--exclude-standard",
      "--",
      "Cargo.toml",
      "Cargo.lock",
      "rust-toolchain.toml",
      "crates",
      "scripts/tui_capture"
    ],
    "exit_code": 0,
    "stdout": "Cargo.lock\u0000Cargo.toml\u0000crates/oc-adapters/Cargo.toml\u0000crates/oc-adapters/src/application.rs\u0000crates/oc-adapters/src/application_selection.rs\u0000crates/oc-adapters/src/attachments.rs\u0000crates/oc-adapters/src/composition.rs\u0000crates/oc-adapters/src/config.rs\u0000crates/oc-adapters/src/dcp.rs\u0000crates/oc-adapters/src/dcp_auto.rs\u0000crates/oc-adapters/src/defs.rs\u0000crates/oc-adapters/src/discovery.rs\u0000crates/oc-adapters/src/files.rs\u0000crates/oc-adapters/src/lib.rs\u0000crates/oc-adapters/src/mcp_http_lifecycle.rs\u0000crates/oc-adapters/src/mcp_remote.rs\u0000crates/oc-adapters/src/mcp_result.rs\u0000crates/oc-adapters/src/mcp_stdio.rs\u0000crates/oc-adapters/src/models.rs\u0000crates/oc-adapters/src/patch.rs\u0000crates/oc-adapters/src/patch/fs.rs\u0000crates/oc-adapters/src/permissions.rs\u0000crates/oc-adapters/src/provider.rs\u0000crates/oc-adapters/src/runtime.rs\u0000crates/oc-adapters/src/shell.rs\u0000crates/oc-adapters/src/smoke.rs\u0000crates/oc-adapters/src/storage.rs\u0000crates/oc-adapters/src/tools.rs\u0000crates/oc-adapters/src/tui_workspace.rs\u0000crates/oc-adapters/src/webfetch.rs\u0000crates/oc-adapters/tests/blob_audit.rs\u0000crates/oc-adapters/tests/context_bounds.rs\u0000crates/oc-adapters/tests/dcp_atomic.rs\u0000crates/oc-adapters/tests/e2e_live.rs\u0000crates/oc-adapters/tests/e2e_offline.rs\u0000crates/oc-adapters/tests/mcp_remote.rs\u0000crates/oc-adapters/tests/mcp_stdio.rs\u0000crates/oc-adapters/tests/patch_audit.rs\u0000crates/oc-adapters/tests/permissions.rs\u0000crates/oc-adapters/tests/runtime.rs\u0000crates/oc-adapters/tests/shell_watchdog.rs\u0000crates/oc-adapters/tests/soak.rs\u0000crates/oc-adapters/tests/storage_lock.rs\u0000crates/oc-adapters/tests/subagent.rs\u0000crates/oc-core/Cargo.toml\u0000crates/oc-core/src/application.rs\u0000crates/oc-core/src/context_plan.rs\u0000crates/oc-core/src/core_app.rs\u0000crates/oc-core/src/domain.rs\u0000crates/oc-core/src/lib.rs\u0000crates/oc-core/src/ports.rs\u0000crates/oc-core/src/queries.rs\u0000crates/oc-core/src/runtime.rs\u0000crates/oc-core/src/session.rs\u0000crates/oc-tui/Cargo.toml\u0000crates/oc-tui/assets/fuzzysort-LICENSE\u0000crates/oc-tui/assets/fuzzysort-oracle.json\u0000crates/oc-tui/assets/upstream/PROVENANCE.md\u0000crates/oc-tui/assets/upstream/v2/opencode.json\u0000crates/oc-tui/src/app.rs\u0000crates/oc-tui/src/commands.rs\u0000crates/oc-tui/src/dcp_panel.rs\u0000crates/oc-tui/src/dialog.rs\u0000crates/oc-tui/src/editor.rs\u0000crates/oc-tui/src/events.rs\u0000crates/oc-tui/src/fuzzy.rs\u0000crates/oc-tui/src/history.rs\u0000crates/oc-tui/src/layout.rs\u0000crates/oc-tui/src/lib.rs\u0000crates/oc-tui/src/messages.rs\u0000crates/oc-tui/src/picker.rs\u0000crates/oc-tui/src/shell.rs\u0000crates/oc-tui/src/smoke.rs\u0000crates/oc-tui/src/styled.rs\u0000crates/oc-tui/src/terminal.rs\u0000crates/oc-tui/src/theme.rs\u0000crates/oc-tui/src/tools.rs\u0000crates/oc-tui/src/views.rs\u0000crates/oc/Cargo.toml\u0000crates/oc/src/bootstrap.rs\u0000crates/oc/src/cli.rs\u0000crates/oc/src/headless.rs\u0000crates/oc/src/main.rs\u0000crates/oc/src/tui_cmd.rs\u0000crates/oc/tests/application.rs\u0000crates/oc/tests/configured_workspace.rs\u0000crates/oc/tests/dcp_runtime.rs\u0000crates/oc/tests/durability.rs\u0000crates/oc/tests/golden_binary.rs\u0000crates/oc/tests/live_bounded.rs\u0000crates/oc/tests/mcp_application.rs\u0000crates/oc/tests/memory_bounds.rs\u0000crates/oc/tests/pty.rs\u0000crates/oc/tests/pty_t39.rs\u0000crates/oc/tests/pty_t42.rs\u0000crates/oc/tests/recovery_startup.rs\u0000crates/oc/tests/recovery_v02.rs\u0000crates/oc/tests/recovery_v03.rs\u0000crates/oc/tests/responses.rs\u0000crates/oc/tests/support/geometry.py\u0000crates/oc/tests/support/startup.py\u0000crates/oc/tests/support/title.rs\u0000rust-toolchain.toml\u0000scripts/tui_capture/README.md\u0000scripts/tui_capture/bridge.py\u0000scripts/tui_capture/capture.mjs\u0000scripts/tui_capture/check_frontend.mjs\u0000scripts/tui_capture/frontend.js\u0000scripts/tui_capture/fuzzy_oracle.mjs\u0000scripts/tui_capture/geometry.mjs\u0000scripts/tui_capture/inspect_blanks.py\u0000",
    "stderr": ""
  },
  {
    "argv": [
      "cargo",
      "build",
      "--locked"
    ],
    "exit_code": 0,
    "stdout": "",
    "stderr": "   Compiling oc-tui v0.1.0 (/home/opencode/ai/oc/crates/oc-tui)\n   Compiling oc v0.1.0 (/home/opencode/ai/oc/crates/oc)\n    Finished `dev` profile [unoptimized + debuginfo] target(s) in 4.67s\n"
  },
  {
    "argv": [
      "fc-match",
      "-f",
      "%{family}|%{style}|%{file}|%{fontversion}",
      "DejaVu Sans Mono"
    ],
    "exit_code": 0,
    "stdout": "DejaVu Sans Mono|Book|/usr/share/fonts/truetype/dejavu/DejaVuSansMono.ttf|155320",
    "stderr": ""
  },
  {
    "argv": [
      "/usr/bin/python3",
      "/home/opencode/ai/oc/scripts/tui_capture/bridge.py",
      "/home/opencode/ai/oc/evidence/tui/recovery-v06a/paired-120x40/upstream/bridge-spec.json"
    ],
    "exit_code": 0
  },
  {
    "argv": [
      "/usr/bin/python3",
      "/home/opencode/ai/oc/scripts/tui_capture/bridge.py",
      "/home/opencode/ai/oc/evidence/tui/recovery-v06a/paired-120x40/oc/bridge-spec.json"
    ],
    "exit_code": 0
  },
  {
    "argv": [
      "/usr/bin/python3",
      "/home/opencode/ai/oc/tui-recovery/scripts/compare_frames.py",
      "grid",
      "/home/opencode/ai/oc/evidence/tui/recovery-v06a/paired-120x40/upstream/home.cells.json",
      "/home/opencode/ai/oc/evidence/tui/recovery-v06a/paired-120x40/oc/home.cells.json",
      "--report",
      "/home/opencode/ai/oc/evidence/tui/recovery-v06a/paired-120x40/home.grid-diff.json"
    ],
    "exit_code": 1,
    "stdout": "{\"kind\": \"grid\", \"status\": \"FAIL\", \"scope\": \"frame equality only; capture provenance is not attested\", \"scenario\": \"home\", \"fixture_sha256\": \"2c3e6ae556935ce85de2854ab4e192097cede6ca85128b95eaaa593a78b1ac41\", \"environment_id\": \"e3cf33539f0e1d6485c01217ef4f632171a7de480eea7bdd9f9c598ea70f45be\", \"reference_sha256\": \"4108a8d8155fb5752d754f40ada4c6ff83a81ad60f2e3310f95b1caa6991213a\", \"actual_sha256\": \"50eae7f38581289297736f87a51dc5d9689141930818d54adafcd0f07a5749fa\", \"reference_commit\": \"2670273ff17da96f85c5826ced57aa1b368754fa\", \"actual_commit\": \"98e81eedea4963bc16aae1d944210f9da65c18c1\", \"cells_checked\": 4800, \"different_cells\": 4501, \"cursor_differs\": false, \"difference_bbox_inclusive\": [0, 0, 119, 39], \"samples\": [{\"x\": 0, \"y\": 0, \"different_fields\": [\"fg\"]}, {\"x\": 1, \"y\": 0, \"different_fields\": [\"fg\"]}, {\"x\": 2, \"y\": 0, \"different_fields\": [\"fg\"]}, {\"x\": 3, \"y\": 0, \"different_fields\": [\"fg\"]}, {\"x\": 4, \"y\": 0, \"different_fields\": [\"fg\"]}, {\"x\": 5, \"y\": 0, \"different_fields\": [\"fg\"]}, {\"x\": 6, \"y\": 0, \"different_fields\": [\"fg\"]}, {\"x\": 7, \"y\": 0, \"different_fields\": [\"fg\"]}, {\"x\": 8, \"y\": 0, \"different_fields\": [\"fg\"]}, {\"x\": 9, \"y\": 0, \"different_fields\": [\"fg\"]}, {\"x\": 10, \"y\": 0, \"different_fields\": [\"fg\"]}, {\"x\": 11, \"y\": 0, \"different_fields\": [\"fg\"]}, {\"x\": 12, \"y\": 0, \"different_fields\": [\"fg\"]}, {\"x\": 13, \"y\": 0, \"different_fields\": [\"fg\"]}, {\"x\": 14, \"y\": 0, \"different_fields\": [\"fg\"]}, {\"x\": 15, \"y\": 0, \"different_fields\": [\"fg\"]}, {\"x\": 16, \"y\": 0, \"different_fields\": [\"fg\"]}, {\"x\": 17, \"y\": 0, \"different_fields\": [\"fg\"]}, {\"x\": 18, \"y\": 0, \"different_fields\": [\"fg\"]}, {\"x\": 19, \"y\": 0, \"different_fields\": [\"fg\"]}]}\n",
    "stderr": ""
  },
  {
    "argv": [
      "/usr/bin/python3",
      "/home/opencode/ai/oc/tui-recovery/scripts/compare_frames.py",
      "png",
      "/home/opencode/ai/oc/evidence/tui/recovery-v06a/paired-120x40/upstream/home.png",
      "/home/opencode/ai/oc/evidence/tui/recovery-v06a/paired-120x40/oc/home.png",
      "--report",
      "/home/opencode/ai/oc/evidence/tui/recovery-v06a/paired-120x40/home.png-diff.json"
    ],
    "exit_code": 1,
    "stdout": "{\"kind\": \"png\", \"status\": \"FAIL\", \"scope\": \"decoded RGBA equality only; use capture lock for provenance/profile\", \"reference_sha256\": \"afb2e34b731c2406303fae20870c1c9b134cbc670017dc45fccea8b99f2ed8d6\", \"actual_sha256\": \"54382814b2a8461ffd3ab881ee3c4d4986c368c1a98ba69124de7c112db6a1b5\", \"width\": 1011, \"height\": 640, \"pixels_checked\": 647040, \"different_pixels\": 3546, \"changed_fraction_diagnostic_only\": 0.005480341246290801, \"difference_bbox_inclusive\": [196, 320, 992, 620]}\n",
    "stderr": ""
  },
  {
    "argv": [
      "/usr/bin/python3",
      "/home/opencode/ai/oc/tui-recovery/scripts/compare_frames.py",
      "grid",
      "/home/opencode/ai/oc/evidence/tui/recovery-v06a/paired-120x40/upstream/session-wide-completed.cells.json",
      "/home/opencode/ai/oc/evidence/tui/recovery-v06a/paired-120x40/oc/session-wide-completed.cells.json",
      "--report",
      "/home/opencode/ai/oc/evidence/tui/recovery-v06a/paired-120x40/session-wide-completed.grid-diff.json"
    ],
    "exit_code": 1,
    "stdout": "{\"kind\": \"grid\", \"status\": \"FAIL\", \"scope\": \"frame equality only; capture provenance is not attested\", \"scenario\": \"session-wide-completed\", \"fixture_sha256\": \"2c3e6ae556935ce85de2854ab4e192097cede6ca85128b95eaaa593a78b1ac41\", \"environment_id\": \"e3cf33539f0e1d6485c01217ef4f632171a7de480eea7bdd9f9c598ea70f45be\", \"reference_sha256\": \"94254eb8132e96f22df7fa6a607b5facaa6b5b90e8ea11b2fec64106e4f16938\", \"actual_sha256\": \"0767c3b04957f71fb01f241a180f7e86b953b893e10e402bf2497ff046ebc5a6\", \"reference_commit\": \"2670273ff17da96f85c5826ced57aa1b368754fa\", \"actual_commit\": \"98e81eedea4963bc16aae1d944210f9da65c18c1\", \"cells_checked\": 4800, \"different_cells\": 4402, \"cursor_differs\": false, \"difference_bbox_inclusive\": [0, 0, 119, 39], \"samples\": [{\"x\": 0, \"y\": 0, \"different_fields\": [\"fg\"]}, {\"x\": 1, \"y\": 0, \"different_fields\": [\"symbol\", \"fg\"]}, {\"x\": 2, \"y\": 0, \"different_fields\": [\"fg\"]}, {\"x\": 3, \"y\": 0, \"different_fields\": [\"modifiers\"]}, {\"x\": 4, \"y\": 0, \"different_fields\": [\"modifiers\"]}, {\"x\": 5, \"y\": 0, \"different_fields\": [\"modifiers\"]}, {\"x\": 6, \"y\": 0, \"different_fields\": [\"modifiers\"]}, {\"x\": 7, \"y\": 0, \"different_fields\": [\"modifiers\"]}, {\"x\": 8, \"y\": 0, \"different_fields\": [\"modifiers\"]}, {\"x\": 9, \"y\": 0, \"different_fields\": [\"modifiers\"]}, {\"x\": 10, \"y\": 0, \"different_fields\": [\"modifiers\"]}, {\"x\": 11, \"y\": 0, \"different_fields\": [\"modifiers\"]}, {\"x\": 12, \"y\": 0, \"different_fields\": [\"modifiers\"]}, {\"x\": 13, \"y\": 0, \"different_fields\": [\"modifiers\"]}, {\"x\": 14, \"y\": 0, \"different_fields\": [\"modifiers\"]}, {\"x\": 15, \"y\": 0, \"different_fields\": [\"modifiers\"]}, {\"x\": 16, \"y\": 0, \"different_fields\": [\"modifiers\"]}, {\"x\": 17, \"y\": 0, \"different_fields\": [\"modifiers\"]}, {\"x\": 18, \"y\": 0, \"different_fields\": [\"modifiers\"]}, {\"x\": 19, \"y\": 0, \"different_fields\": [\"modifiers\"]}]}\n",
    "stderr": ""
  },
  {
    "argv": [
      "/usr/bin/python3",
      "/home/opencode/ai/oc/tui-recovery/scripts/compare_frames.py",
      "png",
      "/home/opencode/ai/oc/evidence/tui/recovery-v06a/paired-120x40/upstream/session-wide-completed.png",
      "/home/opencode/ai/oc/evidence/tui/recovery-v06a/paired-120x40/oc/session-wide-completed.png",
      "--report",
      "/home/opencode/ai/oc/evidence/tui/recovery-v06a/paired-120x40/session-wide-completed.png-diff.json"
    ],
    "exit_code": 1,
    "stdout": "{\"kind\": \"png\", \"status\": \"FAIL\", \"scope\": \"decoded RGBA equality only; use capture lock for provenance/profile\", \"reference_sha256\": \"75112707d2d4669cce6fa281c41349a42498befe7730136f5c76c0f4c791ff8e\", \"actual_sha256\": \"db3222f7e6fdd886bc2484ffa3b20321bfe82030045e41960c4d7069f8991810\", \"width\": 1011, \"height\": 640, \"pixels_checked\": 647040, \"different_pixels\": 112004, \"changed_fraction_diagnostic_only\": 0.1731021266073195, \"difference_bbox_inclusive\": [10, 3, 992, 600]}\n",
    "stderr": ""
  }
]
