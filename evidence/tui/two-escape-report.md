# T44 / VIS28 — guarded Esc interruption (2026-09-25)

**Partial behavior correction, not VIS28/V09 PASS.** Pinned original `opencode/packages/tui/src/component/prompt/index.tsx:499–528` arms `session.interrupt` on the first focused Esc and resets that guard after five seconds; the second Esc interrupts the accepted running session. Its running footer changes from `esc interrupt` to `esc again to interrupt` in warning color (`:108–143,1885–1899`). Native had cancelled on the first Esc. The native TUI now arms the accepted streaming turn for five seconds before calling the existing application cancel owner on the second press. Pending submission, modal and idle Esc retain their existing owners; receipt/turn transitions clear the arm. On genuine interruption the native footer labels the cancelled outcome `interrupted`, matching the original without falsifying storage status.

Both immutable source-built 120×40, Reader/tools, animations-disabled paired attempts exercised a **held real fake-provider Responses request** and recorded both Esc inputs, painted hints and provider events: [`two-escape-20260925-01`](two-escape-20260925-01/capture.lock.json) before the footer-label correction, [`two-escape-20260925-02`](two-escape-20260925-02/capture.lock.json) after it. The runner now requires first Esc to display `esc again to interrupt` *while the request remains held* and requires exactly a second Esc to cancel. Both sides in both runs report `SCANNER_PASS`, `two_escape_guard=true`, valid provider contract, durable accepted input, no completed answer, no extra transcript requests, and server release. Rust raw PTY and TUI tests also cover the same guard, timeout re-arm and warning color.

Full-frame source-built `-02` styled-grid and PNG comparators still exit **1/DIFFERENT** (no masks): running fallback **32/4800 differing cells** at independently animated title row y0; interrupted **29/4800 differing cells** also only at y0. The interrupted footer, duration, geometry and all other styled cells happened to match in this run (2.3s each), **not** a deterministic elapsed-time guarantee. The original launched a real title request while the main response was held and displayed the resulting title; native still waits for main-turn completion before its title request, so it displays `Untitled session` on cancellation. The `-01` interrupted frame additionally exposed the native `cancelled` footer label, fixed in `-02`. All failed regional/full comparator artifacts remain immutable. No VIS28 or final V09 is declared PASS.

The captures retain VT, styled-cell grids, PNGs, per-stage comparator JSON, input/protocol logs and source/build hashes. Pinned reference executable SHA-256 `2b0825721cb12f9bca3d5099588087d557a21ed2b5b56efebea3f17dc5f79e6a`. The first full workspace gate exposed two existing PTY expectations for one-Esc cancellation and the stale `cancelled` label; both tests were updated to assert the pinned behavior (not skipped). After correction the serial **fmt, locked workspace tests, all-target Clippy `-D warnings`, locked build, docs/progress, JS/Python syntax and diff gate PASS**, with no test failures. Output: `/home/opencode/.local/share/opencode/tool-output/tool_0da63b1fc001uvGVDhwLZnwxde`. A green code gate is not a pixel gate.
