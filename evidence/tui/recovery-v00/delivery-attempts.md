# Delivery attempts

- Initial checkpoint command exit 1: required `## Risks` heading was combined with
  Next. Corrected note structure; second checkpoint exit 0, progress engine unchanged.
- Initial staged `git diff --cached --check` exit 2: captured full-width text grids
  have intentional trailing blank cells and final blank rows. Commit/push did not run.
  Added directory-local attributes for those capture `.txt` files only and raw VT
  binary treatment, preserving evidence bytes. Source whitespace checks remain intact.
