# T44 — paired Markdown under Commands/Models; resize PNG edge diagnosis

**Diagnostic slice, not VIS05/VIS08/VIS09/VIS24 or T44 PASS.** Pinned original OpenCode v2.0.12 source `2670273ff17da96f85c5826ced57aa1b368754fa`, executable SHA-256 `2b0825721cb12f9bca3d5099588087d557a21ed2b5b56efebea3f17dc5f79e6a`. Paired fixture uses an explicitly configured `Reader` primary profile, its real instruction validated in both Responses requests, isolated independent HOME/data, the same public Markdown/table content and terminal profile. No real credentials. All attempts below retain locks, actual PTY input/output, protocol checks, styled cells, unmasked PNGs and comparator results. All paired PTYs `EXECUTED`, `provider_contract=true`; all whole-frame comparators exit **1 DIFFERENT**.

## Table and prose at 160×48

Captured fresh pairs `recovery-v04/dialogs-current-20260924-{01,table-02,prose-03}/`, all immutable. Final fixture SHA-256 `4af8f46c344629b5bfde86f0cce4502e4958841eb671ef245e10c305a9e15cd2`, terminal ID `50d156640d3a2bd27b172e65def22101e07bba6485084f80c11f3b8dbb49abea`, rebuilt native executable SHA-256 `323b7b0d33bf40511edc219ad3109352aee4ef712660e1b769940dcb52693dca`; final capture lock attests original Rust HEAD `146af21` plus dirty source diff `c41a9bff286e43d9b8e245f6a39bc3ce50f4f721a8c8e444ca45f5ddc48afa5a`. The implemented code is committed separately as `1ef0123`.

At baseline `-01`, full completed-session grid differed in 568/7680 cells: source-backed table grid left divider at x22 upstream versus x23 native (outer x5/x115 equal), changing its lines through y32. Pinned TextPart uses `style: grid,cellPaddingX:1` and OpenTUI's `TextTable` distributes spare content width across columns. Production `messages.rs::fit_table_widths` now shares even spare-width distribution for full/indexed tables (preserving constrained-width behavior). A failing-before-fix regression on the source fixture covered 80/120/160, Unicode and paged width. After that fix `table-02` completed frame differed in 21 cells; after preserving **only the real source Markdown separator space** after `navigate,` on the line wrap at x113,y36, `prose-03` completed grid differs in 17/7680 cells, all on the elapsed/token-rate footer row y39. No synthetic whitespace is added for exact-fit/no separator. Existing bounded parse/index and full-versus-visible paths pass. This is not a full frame PASS because reported elapsed time/token-rate differ and are not fabricated. The paired dialog chrome title/search/selection position matches, but Commands remains 290/7680 cells different and Models 102/7680 cells different in `prose-03`: original offers real operations including Rename session, integrations/settings/share; native's menu is a different set. No inert palette item was painted. In pinned source `component/session-frame.tsx:182–195`, sidebar is a **real palette action**; its omission from a *visible slice* of the original dialog is not grounds to remove it from native.

## Narrow 80×24 PNG despite equal VT cells

The reflow fixture was rerun as `recovery-v03/vis05-reflow-geometry-20260924-{01,02,refreshed}/`, using the same executable original and fixture SHA-256 `d8a8a24d5639214a6708d97e6336fec6cb3d7e43fe4691fbb9a2e50230d48fc5`. Both sides pass the real scroll/resize/draft/cursor predicates. In `scroll-shrink`, VT grid equals 0/1920 cell differences and cursor equality, but decoded PNG still differs at exactly x673,y352–383 (32 pixels: original `#1e1e1e`, native `#0a0a0a`). New per-frame `.render.json` locks the screenshot clip, screen bounds, DPR, CSS rows and trailing child rectangles. Both sides have the exact 674×384 screen/clip and identical dimensions before/after screenshot. In upstream, the DOM row y22 has an extra painted `#1e1e1e` span starting at x673.375 and extending *beyond* the 674px screen to x976.36 (old 160-column row); native row ends at x673.375 and paints no such span. The matching cell buffer only has 80 visible cells, so a browser renderer displays different off-grid pixels at the border after resize. Calling xterm's `refresh(0, rows-1)` equally on both sides in the independent `-refreshed` attempt **did not** remove the extra span or the 32 pixels; do not call this resolved by repaint or mask it. The opt-in repaint flag and its negative evidence are preserved. No production cell-style change is justified from an off-grid browser span. The active full-frame PNG parity gate remains open.

## Checks and next

`CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=1 cargo test --locked --workspace --no-fail-fast --quiet` PASS, 220 `oc-tui` tests and existing opt-in live ignores; `cargo fmt --all -- --check`, workspace all-target Clippy `-D warnings`, `cargo build --locked`, Node syntax, test-only `check_capture_geometry.mjs` and `check_frontend.mjs`, `python3 scripts/check_docs.py`, `python3 scripts/progress.py check`, and `git diff --check` PASS before checkpoint. Both source edits independently reviewed with no actionable findings. The per-frame probe records no DOM text, credentials, URLs or canvas pixels. Pending: original/native Commands and Models actions/dialog content, 80×24 PNG edge without masks, dynamic elapsed/version, remaining VIS01–VIS24 and V08–V09/S07. Next smallest actionable palette gap is **real** Rename session, requiring owner-backed title mutation and actual dialog (not a decorative entry); source behavior/validation requires a new paired interaction first. T44 stays active.
