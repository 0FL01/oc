# T44 VIS09 — paired Models interaction (2026-09-25)

**Interaction: PASS on both sides in the latest capture; full-frame parity: DIFFERENT (exit 1).** Evidence is the five preserved, immutable `models-20260925-{01,02,03,04,05}/` attempts, especially `models-20260925-05/capture.lock.json`, per-side `models-interaction-checks.json`, actual `.cells.json`/`.txt`/`.vt`/`.png` frames, and each `*.grid-diff.json`/`*.png-diff.json`. Comparator reports assess frame equality only; provenance and the actual interaction predicates come from the lock and per-side checks. No VIS09/full T44 gate is claimed complete.

## Setup and real effect

For attempts `-01`–`-04`, the opt-in `--models-interaction true --geometry true --sample tools --sidebar hide --agent-profile true` ran pinned upstream v2.0.12 (`2670273ff17da96f85c5826ced57aa1b368754fa`, executable SHA-256 `2b0825721cb12f9bca3d5099588087d557a21ed2b5b56efebea3f17dc5f79e6a`) against native `oc` at **120×40** with the same terminal profile (`environment_id=edd489131c5c831e7992c354ccc570c61c1996dd9abf3c1478ebd872024f2ecc`), isolated fixture (`fixture_sha256=c83225fa0b228cf3eacc6c1b6576b8a3d1cdaea9d33d38464d3c24c20c1025ac`), Reader agent and fake Responses provider. The catalog has eight normal models plus twelve configured `ZZ Scroll 00`–`ZZ Scroll 11` models; this extension is scoped to the opt-in. Both applications begin on `fixture-model-1` (painted `MiMo-V2.6-Flash Free`). No named variant was configured or switched; the recorded `no_named_variant` predicate is true, and this is **not** a variants test.

After the first completed `read` round-trip and title, real Ctrl+X then `m` opens Models. Initial dialog: both sides paint the same first 14 options at `x=34, y=15–28`; the current-model marker is at `(32,18)`, distinct from the focus on the first option. Sixteen Down keys scroll to the same 14 visible model names and positions: `Nemotron 3 Ultra Free`/`Nemotron 3.5 Lightning Free` at `y=15/16`, followed by `ZZ Scroll 00`–`ZZ Scroll 11` at `y=17–28`; `ZZ Scroll 08` is focused at `y=25`, with the original current-model marker offscreen on **both** sides. Typing `ZZ Scroll 11` filters to that focused target at `y=15`; Return closes the dialog and paints the new model in the same-session composer. There is no provider request during navigation/selection. The next turn retains the original answer and same session/title, performs another real `read`, and completes on the selected `fixture-scroll-11` model (`ZZ Scroll 11`).

Each `-04` per-side lock attempt reports `MODELS_INTERACTION_PASS` and `provider_contract=true`: exactly **four valid completed transcript requests + one valid completed title request**, zero invalid. The first turn uses two transcript rounds on `fixture-model-1` plus one title request; the second uses two transcript rounds on `fixture-scroll-11`. The per-side checks confirm the second read result, title stability, unchanged first answer, and no named variant. These are per-side functional predicates, not assertions that the two applications emit identical request bodies. No raw requests or response bodies are reproduced here.

## Attempts and comparison

| Immutable attempt | Native provenance / interaction | Scrolled full grid | Outcome |
| --- | --- | --- | --- |
| `models-20260925-01/` | Existing executable; build/source association unattested; upstream PASS, native **FAILED** at scrolled predicate (`offscreen_current_marker_absent=false`). | No paired scrolled native frame (BLOCKED). | Early diagnostic failure; later captures blocked. Exit 1. |
| `models-20260925-02/` | Existing executable SHA-256 `2a54e8702d5bb1d2b187f919d4040b5af49454f11acd8e0e4b55308d58010222`; association with the recorded dirty source **not attested**. Both interaction PASS, but scroll policy differs: original current marker absent, native still visible on the original model. | **324/4800** differing styled cells (bbox `x=30–89, y=15–31`). | Seven grid/PNG pairs DIFFERENT; exit 1. |
| `models-20260925-03/` | `cargo build --locked` executed by that capture (exit 0); source-built from dirty HEAD `a4e712d271fea7e4e11e0b86381fa50020a74d3d`, lock dirty-diff SHA-256 `97943c522933f620702a9dbdb5b0abaf30c14c13b87da334521396483ab03e18`, executable SHA-256 `9a658cb6c7a47345726d76832177a052b03a6e9b5f6692d6e1d30aeb3b6e6b28`. Both interaction PASS; scroll policy matches. | **88/4800** (bbox `x=30–89, y=30–31`). | Seven grid/PNG pairs DIFFERENT; exit 1. |
| `models-20260925-04/` | Source-built dirty HEAD `a4e712d271fea7e4e11e0b86381fa50020a74d3d`; capture's `cargo build --locked` exit 0, dirty-diff SHA-256 `bfe05c77e36e36bde4ff3bdce25a21e3c1b7dbf15b94422bb1656e31b63ddf1d`, source-manifest SHA-256 `8054292c1e1939c67d1f95075d8cff3913d41868b343173150085a2aa0715fe3`, executable SHA-256 `9a658cb6c7a47345726d76832177a052b03a6e9b5f6692d6e1d30aeb3b6e6b28`. Both `MODELS_INTERACTION_PASS`, scroll policy matches. | **88/4800** (bbox `x=30–89, y=30–31`). | **All seven** grid and PNG comparisons DIFFERENT; runner exit **1**. |
| `models-20260925-05/` | Distinct **160×48** capture after dynamically sized runner initial predicate fix; source-built dirty HEAD `a4e712d271fea7e4e11e0b86381fa50020a74d3d`, capture build exit 0; both `MODELS_INTERACTION_PASS`, scroll policy matches with current marker **visible**. Provenance and measurements below. | **90/7680** (bbox `x=38–109, y=10–37`). | **All seven** grid and PNG comparisons DIFFERENT; runner exit **1**. |

`-04` unmasked comparisons (each grid checks 4,800 cells; each PNG checks 647,040 decoded RGBA pixels; every row has grid **FAIL** and PNG **FAIL**, comparator exit 1):

| Paired frame | Different styled cells | Different pixels | Observed scope |
| --- | ---: | ---: | --- |
| `home` | 6 | 298 | Actual version text in bottom status: `2.0.12` upstream vs `0.1.0` native. |
| `session-wide-completed` | 2 | 117 | First-turn elapsed display (`126ms` upstream vs `132ms` native in this attempt). |
| `models-initial` | 88 | 9,141 | Dialog's upstream-only `View all integrations ctrl+a` action (row 30). |
| `models-scrolled` | 88 | 9,141 | The **14 painted model rows, focus, current-marker policy and cursor agree**; upstream-only integration action remains at row 30. |
| `models-filtered` | 88 | 9,141 | Upstream-only integration action shifts to row 17 in the filtered dialog. |
| `models-selected` | 101 | 12,933 | Header/background and elapsed differences after dialog closure. |
| `models-second-turn` | 572 | 67,149 | Original paints durable `Switched model to ZZ Scroll 11` between turns (at text row 13); native has **no model-switched durable notice**. Real second-turn elapsed is `80ms` upstream vs `128ms` native; first-turn elapsed remains `126ms` vs `132ms`. Neither clock nor elapsed/token-rate output was frozen or masked. |

The upstream integration action is genuine but absent natively; adding a painted-only option would not supply its behavior. The durable switch notice likewise remains missing. The tested navigation and wire selection do **not** establish exact Models rendering, integrations behavior, variants, resize, persistence after restart, or VIS09 parity.

## Latest attempt `-05`: 160×48

`models-20260925-05/commands.json` records `--columns 160 --rows 48 --build-oc true`, the same interaction/geometry/tools/hidden-sidebar/Reader opt-in, and capture runner exit **1**. The runner's initial dialog predicate was made dynamic for the larger viewport (runner `capture.mjs` SHA-256 `4ec34dcaa957f1a9755d20c913ed0b596c1febf269e69d2c7671933337ad5000`; `bridge.py` SHA-256 `b17abde5d053fa31f4f4708c81c1740b89580792df7d2efa9f9c647ed42154e5`). The capture lock records the same fixture SHA-256 `c83225fa0b228cf3eacc6c1b6576b8a3d1cdaea9d33d38464d3c24c20c1025ac` and shared per-side `environment_id=046b0a19f15a5d820e9833b6f9c82e7a046adfc0c07326340dc055b19c3009b4`: xterm.js 6.0.0 with Unicode 11, Chromium `145.0.7632.6`, DejaVu Sans Mono 14, `TERM=xterm-256color`, `C.UTF-8`, dark `opencode` theme, sidebar hide, no masking or frozen application clock. Pinned original executable SHA-256 is `2b0825721cb12f9bca3d5099588087d557a21ed2b5b56efebea3f17dc5f79e6a` at `2670273ff17da96f85c5826ced57aa1b368754fa`; native dirty HEAD is `a4e712d271fea7e4e11e0b86381fa50020a74d3d`, dirty-diff SHA-256 `b908497fee9f8a9f57aa9c541834e60bac05321f224d4eacb262a6db71213951`, source-manifest SHA-256 `fc348d467b74bf12c46eddeb3b7796bf35088975d3a30e7508baac7edf4ad155`, executable SHA-256 `9a658cb6c7a47345726d76832177a052b03a6e9b5f6692d6e1d30aeb3b6e6b28`. The capture's `cargo build --locked` exited 0 (`commands.json`); this is capture provenance, not the pending full workspace gate.

Both per-side checks and lock say `MODELS_INTERACTION_PASS`, `provider_contract=true`: **4/4 completed transcript requests, 1/1 completed title request, zero invalid** on each side. First turn uses `fixture-model-1`, second turn uses `fixture-scroll-11`; second read result, first answer, stable title/session, no requests during dialog navigation, and `no_named_variant` all pass. At 160×48 the initial dialog paints **18** models at `x=54, y=17–34` (current marker `(52,20)`), not 14 at the 120×40 position. After 16 Down keys both sides paint `MiMo V2.5 Free` through `ZZ Scroll 11` at `y=17–34`, with `ZZ Scroll 08` focused at `y=31` and the original `MiMo-V2.6-Flash Free` **still visible** with marker `(52,18)`; `offscreen_current_marker_absent=false` is an observation, not a failed predicate. The lock's `scroll_policy_comparison.same=true` reflects that both sides have `current_model_visible=true` and `current_marker_on_current_model=true`. Filtering and Return select the target in the same session. The upstream-only `View all integrations ctrl+a` appears at row 36 (row 19 when filtered); native omits it. Original alone paints the durable `Switched model to ZZ Scroll 11` notice in the second-turn frame.

Latest `-05` unmasked full-frame comparisons (each grid checks **7,680** styled cells; each 1349×768 PNG checks **1,036,032** decoded RGBA pixels; all seven grid and PNG statuses **FAIL**, comparator exits 1):

| Paired frame | Different styled cells | Different pixels | Observed scope |
| --- | ---: | ---: | --- |
| `home` | 6 | 294 | Version display differs (`2.0.12` vs `0.1.0`). |
| `session-wide-completed` | 2 | 107 | First-turn elapsed `155ms` original vs `139ms` native. |
| `models-initial` | 90 | 9,256 | First-turn elapsed and upstream-only integration action at row 36. |
| `models-scrolled` | 90 | 9,256 | The 18 painted model names/positions, focus and visible current-marker policy agree; elapsed and integration action differ. |
| `models-filtered` | 90 | 9,261 | Elapsed and upstream-only integration action at row 19. |
| `models-selected` | 101 | 12,923 | Header/background and elapsed differences after closure. |
| `models-second-turn` | 732 | 88,675 | Upstream durable switch notice absent natively; first-turn elapsed `155ms` vs `139ms`, second-turn `77ms` vs `128ms`. |

These are diagnostics, **not** a visual PASS or resize-in-place test. The `-05` lock still labels qualification `DIAGNOSTIC_BASELINES_ONLY`; exact Models rendering and remaining T44 VIS gates remain open. The runner's `no_named_variant` check inspects the absence of a top-level request key, not a selected named-variant round-trip; a separate variant fixture remains necessary. Preserve every failed/diagnostic attempt for review.

## Workspace qualification for this slice

After the final Rust and runner edits, serialized `cargo fmt --all -- --check`, `CARGO_BUILD_JOBS=3 RUST_TEST_THREADS=2 TMPDIR=/home/opencode/.cache/opencode-tmp/opencode/oc-test-bench-20260924 cargo test --locked --workspace --no-fail-fast` (including 321 `oc-tui` tests and the new centered-viewport regression), all-target workspace Clippy with `-D warnings`, `cargo build --locked`, `scripts/check_docs.py`, `scripts/progress.py check`, Node syntax, Python compile, and `git diff --check` all passed with zero test failures. Full output: `/home/opencode/.local/share/opencode/tool-output/tool_0d999cfd9001DHOpwreR8LXmKz`. This green code gate does not make any of the seven paired full-frame differences equal, and is not final-SHA T44/V09 qualification. An owner-backed persisted model-switch notice and a functioning integration action are still absent; do not paint inert replacements for either.
