# T44 / VIS28 — title generated concurrently with held main request (2026-09-26)

**Real behavior correction and one equal interrupted frame, not final VIS28/V09 PASS.** Pinned `opencode/packages/core/src/session/runner/llm.ts:160–193` starts the first title request after accepted input is promoted and before the main model step completes; `session/title.ts:100–149` guards against replacing a changed title. Native previously waited for successful main completion, leaving cancelled sessions untitled. The application owner now starts one bounded title-provider task only after the user turn is durably accepted for an untitled root. It uses the configured title agent/model and a distinct cancel flag, and only the owner can commit a normalized provider result, guarded by the title/event-sequence CAS. The main turn and title request do not wait on or cancel each other; there is no invented title, extra conversational turn or assistant answer. The accepted title reaches the open TUI through a title-update event. Successful Location switches and shutdown join/cancel outstanding tasks; rejected switches retain the current title task.

Immutable paired 120×40 Reader/tools captures [`early-title-20260926-01`](early-title-20260926-01/capture.lock.json) and [`early-title-20260926-02`](early-title-20260926-02/capture.lock.json) use the pinned original binary and freshly source-built native executable against **independent real fake-Responses requests** held until two Esc presses. Both programs returned `SCANNER_PASS` with `two_escape_guard=true`, a real title-provider completion while the primary response remained held, durable accepted input, provider cancellation and no extra transcript requests or completed assistant answer. Both displayed the same provider-generated title before/after main cancellation. The `-01` **interrupted** styled grid is EQUAL in all 4,800 cells including cursor and PNG is EQUAL in all 647,040 pixels without masks. In that run both independent clocks happened to show the same elapsed digit: this is a single observed exact frame, not a controlled replay or a claim that every run matches. The independent repeat `-02` is **DIFFERENT** in exactly one duration digit (cell `(39,6)`, 43 pixels); all other interrupted styled cells including title match. Home and running fallback are DIFFERENT in both attempts. The capture runner's aggregate exit is 1 in each attempt, correctly preserving all failed comparisons.

Focused owner/provider regressions cover a held primary request and concurrently fulfilled title, main/title cancellation independence, late manual rename and rename-away/back protection, no duplicate title request after main completion, configured title model, successful/failed reload fencing, and bounded drain on shutdown. Integration fake servers were updated to classify the genuine title request independently of the main request rather than assume a serial request order. Full serial workspace gate **PASS**: fmt; locked workspace tests (zero failures); all-target Clippy `-D warnings`; locked build; documentation and progress checks; capture JS/Python syntax; diff check. Output: `/home/opencode/.local/share/opencode/tool-output/tool_0da95f4fe001MFRpuvCuT6Lf82`. This gate and the single exact interrupted frame do not satisfy the full VIS01–VIS28/V09 matrix or deterministic elapsed-time equality.
