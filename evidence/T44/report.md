# T44 — parity qualification remains open (2026-09-26)

Sessions source slice passes the final serialized workspace gate
`tool_0de60cc67001Tsl2vZu7GUCjgE`. Final source-built
[`sessions-live-11`](../tui/recovery-v00/sessions-live-11/capture.lock.json) verifies
search/scope/current and foreign Open/Esc/reopen/selected rename/confirmed delete
on both executables without additional provider requests. All 62 full grid/PNG
comparisons remain DIFFERENT; current-day behavior is verified, multi-day is not.
Legacy unknown update times remain explicitly unavailable. Full diagnosis and
evidence: [`sessions-correction-report.md`](../tui/recovery-v00/sessions-correction-report.md).
Owner amendment `c452180` now requires whole-tail Redo and the real reverted card;
the earlier delivered one-turn Redo is superseded and is not current acceptance.

Latest verified source slice implements profile titlecase/silent success, real
Ctrl+T variant cycling, and supported Shell/Explored hover/expansion with required
successful Shell spacing. Full workspace gate PASS:
`tool_0dd8e54820010JHYPv5cQJpKAw`. Final source-built paired captures
[`bounded-shell-09`](../tui/recovery-v00/bounded-shell-09/capture.lock.json) and
[`bounded-variants-06`](../tui/recovery-v00/bounded-variants-06/capture.lock.json)
pass behavioral checks; all full grid/PNG comparisons remain DIFFERENT. Actual
request effort cycles absent/high/low/absent. Shell commands execute exactly twice
and hover/expand/recollapse makes no further requests. Explicit owner suppression
of synthetic exit-zero text is preserved despite the original's observed suffix;
no empty successful-output placeholder is fabricated. Full factual evidence:
[`bounded-evidence-report.md`](../tui/recovery-v00/bounded-evidence-report.md).

**Owner decision 2026-09-26:** approved conversation/context-only `/undo`, `/redo` and
Revert, snapshots disabled by default and no workspace/file/Git rollback. The former
file-revert requirement is superseded, not satisfied by experimental snapshot code.
Approved contract/work plan: `tui-recovery/T44_CONTRACT_AMENDMENT.md`. The owner
subsequently resumed implementation. The native conversation boundary, incremental
DCP metadata revisions, commands and Message Actions are implemented and regression
checked; experimental filesystem snapshots were removed, not replaced by a preimage
journal. See [conversation-only implementation evidence](conversation-only-report.md).
All unrelated open parity requirements remain open.

**Status: ACTIVE, implementation resumed, not `TUI_PARITY_VERIFIED`.** An initial checkpoint incorrectly classified all remaining T44 work as externally blocked. Independent review found safe narrower-geometry and replay investigations still available; T44 was restarted and the immutable block checkpoint retained as historical evidence, not a current disposition. The owner amendment above changes only the approved undo/redo requirement; prior failed comparators are unmodified and unrelated mandatory specifications remain required. Pinned original: opencode v2.0.12 commit `2670273ff17da96f85c5826ced57aa1b368754fa`, executable SHA-256 `2b0825721cb12f9bca3d5099588087d557a21ed2b5b56efebea3f17dc5f79e6a`. The Rust workspace's real version is `0.1.0` (`Cargo.toml`, `shell.rs` uses `CARGO_PKG_VERSION`).

## Known exact-gate differences and tested alternative

The owner amendment requires full-frame **exact** original/native styled cells, cursor and PNG, no masks or fabricated provider/UI results (`tui-recovery/T44_CONTRACT_AMENDMENT.md:95–105,124–143`), and requires Home trigger frames for VIS25 and VIS26 (`ACCEPTANCE.json:338–360`). The pinned original renders its **own** app version on Home at widths at least 64 (`opencode/packages/tui/src/feature-plugins/home/footer.tsx:7–12,97–100`); native renders **its own** cargo package version (`crates/oc-tui/src/shell.rs:777`). At 120×40, VIS26 filtered and after-Tab frames differ in exactly six version cells while their menu/draft cells match (`evidence/tui/mention-style-followup.md:5–18`). Falsifying native's version, changing the reference, or masking cells is disallowed. Below 64 columns the pinned Home hides its version. After a native visibility/empty-footer centering correction, the independent `home-visibility-20260925-06` 63×24 paired **Home** frame is exactly equal in all 1,512 styled cells and its PNG; the *completed session* remains different. This is a real alternative for that one geometry, **not** a replacement for 120×40 or the required Home autocomplete scenarios. At 44×24, a random original Home example versus a different random native example still makes the full frame unequal after positioning matches (`home-visibility-20260925-07`).

VIS26's unfiltered `@` trigger in the pinned original displays skills/non-primary-agent sections, which native omits. The amendment explicitly assigns that exposure to **T45** and calls the omission a *supported difference, not a PASS* (`T44_CONTRACT_AMENDMENT.md:37–44`; `ACCEPTANCE.json:351–360`). Under the measured 120×40 Reader/tools fixture the trigger differs in 304 Home and 464 session styled cells; filtered and post-Tab file suggestions themselves match apart from truthful version/elapsed-time cells (`evidence/tui/mention-style-followup.md:9–20`). T45 is ready work, not evidence that no independent T44 work remains. A different fixture with no eligible non-file suggestions or completion of T45 may permit further controlled comparisons; neither has been qualified here.

Completed sessions also display independently measured elapsed times: the paired 121×40 real-sidebar/read/copy attempt `evidence/tui/toast-overlap-20260925-06/{toast-overlap-before,toast-overlap-toast}.grid-diff.json` has **3/4,840 different cells only at (37–39,10)** after the sidebar/title/toast regions became identical; both PNG comparisons still differ by 167/652,800 pixels. At 120×40 the new source-built Models-selected frame differs by five real elapsed digits alone after removing an unpinned native selection-time toast (`evidence/tui/models-feedback-followup.md:17–27`). These separately run live turns have no shared test clock. A supported, identical **durable replay fixture with the same recorded timing metadata** might qualify *replay rendering* without hiding values; this path is untested and cannot replace required live-turn behavior. Do not synthesize a duration, mask cells or rewrite source captures to claim equality.

There are further **product gaps** that can be researched independently: VIS09's real upstream `View all integrations` action is absent natively. A later implementation records the original's durable `model-switched` transcript row at the *next accepted prompt*, not dialog selection (pinned `component/prompt/index.tsx:1274–1306`, `core/src/session/session.ts:90–102`). Source-built paired original/native `evidence/tui/model-switch-report.md` verifies the real next-prompt effect and correct provider request without early/fake notice; the final full frame remains DIFFERENT solely in independently measured elapsed digits. Advertising an inert Integrations action would violate C5. The full VIS01–VIS28/V09 matrix is not declared PASS. VIS27's OS clipboard destination remains unverified in this non-graphical PTY environment: OSC 52 + a toast proves a terminal write attempt, not system clipboard readback. All reported full-frame DIFFERENT attempts remain immutable.

The source-built [`two-escape-report.md`](../tui/two-escape-report.md) verifies that one Esc now arms the original five-second interrupt guard and a second Esc really cancels a held provider request. Its older interrupted frame remained DIFFERENT 29/4800 cells, isolated to the real generated session title. A newer [`early-title-report.md`](../tui/early-title-report.md) documents the native owner starting genuine concurrent title generation after durable input acceptance. A single later 120×40 interrupted full frame is exactly EQUAL in 4,800 styled cells and 647,040 PNG pixels; the next independent repeat differs by one real elapsed digit. Neither a coincidental equal frame nor a code gate qualifies the full VIS28/V09 matrix. No title text or timing was fabricated.

## Independently completed work and verification

Earlier independently tested/pushed slices include `23b069a` (real Models keyboard-centered scroll), `4c345c5` (hyphenated sidebar title source layout) and `f40986d` (no premature model-selection toast). Both the model selected action and its real second-turn provider request pass on original and native; the source-associated real PTY captures have full-grid/PNG `DIFFERENT` status, reported in `evidence/tui/{models-interaction-report,models-feedback-followup,sidebar-title-wrap-report}.md`. Earlier real slash/mention/reasoning/streaming/keyboard/clipboard diagnostics remain linked from `evidence/tui/`; no older PASS is promoted to final V09.

Additional committed source `ae17373` aligns narrow Home footer and completed attribution clipping. A subsequent 44×12 Home capture exposed a real regression: the native 3-row top spacer did not shrink and cropped the prompt; the original spacer has `minHeight={0}`/`flexShrink={1}`. The independent `evidence/tui/home-height-report.md` preserves the failed capture, the corrected 44×12 and 44×13 original/native frames, and the 44×15 equal-Home control. After the correction the 44×12 full Home frame remains DIFFERENT only in its random example (20/528 styled cells), while the completed session differs in real elapsed time. At 44×11, both programs complete turns but their answer text scrolls offscreen; a capture-only guard correction now retains completed frames with a visible actual attribution and provider receipt rather than falsely timing out, and full-session comparisons remain DIFFERENT. Nothing is promoted to VIS/V09 PASS by this layout fix.

For the **concurrent title** source slice, serialized workspace fmt, locked tests (zero failures), all-target Clippy `-D warnings`, locked build, docs/progress checks, capture JS/Python syntax and diff check **PASS**: `/home/opencode/.local/share/opencode/tool-output/tool_0da95f4fe001MFRpuvCuT6Lf82`. Prior short-Home gate: `/home/opencode/.local/share/opencode/tool-output/tool_0d9f9c9fa0017Tkc8g24VlrSNk`; prior Home footer gate: `/home/opencode/.local/share/opencode/tool-output/tool_0d9ed5ef0001ICIYpexUnLdJBu`. No code gate qualifies V09. Inherited untracked `.opencode/` was never inspected, modified or staged and prevents a literal clean-status V09 claim.

**Current next work:** qualify remaining Message Actions exact frames and supported tool hover/expand behavior, correct successful Shell spacing, investigate same-state public replay with recorded durations, and qualify the T45-dependent mention inventory or a genuinely identical supported fixture. A future owner decision may be needed for genuinely irreconcilable truthful identity on required wide Home frames; do not presume such a decision now, modify the acceptance contract, or claim `TUI_PARITY_VERIFIED` while any required exact comparison fails. The earlier block checkpoint `progress/M9/T44/0055.md` is superseded by resumed active T44; it was not rewritten.

The historical first VIS10 foundation slice mapped painted user-message lines and hover shading. [`message-hover-20260926-01`](../tui/message-hover-20260926-01/report.md) observed the same normal-to-hover transition, with full frames DIFFERENT by two elapsed digits. At that checkpoint there was no menu or durable ID. The resumed implementation now carries durable owner IDs and real actions; see [current functional evidence](conversation-only-report.md). The earlier capture remains immutable and is not promoted to parity PASS.

Hover-slice full serialized regression gate **PASS**: workspace fmt, locked tests, all-target Clippy `-D warnings`, locked build, xterm frontend check, JS/Python syntax, docs/progress and diff checks; output `/home/opencode/.local/share/opencode/tool-output/tool_0dc5c25c1001nUr2e8fxEv7nai`. Earlier attempts exposed one transient PTY cursor wait (targeted rerun passed) and an MCP fixture counting a concurrent title as an unsafe main retry. The fixture now counts structured main requests separately at all assertions in the affected scenarios; main/tool retry prohibitions remain asserted. No test was skipped or expectation changed for an actual retry.
