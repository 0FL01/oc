# T48 direct Zen synthetic smoke preflight

## Result

Offline native Zen Chat/catalog integration complete enough for a single synthetic, keyless **text-only** direct smoke: exact current selected model is explicitly passed in test environment, price/protocol/tool/text status rechecked in both public catalogs immediately before any POST. Only truthful `oc/<version>`, own stable `x-opencode-session`, no bearer if no product key; no OpenProxy, Go, Responses-Zen or spoofed identity. This checkpoint is before the external attempt, not a PASS.

## Checks

Opt-in read-only official catalog GET passed; its current zero-priced Chat intersection included the explicitly chosen `big-pickle` at this instant. Local fake binary tests 4 PASS including tool/results/restart and two price-change timing cases; Zen parser/cancel/auth tests PASS; workspace clippy/tests previously exit 0 before subsequent bounded-live instrumentation, current targeted clippy/test to be repeated after. Campaign envelope is capped in `zen_chat::reserve_live_smoke` by 24 durable create_new slots under a canonical owner-private directory, output ≤2048; this one test sets output 32 and uses no MCP/search/title/child and no retries. A failed send consumes a slot, not repeated after unknown outcome.

## Risks

Two public catalog snapshots are not an atomic Zen billing quote; dynamic free offers/privacy terms can change, so prompt is synthetic and short. Anonymous direct-client entitlement is unproven; 401/403/429 must be reported without header spoof, endpoint hopping or automatic retry. No key read from authoring-agent config or secrets.env. The T27 campaign remains independently blocked; this narrow T48 smoke does not replace it.

## Next

Use one isolated keyless test (`OC_TEST_ZEN_LIVE=1`, `OC_TEST_ZEN_MODEL` explicit, `OC_TEST_ZEN_CAMPAIGN_DIR` private persistent, no product credential), record only sanitized exit/category and count the marker; stop on access refusal. Then finish missing offline DCP/child coverage and rerun applicable gates.
