# T49 — model identity / ancillary Responses attribution

Status: **offline regression PASS; scoped live text PASS with reissued credentials**. This does not identify the owner's three historical dashboard rows or prove the provider's internal route behind the requested model ID.

## Result

- A completed TUI turn now uses the model label pinned in its `TurnPresentation`, not the picker's current label, in the live footer. Persisted history already uses that turn label. No provider routing or retry policy was changed.
- An actual-binary PTY test chooses two differently named models on consecutive turns and checks the exact main Responses `model` IDs, the live and reopened footers, and the one separate title POST. There is no offline evidence of a Muse-to-DeepSeek substitution in the main Rust request path.
- A fake Responses peer verifies that cancellation of a pending title closes its socket without changing the committed main turn to `Interrupted`; the ten-second title watchdog likewise closes a stalled title request while the main turn remains `Completed`. The title is an ancillary request; this does not identify the owner's dashboard rows. Terminal `TurnFinished` remains after title work: an experimental early event broke the TUI title/turn ownership contract and was removed after targeted and workspace tests exposed the regression.
- Three existing stdio MCP retry tests now wait for the TUI's terminal failure row before submitting the retry and inspect the final rendered screen, rather than treating an earlier tool card or an output byte sequence as a terminal-event barrier.

## Checks

`cargo fmt --all -- --check`; `cargo clippy --locked --workspace --all-targets -- -D warnings`; `CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=2 cargo test --locked --workspace`; `cargo build --locked`; `target/debug/oc --help`; `python3 scripts/check_docs.py`; `git diff --check`: **PASS**. New targeted tests: `selected_model_is_the_main_wire_model_and_each_turn_keeps_its_footer`, `title_cancel_and_watchdog_leave_the_main_turn_completed`, `live_footer_uses_the_turns_pinned_model_not_the_current_picker`, and the offline one-shot reservation check: **PASS**. The T27 external-campaign guard test still passes and its external live test remains ignored/blocked.

An isolated, explicit T49 product-binary smoke was attempted with test values from gitignored `.local/live.env`. A fixed, create-new + fsynced private reservation was consumed before generation: at most `(8 main rounds + 1 title) × 2 attempts = 18` generation POSTs. The initial fixture incorrectly interpreted the `ocg/` prefix of the test model as a *local provider namespace* and would have used only `muse-spark-1.3-contributor` on the wire; its binary exited non-success before any model execution could be established. It capped main output at 768 and title at 256, denied tool execution and MCP, and set a 90-second watchdog. Raw stdout/stderr and secrets were not emitted. A separate one-shot, at-most-two-attempts adapter diagnostic using the same URL/key and **unqualified** `muse-spark-1.3-contributor` ID returned the typed `unauthorized` outcome. A preceding attempted diagnostic panicked before entering a Tokio runtime and made **no** network call; its reservation was conservatively retained. The fixture code was subsequently corrected to preserve slash-containing **remote wire IDs** independent of the local provider namespace.

At the owner's explicit request, the final two attempts of this **same campaign** were reserved for a single tool-free native Responses adapter request with exact wire model `cx/gpt-6-luna`, max output 256 and a 35-second watchdog. This request also received `ProviderError::Unauthorized` (HTTP 401), without a successful generation. It did not route through the TUI or a multi-round product turn and does not establish a server-side model substitution. Total conservative reservations are now **18 + 2 + 2 + 2 = 24 of 24**; all four immutable reservation records live only under gitignored `.local/`. No automatic retry or new campaign ID was used.

After the owner reissued the test key (the gitignored credential file was modified), an explicitly requested **separate fixed-ID campaign** was started without resetting any of the four old reservations. The native adapter generated nonempty text with provider usage for exact `cx/gpt-6-luna` (2 attempts reserved). A single isolated real `oc run` then selected `t49proxy/cx/gpt-6-luna` in the effective product catalog, keeping exact `cx/gpt-6-luna` as its wire model ID; the binary exited successfully, and SQLite persisted that model ID with a `completed` turn (18 further attempts reserved). Finally, one tool-free direct adapter generation on the exact original `OC_TEST_MODEL=ocg/muse-spark-1.3-contributor` succeeded with nonempty text and provider usage (2 further attempts reserved). The new campaign conservatively reserves **2 + 18 + 2 = 22 of 24** HTTP attempts, including retries and ancillary title/rounds. Each call was one-shot with a create-new fsynced reservation before sending; no raw live response, key or headers were logged. A direct success for the original wire ID plus the actual-binary Luna smoke does **not** claim that the original TUI run executed Muse: historical dashboard correlation is unavailable.

## Risks / blocker

- The original key's HTTP 401 responses are historical facts, not evidence of a Rust model fallback: both exact requested wire IDs succeeded with the reissued credentials. The provider's **internal executed-model identity** and the owner's old dashboard rows are still not independently correlated to a Rust session/turn ID. Do not retry the old fully reserved campaign or reinterpret its first binary failure as provider-side routing evidence. The new campaign has only 2 reserved attempts of allowance left, insufficient for another unmodified full product turn's worst-case bound.
- Headless/TUI still await the bounded title attempt before publishing the terminal turn event. Avoid a detached title task or early terminal event without an owned title lifecycle and TUI state reconciliation. A ten-second interrupted auxiliary request is possible by design, and 0/0 in the provider dashboard does not prove zero upstream inference or cost.
- The PTY tests use intentionally distinct fixture model IDs/names, not proprietary provider execution proof. Existing `.opencode/` is pre-existing untracked content and was not modified.

## Next

Keep the verified offline changes and both campaigns' durable reservation records. If exact attribution of the historical DeepSeek request or a real product TUI run with Muse is required, use provider-side correlated request/route evidence and a separately reviewed bounded campaign; do not silently reuse this 22/24 reservation for a full turn. T27's broader live harness remains fail-closed and is not unlocked by this narrow text-only qualification.
